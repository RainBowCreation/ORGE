{
  "summary": "Drift audit — run the 5 MISSING dims, union with 5 salvaged dims, synth + adversarial verify (engine @ c148f49 vs law+v4)",
  "agentCount": 52,
  "logs": [
    "Audit complete: 50 raw findings across 5/5 missing dimensions",
    "Synthesis: 23 NEW unique, 20 known-confirmed, 10 pending, 8 dropped",
    "Verify: 15 confirmed REAL new drift, 3 already-labeled, 5 refuted"
  ],
  "result": {
    "rawCountBatchB": 50,
    "newConfirmed": [
      {
        "id": "ND-1",
        "clause": "Law #2/#4: gravity impulse applied ONCE in ENCODE on EVERY cell.",
        "code_site": "core/engine_b.hpp:1681-1695 (swap DECODE path) vs additive path :1755-1758",
        "description": "A cell selected for a cross-species swap takes the partner's snapshot velocity (prev-tick p/mass) and skips ENCODE's -dt*g impulse for this tick; the additive path uses ugy=uy_snap-dt*g, the swap path uses raw snapshot vy with NO -dt*g. A swapping pair loses one tick of gravity acceleration whenever the cadence fires. Bounded, one tick, but a hole in 'gravity applied once on every cell'.",
        "severity": "medium",
        "evidence": "swap path `float uy=sp->vy[j]` (sp->vy = C.py/mass, no per-tick gravity); additive path `e.ugy = uy - dt*G.g` (:256). Batch B law-1-3#1 [DRIFT-NEW]; not in queue/labeled.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "Confirmed the mismatch survives a concrete REFUTE attempt. Orchestration (engine_b.hpp:1882-1889): `WorldSnapshot snap = snapshot_world(world)` is captured at the START of step_world, from persisted Chunk::px/py/pz (decode writes p=ux*mass at :1712 with NO gravity baked in). `encrypt_world` then writes gravity into a SEPARATE buffer `enc` via `e.ugy = uy - dt*G.g` (:256) — gravity never goes back into `snap`. Therefore the two DECODE paths diverge: the additive path reads `ugy=e->cells[i].ugy` (:1755, gravity-applied) while the swap path reads `uy=sp->vy[j]` (:1685) where `sp` is the pre-ENCODE snapshot (gravity NOT applied). The swap branch (1681-1715) contains no compensating -dt*g term; the :1679 comment only addresses buoyant up-reaction, not the gravity body impulse. The v4 spec is explicit against this: §6.1 (line 223) normatively defines \"Snapshot = the post-ENCODE state (gravity + external already in momentum)\" and §7.1 says the swap permutes momentum — i.e. the gravity-inclusive momentum. The code's C++ snapshot is the PRE-ENCODE image, so the swap path's permuted payload omits the tick's gravity impulse, violating Law #2/#4 \"gravity applied once on every cell.\" Strongest REFUTE (the omission is one-tick/de-minimis) fails: a cell in a sustained convection/swap column loses one dt*g per cadence fire, recurring, not a one-time event. No compliant reading of the cited code/clause reconciles the gap.",
        "labelReasoning": "Mechanism confirmed against c148f49. ENCODE applies the per-tick gravity impulse `e.ugy = uy - dt*G.g` (engine_b.hpp:256). The additive DECODE path consumes that gravity-updated `e->cells[i].ugy` (:1755-1758), but the swap DECODE path reads `sp->vy[j]` (:1685), which `snapshot_world` defines as `py/mass` = the prev-tick PERSISTED velocity (sim_engine.hpp:445-451) — no `-dt*g` for the current tick. A cell selected for a cross-species/§7 swap therefore loses exactly one tick of gravity acceleration. The DESIGN-LAW / v4 spec header states gravity is applied 'ONCE into momentum … on every cell' / 'once per tick (first sweep only)'; the swap path is a real exception to 'every cell', so this is genuine drift (not refuted). It is NOT already tracked: no debt label (T3-OPEN / TODO(T4/T10) / DEBT / ARG-PARITY / owner) sits on the swap region (engine_b.hpp:1676-1716); the ratification queue (items 1-8) in docs/superpowers/handoffs/2026-06-11-engine-b-v4-T4-continuation.md does not cover it (item 7's 'post-ENCODE −g·dt persisted velocity' is about the κ·divU pressure source, a different site; T8's '§7 full-payload swap' is about payload completeness, not the missing gravity impulse); and no commit (git log) or test references it. Severity is bounded/low-medium: the lost impulse is one tick (10 m/s²·dt), recovered the next tick via ENCODE, the swap is cadence-gated (t_swap_min=0.5 s) so it fires infrequently, and conservation is untouched (the swap remains a pure permutation of persisted momentum).",
        "disposition": "Code fix in a future task: in the swap DECODE branch (engine_b.hpp:1685) seed the swapped-in velocity from the partner's post-ENCODE gravity-updated momentum (e->cells[j].ug*) rather than the pre-gravity snapshot vx/vy/vz, so the permuted payload carries the tick's -dt*g impulse like the additive path — preserving the §7.1/§6.1 \"post-ENCODE momentum\" invariant and pure-permutation conservation."
      },
      {
        "id": "ND-2",
        "clause": "Law #4: DECODE is per-cell local (derive T/v, relabel, write back); cross-cell work confined to RESOLVE.",
        "code_site": "core/engine_b.hpp:1761-1787 (normal DECODE) + :1690-1695 (swap path)",
        "description": "DECODE (decrypt_world) performs 1-hop NEIGHBOR reads via wall_neighbor() and xspecies_noflux_neighbor() to zero velocity components into wall / cross-species no-flux faces, plus the swap path reads the partner cell snapshot. The no-penetration BC is physically a RESOLVE face condition applied late in DECODE. Mitigating: reads are of the immutable pre-step snapshot, velocity-only, 1-hop, order-independent (GPU-safe, no fabrication).",
        "severity": "low",
        "evidence": "`if(ux>0 && wall_neighbor(...))ux=0;` (x6) and `xspecies_noflux_neighbor(...)` (x6) at :1761-1787. Batch B law-4-5#1 [DRIFT-NEW]; not in queue items 1-22.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I read the cited code and clauses to try to refute this, and the mismatch survives. CODE (engine_b.hpp:1761-1787 normal path, 1690-1695 swap path): DECODE (decrypt_world) calls wall_neighbor() and xspecies_noflux_neighbor() to zero velocity components pointing into wall / cross-species no-flux faces. Both helpers (1598-1647) genuinely read a neighbor cell — `snap.find(ncx,ncz)` then `sj->matIx[j]`/`sj->mass[j]` — i.e. real 1-hop cross-cell reads, not cell-local. CLAUSE (DESIGN-LAW.md Law #4, lines 30-33): \"RESOLVE is the **only** cross-cell step ... DECODE is per-cell **local** (derive T/v, relabel, write back).\" The no-penetration / cross-species no-flux is a face condition; §6.3 of the spec (lines 249-251) makes \"no partial cross-species flux\" a RESOLVE-domain structural rule, and line 182-183 makes \"solid faces: closed\" a force/flux condition — both belong to RESOLVE, yet they are being enforced in DECODE via neighbor reads.\\n\\nI tried three refutation paths and all failed: (1) \"the spec sanctions DECODE-time velocity invariants\" — line 176/179 do place vel_damp, the CFL cap, and the void floor in DECODE, but every one of those is strictly CELL-LOCAL (no neighbor read); they do not license cross-cell reads. (2) \"Law #4's 'local' only means no-neighbor-reads for ENCODE\" — line 31 attaches the explicit \"no neighbor reads\" gloss to ENCODE, but Law #4's headline still names RESOLVE as \"the only cross-cell step,\" which is a global constraint DECODE's neighbor reads violate regardless of the ENCODE-specific gloss. (3) \"the spec contemplates DECODE doing cross-cell work\" — the opposite: §6.4 line 258 shows the design pattern is for DECODE to FLAG and the NEXT RESOLVE to do the cross-cell drain, precisely to keep DECODE local; the wall/no-flux zeroing breaks that pattern by reading neighbors in DECODE itself.\\n\\nThe finding's own mitigations are accurate and load-bearing for severity: the reads are of the immutable pre-step snapshot, velocity-only (no mass/E moved), 1-hop, order-independent (GPU-safe), and fabricate nothing — a pure clamp-to-zero. That makes the deviation harmless, not absent. The structural mismatch (cross-cell reads in a clause-designated per-cell-local step) is real but cosmetic in effect. This is a genuine NEW finding (not in queue items 1-22, per the evidence note).",
        "labelReasoning": "VERIFIED THE SITE. At engine_b.hpp:1761-1787 (normal DECODE path in decrypt_world) and :1690-1695 (swap path), DECODE calls wall_neighbor() (:1598) and xspecies_noflux_neighbor() (:1629), each doing a 1-hop neighbor read to zero velocity components pointing into a wall or cross-species no-flux face. Confirmed both helpers read ONLY the immutable snapshot (snap.find / sp), and the effect is velocity-only (ux/uy/uz set to 0; mass/E/P untouched) — so order-independent, GPU-safe, no fabrication, as the finding states.\n\nTENSION IS REAL. DESIGN-LAW.md:30-33 (the cited Law #4): \"DECODE is per-cell local (derive T/v, relabel, write back)\" and \"RESOLVE is the only cross-cell step.\" A 1-hop neighbor read in DECODE is literally cross-cell, so the no-penetration BC applied late in DECODE does deviate from the strict letter of Law #4. Not REFUTED — the reads exist and the law says local.\n\nNOT ALREADY-LABELED. Searched the cited lines and both helper functions for T3-OPEN / TODO(Tn) / DEBT / ARG-PARITY / FIXME / owner labels: none on these lines. The surrounding comments justify the reads as intended design (\"Same free-slip no-penetration the wall path enforces\"), not as flagged debt. Searched the known queue (handoffs/2026-06-11-engine-b-v4-T4-continuation.md): line 109 blesses '1-hop reads of snapshot or prior-pass buffers' but that is about the RESOLVE micro-pass rebuild, NOT about DECODE taking neighbor reads. The xspecies helper's 'deferred to T5' note concerns cross-species MASS displacement (the SWAP), not the Law-#4 locality of the velocity-zeroing. None of queue items 1-22 / F-folds / T4-T10 name the DECODE no-penetration BC or wall/xspecies neighbor reads. git log -16 has no matching entry. So it is genuinely un-owned.\n\nCONCLUSION: REAL-DRIFT, low severity. It is a benign, structurally-safe deviation (velocity-only, immutable-snapshot, 1-hop, order-independent) that the v4 design philosophy already implicitly tolerates (it blesses identical 1-hop snapshot reads in RESOLVE), but Law #4's text has not been amended to permit DECODE doing the same. Best fixed by a one-line law/spec clarification rather than code surgery.",
        "disposition": "Record as an accepted note and resolve via a small spec amendment to v4 (§5/§6 or the DECODE bullet at line 176) that explicitly sanctions DECODE-time velocity-only, snapshot-read, 1-hop no-penetration / cross-species no-flux clamps as boundary reactions (not cross-cell transport) — OR, if strict Law #4 locality is to be preserved, a low-priority code-fix task to move the no-penetration zeroing into RESOLVE (or to a flag consumed next RESOLVE, matching the §6.4 sub-min pattern); no behavioral change is required since the reads are conservation-neutral."
      },
      {
        "id": "ND-3",
        "clause": "Spec §2.1:98 'Cross-gas faces compare ABSOLUTE pressure (p_eos + P0 each side) so per-gas gauge offsets cancel.'",
        "code_site": "core/engine_b.hpp:754-757 (force GAS-j branch) and :484/:490/:498 (relaxation gas-neighbor base = cx.anchor[j])",
        "description": "The mandated cross-gas ABSOLUTE-pressure comparison is implemented NOWHERE. At an air|steam face both the §3.1 relaxation stencil and the §4 force read each side's GAUGE p_eos and never re-add per-gas P0. Air P0 (~99.1 kPa) and steam P0 (~103.4 kPa) differ ~4.3 kPa, so two resting gases at true mechanical equilibrium read a spurious ~4.3 kPa gauge difference, injecting a phantom gradient into P and the momentum-bearing force. Air|steam faces are reachable (boiling water). DISTINCT from queue #18 (antisymmetry) and #1 (force-reads-anchor): even a perfectly antisymmetric force-reads-anchor impl is wrong here because it compares gauges not absolutes.",
        "severity": "high",
        "evidence": "Spot-verified: P0 is only ever SUBTRACTED to form the gauge (engine_b.hpp:204) and grep finds no +P0 re-add at any face. Relaxation `base=cx.anchor[j]` (floored gauge); force `a_j=...p_eos4` (raw gauge). Batch B spec-2-3 [DRIFT-NEW].",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "Refutation attempted on every axis and the mismatch survives. (1) The clause is real and normative: spec §2.1:98 states \"Cross-gas faces compare ABSOLUTE pressure (p_eos + P0 each side) so per-gas gauge offsets cancel,\" and it is logged as a ratified v4.1 decision DEC-v4-B at line 405 (\"absolute-P comparison across gas pairs\"). It is NOT retracted — the line 100-102 deletion only removed the v4.0 \"(+ gas p_eos)\" force double-count, a different thing. (2) The code implements it nowhere. p_eos_v4 (engine_b.hpp:197-205) returns the per-gas GAUGE p_abs − P0; P0 is only ever SUBTRACTED. grep confirms defaultMass·R·Tref (=P0) is used only to FORM gauges: :203 (gauge), :225 (anchor floor), :833 (empty-cell-as-gas anchor = −P0, still gauge). No +P0 re-add exists at any face. The §3.1 relaxation reads cx.anchor[j] (floored gauge, :484/:490/:498) and the §4 force reads ef->cells[fj].p_eos4 (raw gauge, :756-757) — both per-gas gauge, never absolute. (3) Physical consequence verified: at an air|steam face both gases at rest read p_eos=0 (gauge), so the force/P see no gradient, but their absolute rest pressures differ ~4.3 kPa (air P0≈99.1 vs steam P0≈103.4 kPa). The gauge model erases that real 4.3 kPa absolute difference — a false equilibrium — exactly the failure §2.1 says to avoid. (Minor: the finding says \"injects a phantom gradient\"; the more precise framing is that it ERASES a real absolute difference, but the code-vs-spec conclusion is identical.) (4) Reachable: §8.4 (line 322) confirms boiling water → steam, so air|steam faces occur. (5) Distinct from already-labeled queue items: the partial-findings doc records the gas-face raw-p_eos read as queue #1 (face-value form ½(P_i+Φ_f) vs a_j+hydrostatic, finding #2) and queue #4 (gas_anchor_v42 floor + dual raw/floored gauge readings per cell, finding #22). Neither addresses the cross-gas P0-cancellation between two DIFFERENT gases (§2.1:98); that is a separate clause with a separate consequence, so this is genuinely NEW.",
        "labelReasoning": "VERIFIED the mechanism, then checked every tracking surface. Mechanism confirmed: spec §2.1:98 (specs/2026-06-10-engine-b-unified-spec-v4.md) mandates \"Cross-gas faces compare ABSOLUTE pressure (p_eos + P0 each side) so per-gas gauge offsets cancel.\" The code does this NOWHERE. p_eos_v4 at engine_b.hpp:204 only ever SUBTRACTS P0 to form a gauge; the only +P0 re-adds (lines 805/833) build empty-cell/refill anchors, not absolute face comparisons. The §3.1 relaxation gas-neighbor base reads the floored gauge (cx.anchor[j], lines 484/490/498) and the §4 force GAS-j branch reads the raw gauge a_j = ef->cells[fj].p_eos4 (line 756). The GAS i|gas j header doc (lines 666-672) explicitly defines a_j as \"the RAW p_eos4\" — a gauge — and its rationale covers only raw-vs-floored anchor choice and antisymmetry, never P0 re-add. So air (P0~99.1 kPa) vs steam (P0~103.4 kPa) at a resting air|steam face yields a spurious ~4.3 kPa gauge difference that feeds P and the momentum-bearing force. Reachable via boiling water.\n\nNOT tracked with an owner anywhere: (1) Ratification queue items 1-7 in handoffs/2026-06-11-engine-b-v4-T4-continuation.md do not include it — item 1 is force-reads-anchor (candidate correctly distinguishes; the gauge defect survives even when reading relaxed P because the relaxation stencil itself feeds the gauge anchor). (2) T4 fold-in line 121 \"gas|gas faces carry side-dependent values off-equilibrium (bounded momentum-fabrication channel)\" is about antisymmetry/evaluation-side (queue #18), NOT the gauge-vs-absolute P0 offset — distinct: a perfectly antisymmetric gauge comparison is still wrong here. (3) No DEBT/TODO/T_n/ARG-PARITY label near the cited sites mentions absolute/P0/cross-gas. (4) commit c148f49 body and git log -16 do not mention it. The ONLY occurrence is the audit's own hunt prompt in notes/2026-06-11-drift-audit-workflow.js dim spec-2-3 (\"the cross-gas ABSOLUTE-pressure comparison (air|steam faces) — implemented, absent, or moot?\") — that is the question this finding answers (answer: absent), not a tracked/owned debt. The candidate's mechanism, code sites, and distinctness claims all hold.",
        "disposition": "Fold into the existing user-ratification queue alongside items #1/#4 as a v4.2 candidate (same gas-face force/anchor machinery): either implement the cross-gas absolute-pressure comparison (re-add per-gas P0 only when the two endpoints are DIFFERENT gas species, so same-gas faces stay gauge-cancelled) in task T4's flux-intent/force rebuild, OR — if the resting-air|steam false-equilibrium is judged acceptable in-game — amend §2.1:98/DEC-v4-B to drop the absolute-P mandate and document the gauge-only approximation; correct the finding's \"injects a phantom gradient\" wording to \"erases the ~4.3 kPa absolute difference.\""
      },
      {
        "id": "ND-6",
        "clause": "Spec §1.2 schema 'τ_y [Pa]'; §5.1/§7.3 swap gate 'τ_y·A_face [N]'.",
        "code_site": "core/engine_b.hpp:157-161 (swap_resistance); core/sim_engine.hpp:56 (Material::yieldStress declared 'N')",
        "description": "The yield term in the swap gate omits the ·A_face factor and the Material struct declares yieldStress in Newtons, contradicting τ_y [Pa] with a Pa→N conversion via A_face. Also the spec does not specify the max() pair-combination used. Numerically inert today (A=1 m², τ_y∈{0,∞} across LUT) but the manifest UNIT and the formula text both mismatch the ratified spec; any future A≠1 or finite-τ_y material silently inherits the wrong scaling.",
        "severity": "low",
        "evidence": "`float yld=std::max(a.yieldStress,b.yieldStress);` added straight into resistance[N] with no `*G.A`; struct comment `// N — Bingham yield` vs §1.2 `τ_y [Pa]` / §5.1 `τ_y·A_face [N vs N]`. Batch A spec-4-5#9 + law-8-9#9 [DRIFT-NEW]; no owner label.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "REFUTE attempt fails; the mismatch survives on all three sub-claims and is a genuine (if inert) spec/code divergence.\n\nVerified facts:\n- Code engine_b.hpp:157-161 swap_resistance() returns coh + yld where coh = swap_kc·min(minMass)·g and yld = max(a.yieldStress, b.yieldStress), with NO ·A_face / ·G.A on the yield term.\n- Struct sim_engine.hpp:56 declares `float yieldStress // N — Bingham yield`.\n- Ratified v4 spec §1.2 schema (line 55) declares `τ_y [Pa]`; force tables §5.1 (lines 28-30, 190) and §7.3 (line 269) write the yield threshold as `τ_y·A_face [N]` — a Pa→N conversion via face area. So spec stores Pa and multiplies by A; code stores N and never multiplies. That is a real unit + formula divergence.\n- The pair-combination (max()) is genuinely unspecified in the spec, which writes a single `τ_y` with no combine rule.\n\nRefutation levers I tried and why they fail:\n(A) G.A = 1.0 m² (sim_engine.hpp:33) makes τ_y·A numerically equal τ_y — but only if τ_y were stored in Pa. The struct stores N and never multiplies, so A=1 makes it inert today (the finding concedes this) without making the code structurally compliant; any A≠1 or finite τ_y silently mis-scales (spec wants force ∝ area, code gives area-independent force).\n(B) Treating the unit comment as a mere label fails: §1.2 is the ratified manifest schema and the LUT-schema test even names the N-declared field \"tau_y\" (engine_b_lut_schema_test.cpp:115), conflating the two — masked only because the value is 0.\n(C) Strongest lever: the spec is itself internally inconsistent — its cohesion term k_c·min(minMass)·g (§7.3) also carries NO A, so the spec mixes a force-valued cohesion term and a stress×area yield term in one sum. The code faithfully reproduces cohesion-without-A but then ALSO drops the A on yield and relabels Pa→N. This does not make the code compliant with the literal yield formula; it shows the spec needs the fix more than the code does.\n\nInertness confirmed: across the real LUT every yieldStress ∈ {0, ∞} (engine_b_lut_schema_test.cpp:115/134/165 — water 0, ice/stone INF) and G.A = 1, so 0 vanishes and ∞ gates regardless of A. No current scenario diverges numerically — hence severity low, not a ship blocker.\n\nVerdict: REAL-DRIFT. Documentation/formula/unit mismatch against the ratified spec, numerically inert today, latent mis-scaling for future finite-τ_y or A≠1 materials. Not previously labeled with an owner.",
        "labelReasoning": "Verified at c148f49. CODE: sim_engine.hpp:56 declares `float yieldStress` with comment `// N — Bingham yield (D7)` (a pre-v4 unit, introduced issue #2 commit 0bba93f). engine_b.hpp:159-160 `float yld = std::max(a.yieldStress,b.yieldStress); return coh + yld;` adds the yield straight into a resistance carried in [N] with NO `*G.A`. SPEC (docs/superpowers/specs/2026-06-10-engine-b-unified-spec-v4.md): §1.2 schema line 55 lists `τ_y [Pa]`; §0 table lines 28-30 and §7.3 lines 269-270 write the gate term as `τ_y·A_face` / `τ_y·A` [N]. So three genuine mismatches: (1) manifest UNIT N vs Pa; (2) missing Pa→N conversion `·A_face`; (3) the max() pair-combination is unspecified by the spec. INERTNESS CONFIRMED: G.A=1.0f (engine_b.hpp:33) so `τ_y·A_face`==`τ_y` today, and yieldStress is {0,INF} across the LUT (real_lut/lut_schema_test: water=0, ice/stone=INF) so max() and the unit confusion are both no-ops — the finding's \"numerically inert today\" is accurate. NOT TRACKED: no T?-OPEN / TODO(T) / DEBT / ARG-PARITY / owner label sits on swap_resistance or the yieldStress field regarding this; the surrounding comments (engine_b.hpp:152-154, 247-252) only DEFER the granular-yield *mechanism* (§8.6 / [[engine-b-deferred-solid-yield]]) and explicitly call it \"0 for fluids => NO-OP\" — they never flag the unit/A_face/pair-combination spec mismatch. The handoff debt list (2026-06-11-...-T4-continuation.md lines 128-131: find_chain_hop, INV-AL) doesn't mention it, and git log -16 has no commit touching the τ_y unit (T1 bf4c35e/19303d5 added the v4 schema but left the old `// N` struct comment unreconciled). This is a latent drift: a future finite-τ_y or A≠1 material silently inherits the wrong scaling.",
        "disposition": "Spec amendment (not code fix): amend v4 §1.2/§5.1/§7.3 to make the swap threshold dimensionally coherent in a single convention — the code's all-N convention is the sounder one, so redefine τ_y as a force [N] (or define both cohesion and yield as stress×A_face uniformly), specify max() as the pair-combination rule, and fix the sim_engine.hpp:56 `// N` vs schema `[Pa]` comment to match; no behavioral code change since all LUT τ_y ∈ {0,∞} and G.A=1 make it inert."
      },
      {
        "id": "ND-7",
        "clause": "Law #6 advection 'moving mass carries its momentum ṁ·u_donor' + Law #7 'store EXTENSIVE; a stored raw v is the velocity-ghost drift', at the law-#9 place/break boundary.",
        "code_site": "core/sim_engine.hpp:743-802 (relocate_chain), :804-844 (apply_injections), :820-823/:838-841 (direct writes)",
        "description": "The injection/displacement seam moves mass WITHOUT its momentum: relocate_chain and apply_injections write only (matIx, mass_kg, T_curr)+re-encoded E; px/py/pz are never moved, zeroed, or permuted. A placed/vacated cell pairs the displaced incumbent's stale momentum with the new mass (next-step v = p_old/m_new — e.g. a 1000 kg water cell's momentum divided by 1.2 kg of pushed-in air), the velocity-ghost class law #7 forbids, on a live path that runs every placement. E is handled at every site, momentum is not. Global Σp untouched (mis-pairing not fabrication); injects spurious velocity + vel_damp heat each displacing placement; CFL/no-penetration bound the consequence.",
        "severity": "medium",
        "evidence": "injection writes 3 fields + persist_cell_E, no px/py/pz anywhere on the path (grep-confirmed); the engine's own RESOLVE swap permutes full payload incl. momentum (engine_b.hpp:1703-1712). Batch A law-6-7#10 + law-8-9#7 + spec-6#12 [DRIFT-NEW]; queued find_chain_hop covers banned-symbol routing, not this payload gap.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I tried four refutations and all failed. (1) \"Momentum isn't persisted state\" — REFUTED: Chunk stores px/py/pz as std::vector<float> persisted fields (sim_engine.hpp:149-151, init :179-181), the exact mirror of E, and law §7 explicitly lists `momentum (px,py,pz)` in the stored-extensive set. (2) \"Velocity isn't re-derived so no ghost\" — REFUTED: snapshot_world derives v=p/m every tick (sim_engine.hpp:442-451: `cs.vx[i] = C.px[i] * inv`, inv=1/mass). A cell whose mass/species was just overwritten by injection while px/py/pz stay stale produces a spurious next-tick velocity; the finding's \"1000 kg momentum / 1.2 kg air\" example is mechanically exact. (3) \"This is the law-§9 place/break boundary source/sink, so it's exempt\" — REFUTED: the law's velocity-ghost prohibition (§7) is universal and unqualified (\"a stored raw v ... is the velocity-ghost drift — forbidden\"), and the displaced-incumbent mass move inside relocate_chain is an INTERNAL move, not the boundary sink — law §6 requires \"moving mass carries its momentum ṁ·u_donor\", so relocating the incumbent's mass to a new cell without its momentum is a direct §6 violation, while the placed cell inheriting the incumbent's leftover momentum is the §7 ghost. (4) \"CFL/damping makes it benign\" — that's the finding's own honest medium-severity scoping (Σp conserved = mis-pairing not fabrication; vel_damp later burns the spurious KE to heat; no-penetration bounds it), not a compliant reading.\n\nCode confirmation: apply_injections (:804-844) and relocate_chain (:743-802) write only matIx/mass_kg/T_curr + persist_cell_E at EVERY site — the terminal vacuum-adopt (:744-747), same-species merge (:750-755), mode-2 displace (:782-785), the mode-2 escape hop (:769-778), the interior backward shift (:791-797), and both final inject writes (:820-823, :838-841). px/py/pz appear nowhere on the path (grep-confirmed). This is asymmetric with the rest of the engine, which handles momentum at every write-back: the RESOLVE swap explicitly persists the moved parcel's momentum `C.px[i]=ux*C.mass_kg[i]...` (engine_b.hpp:1712, comment \"Persist MOMENTUM (law §7) ... the parcel carries its partner's mass AND its momentum wholesale\"), DECODE zeroes momentum when a cell empties (:1748) and for immovable terrain (:1725). E is handled at every injection site; momentum is the lone omission. The mismatch survives the attack: REAL-DRIFT.",
        "labelReasoning": "Verified at code level: relocate_chain (sim_engine.hpp:743-802) and apply_injections (:804-844) co-write only (matIx, mass_kg, T_curr) + persist_cell_E at EVERY site — direct injection (:820-823, :838-841), vacuum adopt (:744-747), same-species merge (:748-755), mode-2 escape relocate (:768-785), and interior chain shift (:791-797). persist_cell_E (:246) re-encodes E alone from (mass,T) and explicitly notes injections run outside the engine passes. Grep confirms px/py/pz appear NOWHERE on this path. The asymmetry the finding claims is real: the RESOLVE swap permutes momentum wholesale (engine_b.hpp:1711-1712) and immovable terrain even zeroes it (:1725), so E and momentum are handled together everywhere EXCEPT the §9 place/break seam, where momentum alone is dropped. This is genuine law drift: DESIGN-LAW §7 lists momentum(px,py,pz) as persisted extensive truth and brands a mass/momentum mispairing a velocity-ghost (next-step v = p_old/m_new, e.g. a 1000 kg water cell's momentum on 1.2 kg of pushed-in air) as forbidden; §9 governs exactly this place/break boundary. Σp is untouched (mispairing, not fabrication), and CFL + vel_damp bound the consequence — consistent with the finding's medium severity. NOT tracked: no DEBT/TODO/T3-OPEN/ARG-PARITY label near the injection sites; the handoff queue's only injection entry (2026-06-11-T4-continuation.md:128, find_chain_hop) targets a MASTER-RULES-banned SYMBOL — route escape through the standard displacement path — not the momentum-payload gap; T8 (§7 full-payload swap) is scoped to the RESOLVE swap, not injection; no commit (git log -16) addresses it.",
        "disposition": "Code fix in a follow-up task T_n: on every injection/relocation write-back set momentum consistently — zero (or caller-supplied) px/py/pz for the placed cell, and carry the displaced incumbent's px/py/pz with its relocated mass through relocate_chain/escape (mirroring E and the RESOLVE swap's payload), with a small regression test asserting a displacing placement leaves no velocity-ghost."
      },
      {
        "id": "ND-8",
        "clause": "Law #9 'A no_escape detection seam fires on that case (empty body for now); default is do-nothing, never destroy.'",
        "code_site": "repo-wide grep zero; decision points core/engine_b.hpp:1509, core/sim_engine.hpp:720",
        "description": "The law-mandated no_escape detection seam does not exist anywhere (grep no_escape/noEscape hits only third-party hedley.hpp). The RESOLVE push-with-no-room path correctly no-ops but fires no seam; the injection path's no-escape case destroys instead (ND-9). Law requires the seam to exist even with an empty body — absent = drift.",
        "severity": "low",
        "evidence": "grep -rn 'no_escape|noEscape|NO_ESCAPE' core/ jni/ tests/ → no engine hits. The two no-escape points carry no hook/callback/marker. Batch A law-8-9#1 [DRIFT-NEW].",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I read the cited law clause (DESIGN-LAW.md §9, lines 80-81: \"A `no_escape` detection seam fires on that case (empty body for now) for future handling; the default is do-nothing, never destroy.\"), both cited code sites, the v4 spec, and the existing partial-audit notes.\n\nFACTUAL CLAIMS VERIFIED: `grep -rniE 'no_escape|noEscape|NO_ESCAPE' core/ jni/ tests/` returns zero engine hits at engine c148f49. engine_b.hpp:1509 (`if (want > avail) { mdot = ...*avail; }`) is the receiver-room clamp — it correctly no-ops the push-with-no-room (mass conserved, applied antisymmetrically), but carries no hook/marker. sim_engine.hpp:720 (`if (!reached) return false;`) is the injection-chain dead-end — also no seam. The v4 spec (docs/superpowers/specs/2026-06-10-engine-b-unified-spec-v4.md) does not mention the seam at all (neither requiring nor deferring it), so it is purely a law-#9 artifact. The finding correctly scopes the *destroy*-on-no-escape problem out to a separate finding (ND-9).\n\nREFUTE ATTEMPT: The strongest compliant reading is that \"fires on that case (empty body for now)\" + \"default is do-nothing\" describes behavior indistinguishable from a plain no-op, and the RESOLVE path at :1509 is exactly do-nothing-never-destroy — so the *behavioral* mass-conservation requirement of law #9 IS met, and the mechanical drift test (lines 87-89) lists only three triggers (2nd pressure / directional force / stored-T), none of which a missing seam hits. That defends the RESOLVE path's behavior. BUT the law explicitly says \"detection seam ... for future handling\" — the purpose of mandating an empty-bodied seam rather than just \"no-op\" is to have a present extension point for future code to hang on. No such hook/marker exists anywhere. The structural artifact the law calls for is genuinely absent. The mismatch survives the attack as a low-severity missing-scaffold gap (behavior compliant, artifact absent) — exactly the finding's own characterization.\n\nVERDICT = ALREADY-LABELED: this candidate is verbatim the same item already recorded in the audit's own raw output — docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md, \"Dimension: law-8-9\", item #1, tagged [DRIFT-NEW/medium]. Same clause, same two code sites (engine_b.hpp:1509 / sim_engine.hpp:720), same grep-zero evidence, same reasoning, same \"Batch A law-8-9#1\" provenance. It is not a NEW finding; it is the existing law-8-9#1 finding flowing through the refute/verify lens (the doc notes \"NO dedup, NO adversarial refute/label lenses ran\" yet). On the merits it is real but low-severity; for this verification the controlling fact is that it duplicates an already-recorded finding.",
        "labelReasoning": "VERIFIED the law clause is live and the seam is absent. DESIGN-LAW.md:80-81 (active, frozen, user-ratified) says: \"A `no_escape` detection seam fires on that case (empty body for now) for future handling; the default is do-nothing, never destroy.\" Repo-wide grep for no_escape/noEscape/NO_ESCAPE over core/ jni/ tests/ returns ZERO engine hits (only the comment \"no escape\" sealedLoss text in sim_engine.hpp:487 and a test comment; the symbol itself appears solely in third-party hedley.hpp). The two cited decision points are real and carry no hook/callback/marker: engine_b.hpp:1497-1513 is the receiver-room clamp (RESOLVE push-with-no-room correctly no-ops via `if (want > avail) mdot=...*avail`), and sim_engine.hpp:720 `if (!reached) return false;` is the relocate_chain dead-end (\"caller seals the incumbent\").\n\nLABEL-CHECK across the four tracking surfaces is NEGATIVE: (1) commits — git log --oneline -16 has no no_escape/seam commit. (2) Code labels — no T?-OPEN / TODO(Tn) / DEBT / owner marker at or near either site for a seam. (3) Handoff known/ratification queue (2026-06-11-T4-continuation.md) — items 1-14 + T4 fold-ins do NOT include the seam; the adjacent `find_chain_hop` fold-in (line 128-129) tracks the MASTER-RULES-banned-symbol ROUTING of the injection escape path, not the absence of the RESOLVE-path detection seam. (4) Spec v4 — no mention.\n\nThe only doc occurrence is the SAME drift audit's own scratch: docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md:141-143, where this exact finding (Batch A law-8-9#1) is recorded and self-classified `[DRIFT-NEW]` (RAW + UNVERIFIED, pending dedup/synthesis). A sibling raw finding from the same audit that labels itself NEW is not an owner/queue label — it confirms the finding is real and new, not already-tracked. Hence ALREADY-LABELED does not apply.\n\nThe finding is technically correct and not refuted: the law text is unambiguous (the seam must exist with an empty body), and it genuinely does not. Note a minor severity discrepancy: the partial-findings doc rated it medium; the candidate rates it low. Low is defensible since the seam is a future-handling stub with no behavioral consequence today (the RESOLVE no-op is already correct), so the gap is documentary/forward-compatibility, not a live conservation bug — unlike its high-severity neighbor ND-9 (injection destroy-on-no-escape).",
        "disposition": "Record as a duplicate of the existing law-8-9 dimension item #1 (drift-audit-partial-findings.md:141) — dedup into that single finding; on the merits it is a real low-severity missing-scaffold gap (the empty-bodied no_escape seam is absent, though the RESOLVE no-room path is behaviorally compliant: no-op, mass conserved), best dispositioned as either an accepted low-priority note or a tiny code task to add the named empty seam at engine_b.hpp:1509 and sim_engine.hpp:720; keep the destroy-on-no-escape concern under the separate ND-9 finding."
      },
      {
        "id": "ND-10",
        "clause": "Law #9 'The only source/sink is the caller's place/break at the boundary, separately ledgered.' + file header 'mass lost — bounded, logged'.",
        "code_site": "core/sim_engine.hpp:813-825 (apply_injections nothingToDisplace branch)",
        "description": "Placement over an incumbent classified nothingToDisplace (!movable e.g. 2700 kg stone, OR incMass ≤ ADV_EPS_MASS, OR void) overwrites the cell and destroys the incumbent's mass (up to 2700 kg) with NO ledger entry at all — not even sealedLoss — while ledger.injected records the new mass. Contradicts 'separately ledgered' and the file's own header. Contrast the movable-incumbent no-escape path (ND-9) which DOES ledger sealedLoss.",
        "severity": "high",
        "evidence": "`nothingToDisplace = !movable(incMat) || incMass<=ADV_EPS_MASS || incSpecies==void; if(nothingToDisplace){ C->mass_kg[ci]=inj.mass; ledger.injected[...]+=inj.mass; continue; }` with no sealedLoss for incMass. Batch A law-8-9#3 + spec-6#11 [DRIFT-NEW].",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "CONFIRMED at c148f49. core/sim_engine.hpp:816-826: nothingToDisplace = !movable(incMat) || incMass<=ADV_EPS_MASS || incSpecies==void_ix. In that branch the cell is overwritten (matIx/mass/T = inj.*) and ONLY ledger.injected[inj.species] is incremented — the incumbent's mass is destroyed with NO sealedLoss entry.\n\nTried to refute and each attempt fails:\n(1) \"Boundary place/break is sanctioned, so OK.\" Law #9: \"The only source/sink is the caller's place/break at the boundary, SEPARATELY LEDGERED.\" The break/overwrite of the incumbent is exactly the sink side; it must be ledgered. The branch ledgers the source (injected) but not the sink (destroyed incumbent). Non-compliant.\n(2) \"incMass<=ADV_EPS_MASS is de-minimis.\" Spec v4 §3.1 region (line 179-180) is categorical: \"law #9 has no de-minimis clause — nothing silently vanishes\" and requires \"mass AND E ledgered\" even for m<ε_mass events; §6.4 (line 258-260) repeats it: \"Only m<ε_mass relabels to VACUUM with mass and E both ledgered.\" So even the epsilon sub-case must ledger. Refutation fails.\n(3) \"void_ix has 0 mass — nothing to ledger.\" TRUE for that one sub-case (correct, no entry needed), but the finding does not rest on the void case; conditions #1 (immovable, up to 2700 kg stone) and #2 (epsilon) are the genuine violations.\n(4) \"§9 gate compensates.\" No — injected records only the NEW placed mass; the destroyed incumbent species gets no sealedLoss, so the gate cannot reconcile it. The engine's OWN sibling paths prove intent: the movable no-escape path (line 836) and the relocate_chain terminal (line 780) both record sealedLoss[incSpecies]. The nothingToDisplace path simply omits the symmetric entry.\n\nThe file header (line 472) \"else replaced (mass lost — bounded, logged)\" claims the loss is logged — but in this branch it is NOT logged, contradicting the file's own contract. The mismatch survives every attack: REAL-DRIFT.\n\nScope/severity nuance: the immovable-solid case can destroy up to ~2700 kg unledgered (high impact); the epsilon case is small magnitude but still a categorical law violation per spec's explicit \"no de-minimis clause\"; the void case is the only fully-compliant sub-condition. Net high severity because the immovable case is unbounded-ish (material maxMass) and the spec is categorical.",
        "labelReasoning": "Code at core/sim_engine.hpp:813-825 matches the finding verbatim: `nothingToDisplace = !movable(incMat) || incMass <= ADV_EPS_MASS || incSpecies == void_ix; if(nothingToDisplace){ overwrite cell; ledger.injected[...] += inj.mass; continue; }` — the incumbent's incMass (up to 2700 kg stone) is destroyed with NO sealedLoss entry. This contradicts (a) Law #9 'the only source/sink is the caller's place/break... separately ledgered; never destroy' (DESIGN-LAW.md:73-81), (b) MASTER-RULES:66 'place/break go to the boundary ledger — nothing is silently dropped', and (c) the file's own header at :472 'mass lost — bounded, logged'. The movable-incumbent no-escape path at :836 DOES ledger sealedLoss, confirming the asymmetry (ND-9 contrast holds).\n\nNOT already-labeled: (1) no T?-OPEN/TODO/DEBT comment at lines 813-825 — plain code only; (2) absent from the T4 ratification queue (handoff items 1-8) and T4 fold-ins — the queued F1 item explicitly covers the DISTINCT §6.4 in-step vacuum guard at engine_b.hpp:1397, and the partial-findings doc itself flags 'Distinct from the queued in-step F1 vacuum-guard item... not the injection seam'; (3) no commit-message reference (git log -16 clean); (4) the only appearance is the RAW, explicitly-UNVERIFIED 2026-06-11-drift-audit-partial-findings.md salvage (law-8-9 #3 lines 147-149, dup spec-6 #11 lines 240-241) — the audit's own un-deduped output, not a tracked queue with an owner. So this is a genuine NEW, un-labeled drift.\n\nSeverity medium (not high): the leak is real but bounded and grand-total-detectable by the §9 gate; only the !movable sub-case (2700 kg stone overwrite) is materially impactful, while the ADV_EPS_MASS and void sub-cases are negligible/correct. It is the same conservation-ledger class as the queued F1/no-escape items and best fixed alongside them.",
        "disposition": "Code fix in a task T_n: in the nothingToDisplace branch, before overwrite, record ledger.sealedLoss[incSpecies] += incMass (and persist/ledger E) when incSpecies != void_ix — i.e. ledger the destroyed incumbent for both the immovable and sub-epsilon sub-cases, matching the sibling no-escape (line 836) and relocate_chain terminal (line 780) paths and spec v4's \"no de-minimis clause\"; leave the genuine void_ix (0 kg) sub-case unledgered."
      },
      {
        "id": "ND-11",
        "clause": "Law #9 / MASTER-RULES 'Grand ENERGY exact every step... place/break go to the boundary ledger — nothing is silently dropped.'",
        "code_site": "core/sim_engine.hpp:484-490 (LedgerAccum: injected/sealedLoss MASS only), :779-781/:834-837 (sealed incumbents); jni/orge_jni.cpp:199-210 (ledgerOut = mass floats only)",
        "description": "The place/break boundary ledger has NO energy side: a sealed incumbent's enthalpy E (and momentum) is destroyed recording only its mass, and injected enthalpy enters unrecorded — grand-E books cannot close across the sanctioned boundary. Structurally there is nowhere to ledger E (LedgerAccum has injected/sealedLoss only, no E slot). Distinct from the in-step F1 vacuum-guard item (queue 12, §D.4 site).",
        "severity": "medium",
        "evidence": "`struct LedgerAccum { std::vector<double> injected; std::vector<double> sealedLoss; }` — mass-only; seal path `ledger.sealedLoss[incSpecies]+=incMass;` then incumbent's E overwritten. Batch A law-6-7#11 [DRIFT-NEW]; untracked, unlabeled.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "Tried to refute on three fronts; all failed.\n\n(1) Clause-misread? No. MASTER-RULES line 65-66 puts \"place/break\" in the SAME breath as vacuum-relabel and radiation-to-sky as things that \"go to the boundary ledger — nothing is silently dropped,\" and the governing sentence is \"Grand ENERGY exact every step.\" Law #9 (DESIGN-LAW.md:73-75) confirms place/break is \"the only source/sink ... separately ledgered.\" So the energy obligation at the place/break seam is explicit, not a stretch.\n\n(2) Code-compliant under correct reading? No. Verified at the cited sites: `struct LedgerAccum { std::vector<double> injected; std::vector<double> sealedLoss; }` (sim_engine.hpp:485-490) — both MASS, no E or momentum slot. The seal paths record mass only — relocate_chain `ledger.sealedLoss[lSpecies] += lMass;` (:780) and apply_injections `ledger.sealedLoss[incSpecies] += incMass;` (:836) — then the incumbent cell (carrying its enthalpy E and momentum) is overwritten by the injection (:838-841 / :782-785) and persist_cell_E recomputes from the NEW content; the old E is gone, booked nowhere. Injection records `ledger.injected[inj.species] += inj.mass` (:824/:842) — the injected enthalpy implied by inj.temperature is never ledgered. JNI ledgerOut writes 2×matCount mass floats only (orge_jni.cpp:204-206). Grand-E cannot close across this sanctioned boundary.\n\n(3) Already-labeled (the queued F1 item)? No — this was the strongest escape and it fails. The F1 E-ledger queue item is scoped EXPLICITLY and EXCLUSIVELY to the in-step `m<ε_mass` relabel / DECODE vacuum guard in engine_b.hpp (handoff 2026-06-11-...-T4-continuation.md:115-116 pins it to `engine_b.hpp:1397-1404` \"still zeroes booked conduction E ... close the class\"; spec §6.4/§D.4). The candidate's site is a DIFFERENT file (sim_engine.hpp), a DIFFERENT mechanism (the place/break injection seam: LedgerAccum + relocate_chain seal + apply_injections seal), with NO owner label and NO in-code debt comment. The two are the same law-family (E-ledger completeness) but not the same code site or finding; the F1 label does not reach the injection seam.\n\nConclusion: the mismatch survives. Real, untracked drift — the place/break boundary ledger is mass-only and destroys/admits energy (and momentum) unledgered at the one sanctioned source/sink the law names. Severity medium: it is a boundary-seam accounting omission (not in-domain silent fabrication), momentum is likewise unledgered, and today nothing downstream audits E across this seam (the §9 gate it feeds is itself mass-only), so it is a latent hole that becomes load-bearing once F1's energy ledger lands — consistent with the candidate's medium and a notch below the high F1 in-step booked-E destruction.",
        "labelReasoning": "VERIFIED the code: LedgerAccum (sim_engine.hpp:485-490) has only `injected`/`sealedLoss` std::vector<double> — mass-only, no E field. The seal paths (:780, :836) accumulate incMass only; apply_injections (:838-842) overwrites the incumbent cell (with its booked E) and ledgers only inj.mass. JNI ledgerOut (orge_jni.cpp:201-208) writes 2×matCount mass floats, no energy slot. So the structural claim is real: the sanctioned place/break boundary has no energy side, so grand-E books cannot close across it. This is genuine drift vs MASTER-RULES:66 (\"place/break go to the boundary ledger — nothing is silently dropped\") and Law #9.\n\nALREADY-LABELED check (owner-tracked sources):\n- In-code: grep around the cited sites shows NO energy-ledger debt label — only an unrelated TODO(T10) for LUT columns and the mass-only sealedLoss lines. No DEBT/T#-OPEN/ARG-PARITY tag on the injection ledger's missing E side.\n- T4-continuation handoff queue + the workflow note's enumerated queue (items 1-22): the only E-ledger item is #12/F1 = the §D.4/§6.4 in-step VACUUM-GUARD relabel at engine_b.hpp:1744-1751 (C.E[i]=0.0, ~542 J/event, owner T4). That is a different site and mechanism (DECODE pipeline vacuum guard) from the injection/place-break LedgerAccum seam. The only injection-path queue items are find_chain_hop (banned-symbol routing) and T10's \"E must cross the JNI boundary\" (latent-plateau reconstruct-from-T) — neither is the place/break energy-ledger gap.\n- Commits (git log -16): T1–T3 enthalpy/momentum/gas work; none adds an energy side to the injection ledger or labels its absence.\n\nThe finding does appear as item 11 in docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md (labeled [DRIFT-NEW/medium]), but that file is a sibling artifact of THIS SAME drift-audit run, not an owner-tracked queue/handoff/code label — it does not constitute prior tracking. Therefore the item is genuinely untracked by any owner: REAL-DRIFT. The candidate's own distinctness claim (vs queue-12/F1) is correct and confirmed.\n\nSeverity medium is appropriate: it's a real conservation-bookkeeping gap on a live path (every place/break), but Σmass is unaffected and today's linear E=m·cp·T plus the gate's mass focus bound the practical blast radius until T7's latent plateaus / T10's E-across-JNI make the E-books load-bearing.",
        "disposition": "Accept as REAL-DRIFT and fold into T4 as a sibling of the F1 E-ledger item: extend LedgerAccum (and the JNI ledgerOut ABI) with an energy (and momentum) side, ledger the sealed incumbent's E and the injected E at the place/break seam in sim_engine.hpp (relocate_chain :780 and apply_injections :836/:842), and add a grand-E-across-injection conservation test — explicitly noting it is the sim_engine.hpp injection seam, distinct from the engine_b.hpp §D.4 vacuum-guard F1 site."
      },
      {
        "id": "ND-12",
        "clause": "Law #9 'mass moves, never vanishes' + §1.3 frozen knob manifest (ε_mass = 1e-6 kg, exactly one) + §6.4 'only m < ε_mass relabels, mass and E both ledgered (≤1e-6 kg)'.",
        "code_site": "core/orge_kernel.hpp:21 (ADV_EPS_MASS=1e-4f); consumers sim_engine.hpp:83-85,549,557,641,660,668,817",
        "description": "The injection path runs on a SECOND, off-manifest void floor ADV_EPS_MASS = 1e-4 kg — 100x the spec's ε_mass = 1e-6. A cell holding up to 1e-4 kg is treated as vacuum and overwritten (vacuum-adopt at chain terminals, escape mode 0), deleting up to 1e-4 kg/event with no ledger — 100x the §6.4 deletion bound, and §1.3 pins exactly one ε_mass. Engine-B proper uses Globals::eps_mass=1e-6.",
        "severity": "medium",
        "evidence": "`constexpr float ADV_EPS_MASS=1e-4f; // kg; below this a cell is empty/air` vs §1.3 ε_mass=1e-6 / §6.4 ≤1e-6 kg. Batch A law-8-9#5 [DRIFT-NEW].",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I independently verified BOTH the code and the clauses; the technical drift is REAL and survives every refutation angle, but it is already recorded verbatim in the repo, so the verdict is ALREADY-LABELED rather than a fresh REAL-DRIFT.\n\nREAL (refutation fails): (1) orge_kernel.hpp:21 defines a second mass floor `ADV_EPS_MASS = 1e-4f` — 100x the spec's pinned ε_mass. (2) v4 §1.3 frozen manifest pins `ε_mass = 1e-6 kg`, CI-diffed (INV-3), exactly one value; §6.2 (lines 179-181) states \"law #9 has no de-minimis clause — nothing silently vanishes\"; §6.4 states \"Only m < ε_mass relabels to VACUUM with mass and E both ledgered (≤1e-6 kg)\" and explicitly kills the larger `max(ε, min_mass)` floor. (3) The injection path is LIVE and in-scope: orge_jni.cpp:200 calls apply_injections every step with injections, and law #9 / §6.2 explicitly govern the caller's place/break boundary path — this IS that path, not out-of-scope legacy code. (4) Real unledgered deletion occurs: apply_injections (sim_engine.hpp:817-825) overwrites an incumbent with `incMass <= ADV_EPS_MASS` (up to 1e-4 kg) booking only ledger.injected, never sealedLoss; relocate_chain mode-0 vacuum-adopt (sim_engine.hpp:743-747, gated by is_vacuum at :84 using ADV_EPS_MASS) overwrites a ≤1e-4 kg void cell with no ledger. Both silently delete up to 100x the §6.4 bound. The \"de-minimis tolerance\" and \"misread predicate\" refutations both fail — the spec was written precisely to ban this larger floor.\n\nALREADY-LABELED: this is verbatim entry #5 of the \"Dimension: law-8-9\" batch at docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md:153-155 (same clause, same code_site, same description, same evidence string). The candidate's own provenance tag \"Batch A law-8-9#5 [DRIFT-NEW]\" names that entry. It is a written, dated record in the repo's docs tree, salvaged 2026-06-11 (per commit ae011dd \"salvage 5/10 completed drift-audit dimensions\"). Caveat: that note self-describes as RAW+UNVERIFIED with dedup/refute lenses not yet run, so the label is provisional pending the synthesis+verify stage — which is exactly why it should be deduped against, not re-counted as new.",
        "labelReasoning": "VERIFIED GENUINE + NO OWNER. The drift is real: core/orge_kernel.hpp:21 defines a second void floor `constexpr float ADV_EPS_MASS = 1e-4f` (100x the spec's manifest ε_mass). Spec §1.3 (verbatim) pins exactly one knob `ε_mass = 1e-6 kg`; §6.4 + §9 require \"mass AND E both ledgered (≤1e-6 kg, ~1 J)\" with \"no de-minimis clause — nothing silently vanishes\". The injection path is LIVE (jni/orge_jni.cpp:200 apply_injections runs every JNI step before the Engine-B canonical step at :228). apply_injections (sim_engine.hpp:817) gates \"nothingToDisplace\" on `incMass <= ADV_EPS_MASS` (1e-4) and then overwrites the cell (matIx/mass_kg, :820-821) recording ONLY ledger.injected for the NEW mass — the incumbent residue (up to 1e-4 kg) is dropped with no sealedLoss, contradicting \"separately ledgered.\" Engine-B proper meanwhile uses Globals::eps_mass=1e-6 (engine_b.hpp:34), so two eps floors coexist (INV-3 manifest-diff violation). NOT REFUTED.\n\nLABEL-CHECK (the assigned question): NOT tracked with an owner. (1) Code: no DEBT/TODO/T3-OPEN/ARG-PARITY/owner-task comment anywhere near orge_kernel.hpp:21 or the apply_injections region — the only nearby comment (\"mass lost — bounded, logged\", sim_engine.hpp:472) is false advertising, not a tracked-debt label. (2) Handoff queue (2026-06-11-T4-continuation.md): the F1 ε-ledger fold-in (line 115-116) is explicitly scoped to engine_b.hpp:1397-1404 on the Globals::eps_mass=1e-6 path — it does NOT cover ADV_EPS_MASS=1e-4 or the injection floor. The injection-path queue item (line 128) targets only the MASTER-RULES-banned `find_chain_hop` symbol, not the eps mismatch or the unledgered deletion. (3) git log -16: no commit addresses it. So against the owner-task/in-code-label criterion this is REAL-DRIFT.\n\nCAVEAT FOR DEDUPE: this candidate is a verbatim duplicate of an already-RECORDED finding — item 5 (\"Batch A law-8-9#5\") in docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md:153-155, labeled [DRIFT-NEW/medium] with the identical clause/code_site/evidence. That doc is a sibling findings ledger from the same audit, not an owner queue, so it does not make this ALREADY-LABELED — but the orchestrator should merge ND-12 with that prior entry rather than double-count. (Related siblings in the same doc: items at :144/:147/:240 cover the apply_injections unledgered-deletion class from the law-#9 angle; :87/:243 cover the missing-momentum-payload angle.)",
        "disposition": "Dedup against the existing record (partial-findings.md:153-155) so synthesis does not double-count it; carry it forward as one verified medium-severity item in the T4/F1 void-ledger fix cluster — unify the injection path onto Globals::eps_mass=1e-6 and ledger every overwritten residue (it overlaps the same nothingToDisplace/seal sites as related entries #2 and #3 in the same batch)."
      },
      {
        "id": "ND-14",
        "clause": "Spec §6.2 'Transient overshoot beyond maxMass is forbidden — the single exemption is the §8.4 freeze relabel.'",
        "code_site": "core/engine_b.hpp:1184 (if evict > kroom + 1e-3f continue) with :1236",
        "description": "The §6.3 eviction acceptance test tolerates the evicted gas exceeding the recipient's remaining room by up to 1e-3 kg, then floors room at 0 — so recipient cell k can persist at maxMass + ≤1e-3 kg, a maxMass-overshoot path that is neither the §8.4 freeze exemption nor the queued vacuum recvRoom=INF item. Epsilon-scale, conserving, unlabeled.",
        "severity": "low",
        "evidence": "`float kroom=...; if(evict > kroom + 1e-3f) continue; AK.dm += evict; (*rri)[k]=max(0,kroom-evict);` admits evict = kroom+1e-3. Batch A law-8-9#19 + spec-6#2 [DRIFT-NEW].",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I read the cited code (core/engine_b.hpp:1183-1236) and the cited clause (spec §6.2, line 245-246) and tried to refute.\n\nSpec §6.2 is unambiguous: \"Transient overshoot beyond maxMass is forbidden — the single exemption is the §8.4 freeze relabel (water→ice, 1000 > 917).\"\n\nThe eviction recipient k (line 1158-1160) is required to be either VACUUM (kmass <= eps_mass) or SAME-SPECIES gas (ksp == gsp). kroom = recvRoom[k], which (lines 626-628) is INF for vacuum, else max(0, maxMass - mass).\n\n- Vacuum branch: kroom=INF, the check `evict > kroom+1e-3` is never true; k ends at `evict` <= the per-face cap, no maxMass overshoot. This is the spec-sanctioned vacuum recvRoom=INF path. Not drift.\n- Same-species gas branch (REACHABLE per line 1160 `ksp==gsp`, kmass>eps): kroom = maxMass - kmass. Line 1184 accepts evict up to kroom+1e-3. Line 1227 applies AK.dm += evict (the FULL evict, unclamped) to k's persisted mass; line 1236 floors the room ledger at 0 but does NOT clamp the mass actually added. So k ends the tick at kmass+evict <= maxMass+1e-3 kg.\n\nRefute attempts that failed:\n1. \"Branch unreachable / always vacuum\" — false, ksp==gsp same-gas partial cell is a valid, reachable recipient.\n2. \"Within-tick ledger only, not a real overshoot\" — false, AK.dm is the actual mass delta applied in DECODE and persists into the next snapshot. I confirmed there is NO maxMass clamp in DECODE catching this (grep of maxMass usages shows clamps only on the §6.1 budget/flux path, lines 1093/1397, not on the eviction AK.dm).\n3. \"1e-3 is in-band float slop absorbed by the gas band\" — false; gas maxMass is a hard 1000 kg (spec line 63-64), so 1000.001 is genuinely beyond the defined band. Unlike a pure comparison-tolerance, the slop leaks into actual mass because evict is added whole while room floors at 0; a tolerance-only impl would clamp evict to kroom on accept (it does not).\n\nThe mismatch survives: a same-species gas recipient can persist at maxMass + <=1e-3 kg, which is neither the §8.4 freeze relabel nor a vacuum recipient — exactly the case §6.2 names as the SINGLE exemption and forbids otherwise. It is conserving (antisymmetric AG.dm/AK.dm) and epsilon-bounded (next-tick kroom floors to 0, sealing further meaningful inflow), so severity is low — but it is an unlabeled, real deviation from the literal \"single exemption\" wording, matching the candidate's description and severity.",
        "labelReasoning": "Verified the cited site. At core/engine_b.hpp:1184 `if (evict > kroom + 1e-3f) continue;` admits evict = kroom + 1e-3, and line 1227/1236 then book `AK.dm += evict` while flooring `(*rri)[k] = max(0, kroom - evict)`. For a NON-vacuum same-species recipient k (the gate at :1160 admits vacuum OR ksp==gsp, and kroom = maxMass - snapshotMass for non-vacuum), accepting evict = kroom + 1e-3 pushes k to snapshotMass + kroom + 1e-3 = maxMass + 1e-3 kg — a transient maxMass overshoot. Spec §6.2 (line 245-247) forbids transient overshoot beyond maxMass with the SINGLE exemption being the §8.4 freeze relabel; this eviction path is neither that nor the vacuum recvRoom=INF case. It is epsilon-scale and conserving (antisymmetric AG.dm -= evict / AK.dm += evict keeps mass exact).\\n\\nTracking check: git blame shows the +1e-3f tolerance originates in 7a45de3 (§8.5 cross-species displacement WIP); the inline comment only says 'k can't hold it all -> treat as sealed' with no debt/TODO/T-task/owner label. The T3 hardening commit c148f49 added debt labels but none at this site. The handoff queue (2026-06-11-...-T4-continuation.md) has related-but-distinct items that do NOT cover this path: line 122 '§6.3 eviction decrements recvRoom but never charges gasSpent' is the DONOR-side gas budget ladder, a different ledger; INV-RR (line 111) is explicitly scoped to 'the vacuum recvRoom=INF overfill at :574-581' — the vacuum path, not the same-species kroom+1e-3 tolerance on a non-vacuum recipient. So this specific recipient-ceiling overshoot is unlabeled.\\n\\nNote: the +1e-3f is a legitimate float-comparison slop guard mirroring the -1e-3f budget guards at :1206/:1218; the slop is in the ACCEPTANCE test but it is also (incorrectly) propagated into the booked transfer, which is what produces the overshoot.",
        "disposition": "Record as accepted note OR clamp evict to kroom on the same-species accept path (a one-line code fix in a future T_n: `evict = std::min(evict, kroom)` after line 1184's accept), since the cleanest reconciliation is to make the 1e-3 a pure accept-tolerance rather than letting it leak into persisted mass; absent that, amend §6.2 to permit an ε_mass-scale conserving transient at gas eviction recipients."
      },
      {
        "id": "ND-16",
        "clause": "Spec §1.3 knobs 'α_eos gas-EOS relaxation gain (0–1]' — half-open, 0 EXCLUDED.",
        "code_site": "core/engine_b.hpp:451 (aeos = std::clamp(G.alpha_eos, 0.0f, 1.0f))",
        "description": "α_eos is clamped to the closed [0,1] but §1.3 specifies (0,1] (0 excluded — α_eos=0 makes a gas cell's P ignore its EOS source). Default 0.25 in range; the clamp lower bound merely admits an out-of-spec 0. Cosmetic range/manifest mismatch, no physics change at default.",
        "severity": "low",
        "evidence": "`std::clamp(G.alpha_eos,0.0f,1.0f)` (inclusive 0) vs §1.3 `(0–1]`. Distinct from queue 11 (joint ω·(1+α_eos)<2). Batch B spec-1-manifest [DRIFT-NEW].",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I read the cited code (core/engine_b.hpp:451) and the cited clause (spec §1.3, line 82). Both citations are accurate. The code is `const float aeos = std::clamp(G.alpha_eos, 0.0f, 1.0f);` — a CLOSED [0,1] clamp. The spec §1.3 line 82 reads `α_eos gas-EOS relaxation gain (0–1]` — a HALF-OPEN range with 0 EXCLUDED.\n\nI attempted refutation on four angles:\n(1) Is the closed clamp spec-sanctioned? No. The code's OWN adjacent comment (line 448) writes the range as `α ∈ (0,1]` (0 excluded), agreeing with the spec, while the clamp it guards uses 0.0f as the lower bound. The code contradicts both the spec and its own comment.\n(2) Is 0 harmless? No — the finding's mechanism is correct: at α_eos=0 the gas source term `α_eos·(p_eos−P)` vanishes, so a gas cell's P ignores its EOS source. Spec line 143-145 confirms relaxation completes in ~1/α_eos sweeps, which diverges at 0 — that's exactly why the spec excludes 0.\n(3) No-op at default? True — default 0.25 is interior to both ranges, so there is no physics change under default config. The finding already concedes this.\n(4) Does the clamp reject the bad value? No — `std::clamp(x,0.0f,1.0f)` passes through 0 and maps negatives to 0, i.e. it ADMITS the spec-forbidden degenerate value instead of flooring it to a positive minimum.\n\nThe mismatch survives the attack: it is a genuine code-vs-manifest range deviation (closed vs half-open lower bound) that admits a value the spec forbids. It is correctly classed low severity because it produces no behavioral change at the documented default and only matters if a caller deliberately passes α_eos ≤ 0.\n\nNot a duplicate of queue 11 (the joint ω·(1+α_eos)<2 / retired α ≤ 2/ω−1−0.05 clamp), which the comment at lines 446-449 explicitly describes as retired — this finding is solely about the surviving plain-range lower bound.",
        "labelReasoning": "Verified at engine c148f49. Spec §1.3 (specs/2026-06-10-engine-b-unified-spec-v4.md:82) specifies `α_eos gas-EOS relaxation gain (0–1]` — half-open, 0 EXCLUDED. Code at core/engine_b.hpp:451 is `const float aeos = std::clamp(G.alpha_eos, 0.0f, 1.0f)` — closed [0,1], lower bound admits 0. The two disagree on whether α_eos=0 is legal; α_eos=0 drives P_new=(1−ω)P+ω·target with no `+α(a−P)` EOS contribution (a gas cell's P ignores its EOS source), which §1.3 forbids by range.\n\nTracking check — NOT labeled anywhere with an owner:\n- Nearby comments (engine_b.hpp:94, 104, 448) repeatedly restate the legal range as `(0–1]`/`(0,1]` but none flag that the clamp lower bound 0.0f contradicts it; line 446-449 only documents retiring the former JOINT clamp (α ≤ 2/ω−1−0.05), not the 0-exclusion.\n- No T3-OPEN / TODO / DEBT / ARG-PARITY comment covers the lower bound.\n- T4 continuation queue (handoffs/2026-06-11-engine-b-v4-T4-continuation.md) items 1–8 do not list it; the candidate is correctly distinguished from queue item 11 / handoff line 101 (`ω·(1+α_eos)<2` joint upper-region stability constraint — a different, missing-from-spec joint bound, not the α_eos=0 lower-bound manifest mismatch). Handoff item 7's T2a/T2b spec-wording debts cover Neumann ghost wording and the κ·divU source, not this.\n- git log --oneline -16 has no commit referencing the clamp lower bound.\n\nGenuine, untracked spec-vs-manifest range mismatch. No physics change at the default α_eos=0.25 (in range); the defect is that the clamp admits an out-of-spec 0.",
        "disposition": "Code fix in a future tidy task: change the clamp lower bound to a small positive minimum (e.g. std::clamp(G.alpha_eos, eps, 1.0f) or a defined α_eos_min) to honor the spec's half-open (0,1]; alternatively record as an accepted low-severity note since the default 0.25 makes it harmless in practice."
      },
      {
        "id": "ND-19",
        "clause": "Law #6 (amended 2026-06-10): latent plateaus and T-continuity ARE the law; MASTER-RULES 'comments lag — verify against v4.'",
        "code_site": "core/engine_b.hpp:1836-1840 (relabel comment block)",
        "description": "Stale in-code law claim: the comment asserts the keep-E cp-jump 'is the documented law-§6 semantic (audit B-19; latent heat / hysteresis is a law change, user-held)'. The amendments were ratified 2026-06-10, so latent plateaus are NOW the law and this mechanism is T7-owned debt, not law-sanctioned design. The label should point at T7, not 'user-held law change' — a misdirecting wording that could mislead a future agent.",
        "severity": "low",
        "evidence": "Comment vs DESIGN-LAW.md #6 current text 'plateaus of width latentHeat... T continuous across every transition'. Batch A law-6-7#5 [NOTE]; wording fix.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I read the cited code (core/engine_b.hpp:1830-1855, the DECODE phase-change relabel block) and the current DESIGN-LAW.md §6 (lines 38-54). The comment at lines 1836-1838 makes two assertions that are now false against the amended (2026-06-10, ratified) law:\n\n(1) It calls \"the keep-E T jump by cp_old/cp_new ... the documented law-§6 semantic.\" The current law-§6 text instead mandates an enthalpy curve with \"plateaus of width latentHeat\" that are chain-anchored so a relabel is \"the identity on E (ΔE ≡ 0)\" and \"T is continuous across every transition.\" A bare cp_old/cp_new T-jump is the OPPOSITE of T-continuity — it is precisely what the amended law forbids. So the comment misdescribes the current law-§6 semantic.\n\n(2) It calls \"latent heat / hysteresis ... a law change, user-held.\" But the amendments were ratified by the user on 2026-06-10 (HEAD c148f49 already ships the plateau text in DESIGN-LAW.md). Latent heat is no longer a pending/held proposal — it IS the law. The \"user-held\" framing is stale.\n\nI attempted to refute. The only escape would be reading \"user-held\" as \"the implementation is deferred (to T7)\" — but the comment doesn't say that; it affirmatively claims the cp-jump \"IS the documented law-§6 semantic,\" i.e. asserts present law-compliance, not debt. I confirmed the underlying mechanism is genuinely debt: orge_enthalpy in sim_engine.hpp:99-107 is a pure linear E=m·cp·T with no plateau and no chain-anchoring; latentHeatMin/Max are stored but unused (grep shows only the storage site + this comment). So the relabel does perform a discontinuous T-jump, and the comment mislabels that debt as law-sanctioned design while painting the ratified amendment as still open. That is exactly the MASTER-RULES \"comments lag — verify against v4\" failure mode: a future agent would be misdirected. No compliant reading survives.\n\nSeverity low: comment-only, no behavioral effect (the missing-plateau behavior itself is a separate finding); the harm is misdirection of a future reader.",
        "labelReasoning": "Verified the cited comment at core/engine_b.hpp:1837-1838: \"the keep-E T jump by cp_old/cp_new is the documented law-§6 semantic (audit B-19; latent heat / hysteresis is a law change, user-held).\" This is genuinely stale against the amended law. DESIGN-LAW.md #6 (amended 2026-06-10, lines 39-41) now defines the enthalpy curve `E=m·h(T)` with \"plateaus of width latentHeat\" that are chain-anchored, and spec v4 §8/§8.1 mirror this — so latent plateaus / T-continuity ARE the ratified law now, not a \"user-held law change.\" The comment therefore (a) misdescribes the current law and (b) misdirects: it points a future agent at \"user-held law change\" when the correct owner is T7. The handoff queue (2026-06-11 T4-continuation, line 143) does track the latent-heat FEATURE as T7 (\"§8.1 enthalpy curves w/ chain-anchored latent plateaus, ΔE≡0 relabel\"), and commit c148f49 was explicitly a \"debt labels\" pass — but that pass added an owner label (DEBT owner: T4 R1) at a DIFFERENT site (gas-flux direction), NOT at this relabel comment. This specific comment was not relabeled and still reads \"user-held.\" So while the underlying work is queued, the stale/misdirecting comment itself is not labeled with its owner-task — the candidate is a real, uncorrected wording-drift, exactly as scoped (a [NOTE]/wording fix). Not REFUTED (it is a true mismatch) and not ALREADY-LABELED (this comment carries no T7/owner label; only the unrelated feature is queued). Severity low: it is a comment-only misdirection with no behavioral effect — the cp_old/cp_new keep-E jump is correct as interim behavior; only the rationale wording is wrong.",
        "disposition": "Code fix in T7 (the latent-heat/plateau task): rewrite the comment to state that the cp_old/cp_new T-jump is T7-owned DEBT against the now-ratified law §6 (plateaus + ΔE≡0 + T-continuity), not \"the documented law-§6 semantic,\" and drop the stale \"latent heat is a law change, user-held\" wording; until T7 lands, record as an accepted NOTE-class stale-comment finding."
      },
      {
        "id": "ND-20",
        "clause": "Spec §1.2 exact data + MASTER-RULES 'Real material LUT only'; T7 stale-seed sweep names ONLY bug3_accept.",
        "code_site": "tests/engine_b_phase_relabel_test.cpp:41-58 (make_phase_lut)",
        "description": "phase_relabel_test carries its own pre-v4 synthetic LUT under a comment claiming 'Real LUT + REAL phase quadruple (values verbatim from materials/*.json)': lava cp 1450 / band 400/3100/3100 / solidus 1000 K, steam degenerate 0.6/0.6/0.6 (the exact χ=0/0 the spec killed), stone 840/2500. Self-consistent for the keep-E relabel it pins, but the handoff's stale-seed sweep names ONLY bug3_accept (queue 21), so this second stale-lava-3100 + 'Real LUT' MISLABEL survives the T7 sweep as written and could mislead a future agent reading it as authoritative data.",
        "severity": "low",
        "evidence": "`M.LAVA={1450,1.5,0.060,400,3100,100,3100}` + `M.STEAM={2080,0.025,0.001,0.6,0.6,1e-4,0.6}` + lava.minTemp=1000 under header 'Real LUT... values verbatim' vs §1.2 lava 330/2650/2650 solidus 1275 / steam 0.06/0.6/1000. Batch A t1-data-jni#7 [PENDING-IMPL] — but not a future-task section; a live mislabel beyond queue 21.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "Verified all cited facts read-only. (1) phase_relabel_test.cpp:50-75 make_phase_lut builds its OWN material rows, NOT via the shared real LUT: M.LAVA={1450,1.5,0.060,400,3100,100,3100} with lava.minTemp=1000 (line 70), and M.STEAM={2080,0.025,0.001,0.6,0.6,1e-4,0.6} (line 57). (2) These are the stale pre-v4 numbers: lava maxMass 3100 > v4 §1.2 maxMass 2650, solidus 1000 K vs spec 1275 K, and steam 0.6/0.6/0.6 is exactly the degenerate χ=(max−def)/(max−min)=0/0 band the spec §1.2 explicitly killed ('kills its χ=0/0'). (3) The header comment lines 10-14 + 41 claim 'Real LUT + REAL phase quadruple (values verbatim from the materials/*.json datapack)' — but no materials/*.json exists in the engine repo (confirmed NO MATERIALS DIR), and the values contradict v4 §1.2. The sibling file engine_b_real_lut.hpp in the same dir carries the CORRECT v4 data (lava 330/2650/2650 solidus 1275, steam 0.06/0.6/1000), proving the authoritative numbers exist and this test diverges from them under a 'verbatim' label.\n\nREFUTE attempts and why they fail: (a) Spec-sanctioned? No — the 'verbatim from materials/*.json' provenance is false on two counts (file absent, numbers wrong). (b) ALREADY-LABELED? This is the strongest attack: known-queue item (21) and the T7 handoff line both address stale lava-3100. BUT both are explicitly scoped to bug3_accept_test.cpp — the handoff line 144 reads 'sweep stale lava-3100 seeds in bug3_accept' and names no other file. phase_relabel_test.cpp's make_phase_lut is a DISTINCT code site (different file, different function) not enumerated anywhere in the queue, so its stale seeds + the 'Real LUT verbatim' mislabel would survive a T7 sweep that follows the handoff literally. Same-family, not same-occurrence → not already-labeled. (c) Cosmetic only? The values are functionally inert for the keep-E mechanism the test pins (which T7 replaces anyway), so no wrong engine behavior results — this caps severity at low. But the mislabel is a real anti-MASTER-RULES ('Real material LUT only') hazard: a future agent reading 'verbatim from materials/*.json' could treat lava-3100/steam-χ0 as authoritative.\n\nFinding survives. Real, un-queued, distinct from item (21). Low severity (test-hygiene/doc mislabel, no behavioral impact).",
        "labelReasoning": "VERIFIED REAL, NOT already-labeled with an owner. Facts confirmed at engine c148f49:\n\n1. tests/engine_b_phase_relabel_test.cpp uses its OWN local LUT builder make_phase_lut (lines 50-75), NOT the canonical rlut::make_real_lut. Its values: M.LAVA={1450,1.5,0.060,400,3100,100,3100}, lava.minTemp=1000; M.STEAM={2080,0.025,0.001,0.6,0.6,1e-4,0.6}; M.STONE={840,...,2500,2500,...,2500}. These sit under the header comment 'Real LUT + REAL phase quadruple (values verbatim from the materials/*.json datapack)' (lines 41-44 / 10-11).\n\n2. This contradicts the canonical source: tests/engine_b_real_lut.hpp make_real_lut has lava cp 1150 / band 330/2650/2650 / solidus 1275, stone 2700, steam band 0.06/0.6/1000 — matching spec §1.2 exactly (verified spec lines 59-75). So the 'verbatim' label is a genuine MISLABEL, and lava maxMass 3100 + degenerate steam 0.6/0.6/0.6 (the χ=0/0 the spec explicitly killed) + solidus 1000 K are exactly the pre-v4 data v4 retired.\n\n3. NOT TRACKED WITH AN OWNER: The handoff T7 sweep line (queue item 21) names ONLY 'sweep stale lava-3100 seeds in bug3_accept' — and in bug3_accept the 3100 is a MASS SEED (setc ... 3100.f) over the canonical make_real_lut (maxMass 2650), a distinct staleness already tracked separately (partial-findings #17). phase_relabel's 3100 appears in BOTH the local LUT band AND the seed, and is named NOWHERE: no in-code debt label (grep for TODO/DEBT/OPEN/T7/ARG-PARITY in the file = zero hits), not in the ratification queue (items 1-8), not in the T4 fold-ins. git log -16 has no mention.\n\n4. The ONLY place it appears is docs/.../2026-06-11-drift-audit-partial-findings.md lines 135-137 — but that doc is self-described as 'RAW + UNVERIFIED — no dedup, no adversarial lenses' and is precisely the audit FEED this verification pass consumes (this candidate IS finding #8 from it). A raw unverified feed is not an owner-bearing tracking record, so it does not satisfy ALREADY-LABELED.\n\nSeverity low is correct: the file pins the keep-E DECODE relabel (issue #9, commit 4127ee0) which T7's §8.1 latent-plateau curves rework, so the synthetic values don't break a live invariant today — the harm is a 'Real LUT verbatim' comment that could mislead a future agent into treating 3100/1000/0.6-steam as authoritative §1.2 data, and it falls outside the named T7 sweep so it survives as written.",
        "disposition": "Fold this code site into the T7 stale-seed sweep alongside bug3_accept: re-base make_phase_lut on the v4 §1.2 values (or on make_real_lut), and either correct or delete the 'values verbatim from the materials/*.json datapack' header since no such file exists and the numbers diverge; track as a test-fix in T7."
      },
      {
        "id": "ND-21",
        "clause": "engine_b_lut_schema_test header 'the engine Material struct must carry EXACTLY the fixed schema' + §1.3 'INV-3 = struct/knob diff against §1.3 must be empty.'",
        "code_site": "tests/engine_b_lut_schema_test.cpp:96-210",
        "description": "The test's assertions do not pin what its header claims: several §1.2 cells have NO assertion and could drift silently (lava k=1.5/τ_y=0/β=5e-5/T_ref_gas=0; stone M=0.065/β=2e-5/latentMin=0; ice M=0.018; air yieldStress=0; steam k=0.025/β=0/yieldStress=0/latentMax=0). Nothing enforces struct EXACTNESS (no field-count/sizeof pin) and no INV-3 manifest diff exists anywhere. The 'EXACTLY' claim is presence-only — a missing regression guard, not a current data mismatch (data itself verified correct).",
        "severity": "low",
        "evidence": "Only water's row asserted on all 17 fields; lava block asserts 12/17 with no CLOSE(L.thermalConductivity)/CLOSE(L.thermalExpansion). Batch A t1-data-jni#2 [NOTE]; not in queue.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "I read the cited test (tests/engine_b_lut_schema_test.cpp:1-214), DESIGN-LAW.md #8, and v4 §1.1-§1.3. I tried to refute on three fronts.\n\n(1) \"EXACTLY the schema not enforced\": The first block (lines 28-57) assigns and round-trips ALL 17 named fields. That IS a real exactness guard for the field SET — removing/renaming any field breaks compilation. So the header's \"EXACTLY\" is NOT pure presence; removals are caught. BUT: an ADDED 18th field is not caught, and there is no sizeof/field-count/static_assert pin. Confirmed by grep: the only static_assert in core/tests is an unrelated SECTIONS check.\n\n(2) \"several §1.2 data cells unasserted\": Verified by direct reading — only water's row (lines 109-125) asserts all 17 fields. Lava omits CLOSE(k), CLOSE(β), τ_y; stone omits M, β, latentMin; ice omits M; steam omits k, β, τ_y, latentMax (mentioned in comments, not asserted). These could drift silently. Factually correct.\n\n(3) \"no INV-3 manifest diff exists anywhere\": §1.3 is normative — \"Frozen manifest (CI-diffed; INV-3) ... struct/knob diff against §1.3 must be empty\" — covering both the persisted fields AND the knob set (ω, κ, N_relax, vel_damp, ε_mass, k_c, t_swap_min, α_eos, T_sky, T_ref). Grep across the engine repo found no manifest file and no struct/knob diff harness; the \"1.3/manifest/INV-3\" hits in tests are spec-citation comments only. The knobs have zero coverage. Confirmed absent.\n\nRefutation failed: all three sub-claims hold and are concretely articulated. However, the finding is honestly self-scoped: the engine struct DOES match the law-#8 schema, the §1.2 data IS correct, the test passes for the right reasons. So this is NOT engine drift against the law's drift-test (no second-P, no direction-split force, no stored-T). It is a test-completeness + CI-tooling gap: the test header overclaims \"EXACTLY\" relative to what it pins (no addition/struct-size guard), partial per-row data coverage on 5 of 6 non-water rows, and the spec's own INV-3 CI-diff invariant is unimplemented. Verdict REAL-DRIFT in the weak missing-regression-guard sense the candidate itself states; severity low (no current data/behavior mismatch; the live artifact is law-compliant).",
        "labelReasoning": "Verified against all four tracking locations. (1) Code labels: grep at tests/engine_b_lut_schema_test.cpp found NO TODO/T3-OPEN/DEBT/ARG-PARITY/owner-task labels — the test header makes the \"EXACTLY the fixed schema\" claim with no caveat. (2) Ratification queue (handoff §USER RATIFICATION QUEUE items 1-8): none covers a schema-test-exactness or INV-3-manifest gap; the candidate's own evidence string concedes \"not in queue.\" (3) T-task ownership: T1 (the schema task) is DONE; T10 owns JNI LUT column growth + ResidentLutParityIT golden regen, but no task owns a struct-exactness/field-count/INV-3 manifest diff test — the handoff never mentions one. (4) Commit log -16: bf4c35e/19303d5 (T1) added the schema + real-LUT data but no exactness guard.\n\nSubstance verified: INV-3 (spec §1.3 \"struct/knob diff against §1.3 must be empty\") is genuinely UNIMPLEMENTED — the only \"INV-3\" occurrences are descriptive prose in the spec and a lone comment at core/engine_b.hpp:26; no sizeof/offsetof/field-count guard exists anywhere (grep clean). The unasserted-field claim is accurate: the lava block (lines 143-156) omits CLOSE on thermalConductivity (k=1.5), yieldStress, thermalExpansion (β=5e-5), and T_ref_gas; only water's row asserts all 17 fields. Data itself is correct, so this is a missing regression guard, not a behavioral/data drift.\n\nThe finding DOES appear verbatim as item #2 ([NOTE/low]) in docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md:117-118 — but that file is the SAME audit's RAW + UNVERIFIED salvage (per handoff: \"no dedup, no adversarial lenses\"), i.e. this candidate's own predecessor scratch output, not an owned/tracked debt entry. So it is NOT already-labeled in any owner-bearing location.\n\nVerdict REAL-DRIFT (a real, untracked documentation/test-guarantee gap), severity low — it is a presence-only test whose header overclaims, plus a deferred-and-unbuilt INV-3 guard, with no current data mismatch.",
        "disposition": "Record as an accepted low-severity test/CI gap and file a small task T_n: add (a) a sizeof/field-count static_assert pinning struct exactness, (b) full 17-field CLOSE coverage for every §1.2 row (not just water), and (c) the §1.3 INV-3 struct+knob manifest diff in CI — no engine code or data change, since the live LUT already matches the law-#8 schema and the §1.2 data is verified correct."
      },
      {
        "id": "ND-23",
        "clause": "Spec §1.2 gas rest state / §6.2 mass bands — seeding helper semantics.",
        "code_site": "core/sim_engine.hpp:850-870 (fill_section_with: 'a full cell holds the material's max_mass')",
        "description": "fill_section_with seeds every cell at maxMass — a pre-v4 premise. With T1's v4 gas bands (air 1.0/1.2/1000, steam 0.06/0.6/1000) filling a section with a gas now seeds 1000 kg/cell ≈ 69 MPa gauge instead of rest density. Callers today are tests/viz only (synthetic LUTs), no live-path impact, but any future real-LUT use of the helper for gas seeds a pathological scene. A latent helper hazard, not a future-task section.",
        "severity": "low",
        "evidence": "`const float mdef=mats.byIx(mat_ix).maxMass; ... C.mass_kg[i]=(mat_ix==void)?0:mdef;` vs §1.2 air defaultMass 1.2/maxMass 1000. Callers: correctness_test, stress_test, viz/main.cpp. Batch A spec-6#18.",
        "verdictReal": true,
        "verdictTracked": false,
        "refuteReasoning": "VERIFIED, could not refute. core/sim_engine.hpp:849-870 @ c148f49: fill_section_with seeds every non-void cell at mats.byIx(mat_ix).maxMass (line 854/864), and its own comment (lines 850-851) declares \"a full cell holds the material's max_mass ... defaultMass was dropped.\" That premise is pre-v4. v4 §1.2 (spec line 54) RE-INTRODUCED defaultMass into the schema and gives gases a wide band: air 1.0/1.2/1000, steam 0.06/0.6/1000. v4 §2.1 (lines 90-99) sets the gas rest state — where EOS gauge p_eos = 0 — at defaultMass (air 1.2 ⇒ 99.1 kPa gauge 0; steam 0.6 @373K ⇒ gauge 0). Seeding a gas section at maxMass = 1000 kg/cell places it far above rest density; the spec itself anchors the magnitude at line 99: \"Compressed air at 835 kg/cell: p_eos ≈ 69 MPa\", and 1000 kg/cell exceeds that — so the finding's physics is the spec's own number, not a misread.\n\nRefutations tried and failed: (a) \"compliant for the model\" holds only for solids/liquids where defaultMass==maxMass (water/lava/stone/ice), so seeding at maxMass IS their rest state; the drift is gas-specific, which the finding correctly scopes. (b) \"spec-sanctioned\" — no; §1.2/§2.1 define gas rest at defaultMass and the helper comment even asserts the now-false \"defaultMass was dropped.\" (c) \"live-path bug, higher severity\" — refuted in the opposite direction: grep confirmed callers are ONLY tests/ (correctness_test, stress_test, test_harness.hpp) and viz/main.cpp; jni/ and src/ have zero callers. The finding's self-characterization (tests/viz only, no live-path impact, latent helper hazard) is accurate and not overstated. Not already-labeled: not parked in §1.3 retired-symbols nor a future-task slot; the stale comment actively misdescribes current v4 LUT semantics.",
        "labelReasoning": "Verified the cited code at core/sim_engine.hpp:852-869: fill_section_with seeds C.mass_kg[i] = maxMass (line 854, mdef = mats.byIx(mat_ix).maxMass), and the in-code comment (lines 850-851) confirms this is a pre-v4 premise (\"defaultMass was dropped\"). With T1's v4 gas bands now live (engine_b_real_lut.hpp:40,44 — AIR 1.0/1000 default 1.2, STEAM 0.06/1000 default 0.6), filling a gas section via this helper seeds 1000 kg/cell vs ~1.2/0.6 rest density — a pathological ~830x over-compression. Confirmed all callers are synthetic/non-live: tests/correctness_test.cpp, tests/stress_test.cpp, viz/main.cpp; no core, JNI, or world-step caller. The real-LUT acceptance tests (engine_b_accept_test.cpp) already route around the helper by seeding gases at rest density with sc(...,1.2f...), which corroborates the hazard. Not tracked: no TODO/DEBT/T_n-OPEN/owner label at the site; the known queue (handoffs/2026-06-11-engine-b-v4-T4-continuation.md, T4-T8) and the 16-commit engine log never mention fill_section_with, maxMass-seeding, rest density, or this hazard. test_harness.hpp:87-88 only documents the convention, does not flag the gas hazard. Therefore REAL-DRIFT, latent, low severity.",
        "disposition": "Code fix in a future task (T_n): make fill_section_with seed gases at defaultMass (rest density) instead of maxMass — or add a rest-density gas-seed helper — and correct the stale \"defaultMass was dropped\" comment; until then record as an accepted low-severity latent-hazard note since no live/JNI caller exists."
      }
    ],
    "alreadyLabeled": [
      {
        "id": "ND-13",
        "clause": "Law #8 'molarMass's consumer is the gas EOS (#9)' — single-consumer sentence.",
        "code_site": "core/sim_engine.hpp:51 (struct comment), :557, :661-662 (lighter-medium tests)",
        "description": "molarMass is still consumed as a gravitational/buoyancy SORT key on the injection path (escape mode-2 'strictly lighter movable' compares molarMass) and the struct comment documents it as 'gravitational sort key (higher sinks)', violating law #8's single-consumer (EOS) sentence. Inside Engine-B proper its only consumers are the EOS sites (compliant).",
        "severity": "low",
        "evidence": "`float molarMass; // gravitational sort key (higher sinks)` + `mE.molarMass < incMat.molarMass` vs law #8 single-consumer. Compliant sites engine_b.hpp:202-203,224-225,828-830. Batch A law-8-9#8 [DRIFT-NEW].",
        "verdictReal": false,
        "verdictTracked": true,
        "refuteReasoning": "All three cited sites verified factually. Law #8 (DESIGN-LAW.md:69) states flatly \"`molarMass`'s consumer is the gas EOS (#9)\" — a single-consumer sentence with no carve-out. The compliant EOS sites (engine_b.hpp:199-225, 828-833) honor it: molarMass appears only as `mass/molarMass` (moles) inside p_eos. But TWO injection-path sites consume molarMass as a buoyancy/lighter-medium SORT comparator: sim_engine.hpp:557 (`mE.molarMass < incMat.molarMass` in find_injection_escape mode-2) and :660-662 (`mE.molarMass < predMat.molarMass` in find_chain_hop mode-2). The canonical Material struct comment (sim_engine.hpp:51) unconditionally labels the field `// gravitational sort key (higher sinks)`. REFUTE attempts all fail: (1) it is NOT the EOS in disguise — it is a pure inequality \"which species is lighter,\" different semantics from mass/M molar arithmetic; (2) it is NOT spec-sanctioned — the v4 spec's buoyancy/swap machinery (§ lines 29, 34) is explicitly built on `ρ_eff = (m/V)·(1−β·(T−T_ref))` \"read ONLY by the swap machinery,\" NOT molarMass, so molarMass-as-buoyancy is sanctioned by neither law #8 nor the spec; (3) the scope mitigation is real but does not negate the drift — sim_engine.hpp:495 confirms this is the LEGACY caller-side injection/placement path (\"Engine B uses its own internal variant\"), so it sits at the place/break boundary rather than in RESOLVE proper, which is exactly why the finding scopes itself \"Inside Engine-B proper its only consumers are the EOS sites (compliant)\" and rates severity low. Law #8 has no injection-path exemption, and the misleading struct comment is on the canonical struct. Not already-labeled: DESIGN-LAW only files the A+B pressure split as tracked debt; nothing files this molarMass-sort use. Genuine, correctly-scoped, low-severity NEW drift.",
        "labelReasoning": "Confirmed the finding is factually accurate: molarMass is consumed as a buoyancy/sort key outside the EOS at sim_engine.hpp:557 (find_injection_escape, mode-2 'strictly lighter movable': `mE.molarMass < incMat.molarMass`) and :661-662 (find_chain_hop, lighter-terminal test), plus the struct comment at :51 documents it as 'gravitational sort key (higher sinks)'. Engine-B proper (engine_b.hpp:828-830 etc.) only consumes molarMass in the gas EOS, so the finding correctly scopes the violation to the injection path. Law #8 (DESIGN-LAW.md:69-70) does say `molarMass`'s consumer is the gas EOS (#9), so the non-EOS consumption is a genuine deviation from the single-consumer sentence.\n\nIt is ALREADY tracked with an owner: the T4 continuation handoff (docs/superpowers/handoffs/2026-06-11-engine-b-v4-T4-continuation.md:128-129) lists a debt item — 'find_chain_hop (sim_engine.hpp:598, injection path) — MASTER-RULES-banned symbol; route injection escape through the standard displacement path or escalate.' Both cited consumer sites (:557 find_injection_escape, :661-662 find_chain_hop) live on exactly this injection-escape path; rerouting it through the standard displacement path retires the bespoke molarMass buoyancy comparisons. The disposition is named (reroute or escalate), so the load-bearing code sites are owned.\n\nCaveats: the handoff frames this as a banned-symbol cleanup, not explicitly as a law-#8 single-consumer violation, and it does not separately mention the descriptive struct comment at :51 ('gravitational sort key'), which is legacy Engine-A framing that survives independent of the reroute. So the code-site of the violation is tracked; the law-#8 framing and the comment sub-part are not separately captured. Searched commit messages (git log -16: the T3 debt-labels commit c148f49 touched only engine_b.hpp, no molarMass label) and found no separate label there.",
        "disposition": "Record as accepted note / file as labeled legacy-injection-path debt in the spec: the molarMass buoyancy comparison lives on the caller-side place/break path (sim_engine.hpp:557, 660-662) outside Engine-B RESOLVE; its exit is the v4 swap machinery's ρ_eff comparator — until that path is unified, track it as debt and at minimum fix the misleading struct-field comment at sim_engine.hpp:51 (\"gravitational sort key\") to state molarMass's law-#8 consumer is the gas EOS (with the legacy injection-path sort use noted as tracked debt)."
      },
      {
        "id": "ND-17",
        "clause": "Spec §6.1/§9 INV-7 order-independence: 'all passes read snapshot or prior-pass buffers; no atomics'; R0 deterministic priority '(3) lower coordinate-parity'.",
        "code_site": "core/engine_b.hpp:1456-1494 (live budget decrement), :1502-1512 (recvRoom decrement), :906-1020 (swap std::sort + greedy), :1140-1253 (displacement live mutation incl. vacClaim rewrite :1172)",
        "description": "Order-dependence broader than the named gasSpent item (queue 15): (a) the monolithic additive loop reads-and-decrements budget and recvRoom LIVE mid-pass, so allocation among competing faces/donors is traversal-order-dependent (totals bit-stable, WHICH face wins room is not); (b) swap selection is std::sort (not stable) over candidates gathered in unordered_map order with no coordinate-parity tiebreak, so the selected swap SET at priority-ties is hash-order-dependent; (c) the §6.3 displacement pre-pass live-mutates budget/recvRoom/vacClaim and OVERWRITES vacClaim (set order-independently in the swap pre-pass) in traversal order. All dissolve into T4's R0/R1/R1.5 split but the budget/recvRoom/std::sort/vacClaim INV-7 details are NEW beyond the queue's gasSpent-only item.",
        "severity": "medium",
        "evidence": "`float avail=(*bd)[di]; ...; (*bd)[di]=max(0,avail-want);` (live); `std::sort(cands...,a.prio>b.prio)` over `for(kv:world.chunks)` unordered_map, no parity tiebreak; `(*vck)[k]=gsp;` rewrites vacClaim. Batch B spec-9-11 (three findings) [DRIFT-NEW family].",
        "verdictReal": false,
        "verdictTracked": true,
        "refuteReasoning": "ND-17 raises three INV-7 / R0 order-dependence sub-claims: (a) budget+recvRoom live mid-pass single-spend (engine_b.hpp:1456-1494, 1502-1512); (b) swap std::sort over unordered_map gather with no coordinate-parity tiebreak (1006-1020); (c) §6.3 displacement pre-pass live-mutating budget/recvRoom/vacClaim incl. the vacClaim rewrite at :1172 (1140-1253).\n\nI read the cited code and the cited clauses (spec §6.1 R0 priority \"(3) lower coordinate-parity\", §6.1 R1/R1.5 proportional σ/ρ' composition, §9 INV-7 \"all passes read snapshot or prior-pass buffers; no atomics\"). The CODE BEHAVIOR is real and not refutable: the additive loop does read `(*bd)[di]`/`(*rr)[ri]` and decrement them live in traversal order (1456-1494, 1502-1512); std::sort at 1011-1012 keys only on prio with candidates gathered in `for(kv:world.chunks)` unordered_map order and no parity tiebreak; the displacement pre-pass mutates shared budget/recvRoom and rewrites vacClaim at :1172 in ascending-y walk order. So this is NOT spec-sanctioned behavior — it is genuine INV-7 debt (R1/R1.5 want proportional composition from prior-pass buffers; R0 wants a coordinate-parity tiebreak).\n\nHOWEVER, the verifier task is whether this is a NEW finding. The candidate scoped its novelty only against the 22-item known QUEUE (\"NEW beyond the queue's gasSpent-only item\"), but the audit baseline is the salvaged 81-finding partial-findings doc (commit ae011dd). Checking against THAT baseline, all three sub-claims are already recorded with identical code sites and clauses:\n- (b) swap std::sort/coordinate-parity → partial-findings spec-9-11 item 16 (lines 255-257) VERBATIM: same site 906-1020, same `std::sort(...a.prio>b.prio)` evidence, same `for(kv:world.chunks)` unordered_map, same \"no parity/coordinate tie-break ... INV-7 hazard\", same §6.1 R0 \"(3) lower coordinate-parity\" + §9 INV-7 clause.\n- (a) budget/recvRoom sequential single-spend → partial-findings spec-9-11 items 12 (sites :1450-1495 donor, :1502-1512 recvRoom) and 13, classified PENDING-IMPL / DRIFT-KNOWN INV-7 family.\n- (c) displacement pre-pass live mutation + vacClaim → partial-findings spec-6 item 1 (line 211: \"the displacement pre-pass is sequentially order-dependent ... mutating shared accumulators/budgets in walk order\") and item 4 (sites :1119-1253), plus the vacClaim pre-pass itself in spec-6 item 14 (sites :899-948, :1392-1395).\n\nThe only sharper detail ND-17 adds is naming the vacClaim REWRITE at :1172 specifically, but that is subsumed by \"mutating shared accumulators in walk order\" and is a documented conserving no-op donor-side seal (comment :1173-1174), not a new hazard. No genuinely new drift mechanism survives. Verdict: ALREADY-LABELED — the candidate deduped against the queue but not against the salvaged baseline where all three details already live.",
        "labelReasoning": "ND-17 bundles three INV-7 order-dependence sub-claims, all already owned by task T4 — and one of them is recorded verbatim in the existing audit.\n\n(b) swap std::sort over unordered_map with no coordinate-parity tiebreak: ALREADY recorded, verbatim, in the salvaged drift-audit partial findings — dimension spec-6 finding #16 (docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md:255-257). Same code site (engine_b.hpp:906-1020), same evidence (\"std::sort ... no tie-break ... cands filled from for(kv:world.chunks) over an unordered_map ... not coordinate-canonical and an INV-7 hazard\"), same spec clauses (§6.1 R0 priority \"(3) lower coordinate-parity\" + §9 INV-7). It states \"INV-7 is a named T4 invariant.\" The spec itself pins INV-7 (spec §9 lines 337-338, 382) and the handoff (docs/superpowers/handoffs/2026-06-11-engine-b-v4-T4-continuation.md:112) lists INV-7 \"forward/reverse iteration bit-identical\" as a NEW T4 invariant test.\n\n(a) budget/recvRoom live mid-pass decrement → traversal-order-dependent allocation: owned by T4. The handoff frames the whole monolithic single-spend-against-budget/recvRoom as the INV-7 debt family resolved by the R1 (legalized flux + σ donor scale) / R1.5 (ρ′ receiver scale) split, gated by new tests INV-DB (Σout ≤ budget every tick), INV-RR (m ≤ maxMass), and INV-7 (handoff lines 106-112). spec-6 #12 (partial-findings:46) explicitly classes \"Order-dependence is the known INV-7 debt family ... the expected pre-T4 monolith awaiting R1/R1.5.\"\n\n(c) §6.3 displacement live-mutates budget/recvRoom and overwrites vacClaim at :1172: owned by T4. spec-6 #1 (partial-findings:211) names \"the displacement pre-pass is sequentially order-dependent (ascending-y eviction, mutating shared accumulators/budgets in walk order)\" with owner \"T4 task #5\"; spec-6 #4 (:219-220) assigns the §6.3 displacement geometry to \"T4's R0 face-swap rebuild.\" The vacClaim write is an internal mechanism of that pre-pass, replaced by T4's R0 face-swap candidate model.\n\nND-17 itself concedes \"All dissolve into T4's R0/R1/R1.5 split,\" then claims novelty only because \"the queue is gasSpent-only.\" That premise is false: the queue/audit is broader than the gasSpent item — the std::sort/parity finding is recorded independently (spec-6 #16) and budget/recvRoom is the INV-DB/INV-RR/INV-7 T4 family. ND-17 mischaracterizes the existing tracking as narrower than it is. Not REFUTED (the order-dependence is real), but every part already has an owner (T4) via INV-7/INV-DB/INV-RR and the R0/R1/R1.5 restructure.",
        "disposition": "Record as already-labeled (duplicate of partial-findings spec-9-11 items 12/13/16 and spec-6 items 1/4/14); fold into the existing INV-7 PENDING-IMPL family owned by T4's R0/R1/R1.5 split — no new queue entry, no spec amendment, no code fix beyond the already-planned T4 restructure."
      },
      {
        "id": "ND-22",
        "clause": "Law #8 'χ ≡ 0 whenever maxMass == minMass' — guard totality.",
        "code_site": "core/engine_b.hpp:114-120 (chi)",
        "description": "chi() carries two defensive extensions beyond the law's single guard (denom≤1e-9⇒0; [0,1] clamp — benign, unreachable on legal rows) but leaves ONE unguarded edge: maxMass=+INF with FINITE minMass yields (INF−d)/(INF−min)=INF/INF=NaN, and std::clamp propagates NaN ⇒ chi=NaN, silently classifying as not-gas/not-compressible. No such row exists today (Java builder defaults absent maxMass to finite defaultMass) so it is unreachable, recorded as an edge the guard does not make total.",
        "severity": "low",
        "evidence": "`if(m.maxMass==m.minMass) return 0.0f;` (INF==INF→0) but maxMass=INF/minMass finite ⇒ denom=INF (not ≤1e-9) ⇒ c=NaN ⇒ clamp(NaN)→NaN. Batch A t1-data-jni#6 [NOTE]; latent.",
        "verdictReal": false,
        "verdictTracked": true,
        "refuteReasoning": "The cited law sentence (DESIGN-LAW.md §8, mirrored in v4 §1.2) requires exactly one thing: \"χ ≡ 0 whenever maxMass == minMass.\" The code at engine_b.hpp:115 implements precisely that guard (`if (m.maxMass == m.minMass) return 0.0f;`), which correctly covers INF==INF as well. The law states NO requirement that chi() be total / NaN-safe over arbitrary float inputs, and in particular says nothing about the input class the finding raises (maxMass=+INF with finite minMass). That antecedent is outside the guard's domain, so handling it differently is not a deviation from any normative sentence — there is simply no clause to violate. The finding itself concedes this: it explicitly states the code IS compliant with the law's single guard, that the two extra extensions (denom≤1e-9⇒0; [0,1] clamp) are \"benign,\" and that the NaN edge is \"unreachable\" — recording it only as \"an edge the guard does not make total.\" Totality over impossible inputs is not a law obligation. Reachability is independently confirmed: Material.java:127 (`float maxM = maxMass != null ? maxMass : defaultMass`) defaults an absent maxMass to the finite defaultMass, and +INF is reserved for the viscosity axis (law #8 \"+INF = frozen\"), never for maxMass — so no legal LUT row can produce maxMass=+INF with finite minMass. There is no code↔law mismatch; the candidate is a self-acknowledged latent NOTE about a non-mandated property over an unreachable input, which under the REFUTE lens is not a drift. Suggested action: record as an accepted defensive note (optionally add an isfinite(denom) early-return if hardening is ever desired), but no spec amendment or code-fix task is warranted.",
        "labelReasoning": "The candidate ND-22 is already tracked. It appears verbatim as finding #6 in the same drift-audit's partial-findings note at /home/claude/ORGE-B/docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md lines 129-131, labeled [NOTE/low], covering the identical clause (law #8 / v4 §1.2 chi guard at core/engine_b.hpp:114-120), the identical two benign extensions (denom<=1e-9 branch + [0,1] clamp), and the identical unguarded edge (maxMass=+INF with finite minMass => INF/INF = NaN => std::clamp propagates NaN). The candidate's own evidence string even cites the source batch (\"Batch A t1-data-jni#6 [NOTE]\"), i.e. it is a re-surfacing of that very note, not a new discovery.\n\nI independently verified the technical claims rather than trusting the prior note: (1) engine_b.hpp:115 `if (m.maxMass == m.minMass) return 0.0f;` — IEEE-754 INF==INF is true, so the both-INF row reads exact 0, never NaN; (2) the INF/finite path: denom=INF (not <=1e-9), c=(INF-d)/(INF-min)=INF/INF=NaN, std::clamp(NaN,0,1) returns NaN — correct. (3) Unreachability confirmed at the source: Material.java:127 `float maxM = maxMass != null ? maxMass : defaultMass;` defaults an absent maxMass to the finite defaultMass, and the slot-0 vacuum sentinel is 0/0 (caught by the equality guard). No LUT row produces maxMass=INF with finite minMass; viscosity (not maxMass) is the +INF axis for frozen terrain. So the edge is genuinely latent/unreachable, consistent with its [NOTE/low] classification.\n\nThe handoff queue (2026-06-11-engine-b-v4-T4-continuation.md) mentions the chi guard maxMass==minMass→0 as part of T1's done work but does NOT carry this NaN edge as a queued debt item — because, as a [NOTE/low], it was deliberately recorded-not-actioned, not assigned to a task. That is its disposition. Per the verdict rule (tracked anywhere with a known disposition => ALREADY-LABELED), this resolves to ALREADY-LABELED; the location is the partial-findings note #6.",
        "disposition": "Record as an accepted note (no spec/code change required); if hardening is ever wanted, fold a one-line `!std::isfinite(denom) ⇒ return 0.0f` guard into an unrelated touch of chi(), but do not open a dedicated task."
      }
    ],
    "refuted": [
      {
        "id": "ND-4",
        "clause": "Spec §1.2 'Gas classification: χ > 0.999, the 0.999 cutoff pinned here so a band edit cannot silently demote a gas.'",
        "code_site": "core/engine_b.hpp:132-133 (CHI_COMPR_EPS=1e-3, is_compressible), consumed at :1154, :1202, :1645",
        "description": "A SECOND, 1000x-looser gas/compressible predicate is_compressible (χ ≥ 1e-3) coexists with the §1.2-pinned is_gas (χ > 0.999) and makes live physics decisions: :1154 §6.3 displacement-pass 'must be a gas' gate, :1202 incompressible-donor skip, :1645 both-compressible no-penetration exempt. On the real LUT the two classify identically, but any future χ∈(1e-3,0.999] material (legacy synthetic tests already have χ=0.53/0.10 rows) is routed through gas-only paths the spec's classification forbids — exactly the silent-demote hazard §1.2 pinned the single cutoff to prevent. CHI_COMPR_EPS is also a knob absent from §1.3 (INV-3 diff).",
        "severity": "medium",
        "evidence": "`constexpr float CHI_COMPR_EPS=1e-3f; is_compressible(){return chi(m)>=CHI_COMPR_EPS;}` vs `GAS_CHI_MIN=0.999`. Only in-code label is 'Stage-4 may calibrate' (retired stage, no owner task). Batch A t1-data-jni#3 + law-8-9#6 + Batch B spec-1-manifest, all [DRIFT-NEW].",
        "verdictReal": false,
        "verdictTracked": false,
        "refuteReasoning": "The finding mis-frames a spec-sanctioned binary distinction as a §1.2 violation. §1.2 pins the 0.999 cutoff to protect the GAS classification (is_gas, χ>0.999) against a band edit silently DEMOTING a gas to incompressible — the dangerous direction (a gas losing EOS pushback = in-game bug #2). The three cited sites do NOT want that boundary: they want the orthogonal incompressible-vs-has-EOS-band boundary, which the spec defines elsewhere as χ=0 (§2.2 \"incompressible iff max==default\"; §2.1/Law#9 \"compresses via EOS applies to gases only\"). The code's own comment at engine_b.hpp:1625 states the intent exactly: \"no-penetrate iff EITHER side is INCOMPRESSIBLE (chi==0)\". So is_compressible (χ ≥ 1e-3) is the correct numerical implementation of \"χ > 0\" with 1e-3 as a float deadband, NOT a second classification threshold. The spec's material taxonomy is strictly binary — there is no spec-sanctioned material with 0<χ≤0.999 (real LUT: every row is χ=0 or χ≈0.9995, the two predicates classify identically, which the finding concedes). The hypothetical χ=0.5 \"future material\" is undefined by the spec, and routing a material that genuinely has an EOS expansion band through the compressible-expansion (:1645) / EOS-displacement (:1202) paths is correct physics — is_compressible errs in the SAFE/permissive direction (treats anything with a band as having one), the OPPOSITE of the \"silent-demote\" hazard §1.2 guards. The cited \"synthetic χ=0.53/0.10 test rows\" are the legacy orge_kernel MatLUT with EXPLICIT gas[] flags (test_phase2b_lut.cpp), not the engine_b chi-derived predicate — they never exercise is_compressible. What survives the attack is only minor hygiene, not behavioral drift: (1) the :1154 \"// must be a gas\" comment is genuinely mislabeled (the §6.3 gas-eviction pass is conceptually gas-only, yet calls is_compressible); (2) CHI_COMPR_EPS is a named constant absent from §1.3's frozen knob manifest, so the INV-3 diff would flag it. Both are documentation/naming gaps on a predicate that, for every real and spec-defined material, does exactly what the law/spec dictates. The medium-severity \"1000x-looser gas predicate violating §1.2\" claim is refuted; the residue is a low-severity note.",
        "labelReasoning": "VERIFIED REAL. The code matches the finding exactly. engine_b.hpp:132-133 defines a second gas/compressible predicate `is_compressible(m){return chi(m)>=1e-3}` (CHI_COMPR_EPS=1e-3) alongside the §1.2-pinned `is_gas(m){return chi(m)>GAS_CHI_MIN}` with GAS_CHI_MIN=0.999 — a 1000x-looser boundary. All three consumer sites confirmed verbatim: :1154 gates the §6.3 gas-eviction pass with the literal comment \"must be a gas\" yet tests chi>=1e-3; :1202 excludes \"incompressible\" displacement donors via the same loose cutoff; :1645 exempts \"both compressible\" pairs from cross-species no-penetration. The hazard is concrete, not hypothetical: the existing synthetic test LUTs already carry intermediate-band rows — resolve_test/step_test water {min=125,max=2000,default=1000} has chi=(2000-1000)/(2000-125)=0.533, which reads is_compressible=true and would be routed through gas-only paths the spec's classification forbids while is_gas=false. This is exactly the silent-demote hazard §1.2 pinned the single 0.999 cutoff to prevent, and CHI_COMPR_EPS is a knob absent from the §1.3 frozen manifest (INV-3 diff).\n\nNOT ALREADY-LABELED with an owner. Checked: (a) in-code — the only label near the constant is \"Stage-4 may calibrate CHI_COMPR_EPS\", a retired pre-v4 stage name, not a T4-T10 owner task; no T?-OPEN/DEBT/TODO(T_n) tag. (b) The known queue in handoffs/2026-06-11-...-T4-continuation.md — the ratification queue (items 1-8) and T4 fold-ins cover the §4 face-value rule, gas-P-force-dead, ¼Δρ face correction, gas_anchor floor, sub-min donor, the design-gap open-top gas, spec-wording debts, E-ledger, drive-map unify (w vs w_eff), gasSpent order-dependence, INV-DB/RR/7 — NONE is the dual gas-classification cutoff. (c) git log -16 — no commit references it. The candidate DOES appear (as raw findings #3 and #6, both [DRIFT-NEW/medium]) in docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md, but that file is explicitly RAW + UNVERIFIED (pre-dedup, pre-adversarial-lens) and is the *input* this synthesis workflow consumes — it is not an owner-tracking record, so it does not make the item ALREADY-LABELED. Severity medium is right: latent/dormant on the real LUT (the two predicates coincide there), but a live foot-gun for any Java-datapack or test material in chi∈(1e-3,0.999], and a direct contradiction of an explicitly-pinned spec invariant.",
        "disposition": "Record as an accepted low-severity hygiene note (not a drift): fix the misleading :1154 \"must be a gas\" comment to read \"must be compressible (has an EOS band, χ>0)\" and add CHI_COMPR_EPS to §1.3's knob manifest (or note it as a derived float-deadband of the χ=0 guard, not an independent tunable) so the INV-3 diff is clean — no behavioral code change, no spec amendment, no separate task needed."
      },
      {
        "id": "ND-5",
        "clause": "Spec §5.2 'σ's donor budget is m−minMass (or m when a SINGLE full-drain face is the ONLY outflow), so post-scale donors never land in (0,min).'",
        "code_site": "core/engine_b.hpp:1112-1115 (canDrain rule), applied :1463-1465",
        "description": "The full-drain exemption is materially BROADER than ratified: canDrain exempts any donor whose SUMMED drive-limited outflux over ALL faces ≥ its snapshot mass — so a donor splitting outflow across several faces (down + lateral) can drain fully to 0, where the spec grants the m budget only when one single face fully drains and is the only outflow (else budget m−minMass retains the cohesion floor). Example: 1000 kg water with 600 down + 500 sideways drains to 0 in code; spec keeps 125.",
        "severity": "medium",
        "evidence": "`(*cd)[i] = (outFlux >= mIi - eps) ? 1:0;` where outFlux = Σ over up to 6 faces. In-code comment cites pre-v4 'canonical §3, audit #3', no §5.2 owner-task debt label. Batch A spec-4-5#8 [DRIFT-NEW]; not in queue.",
        "verdictReal": false,
        "verdictTracked": false,
        "refuteReasoning": "The finding misreads the §5.2 parenthetical as an exhaustive restriction. Spec line 202-203 reads: \"σ's donor budget is `m − minMass` (or `m` when a single full-drain face is the only outflow), so post-scale donors never land in (0, min).\" The operative normative clause is the consequent — \"post-scale donors never land in (0, min)\" — and the parenthetical \"(or `m` when a single full-drain face is the only outflow)\" is an ILLUSTRATIVE example of the m-budget case, not the sole sanctioned condition for it.\n\nThe candidate's example (1000 kg water, 600 down + 500 sideways → drains to 0 in code, \"spec keeps 125\") is a fabricated reading. Draining to exactly 0 (→VACUUM) does NOT land the donor in (0, min); it lands at 0, which is the legal terminal endpoint of the cohesion invariant. The invariant is satisfied identically whether the cell empties through one face or several. No spec passage anywhere mandates minMass retention for a multi-face full-drain — I grepped the entire v4 spec: \"only outflow\" appears exactly once (the §5.2 parenthetical itself) and there is no restrictive single-face language. The spec NEVER prescribes \"keeps 125\" for the split case.\n\nMoreover, §5.2's own per-face legalization (line 197-198) explicitly permits `f ← D` (full-drain of the whole donor mass D, not D−min) via the \"else f ← D if R + D ≤ maxMass\" branch, regardless of face count — so full-drain is spec-sanctioned at the per-face level, and the m-budget exists precisely to keep σ-scaling from clipping that legal full-drain.\n\nThe code's canDrain (engine_b.hpp:1064-1115) is also not \"optimistic capacity\": it sums the drive-limited, receiver-capacity-clamped ACTUAL flux `min(capf, fluxf)` per face and exempts only when that realistic total ≥ mIi, so an exempt cell genuinely empties to 0 this tick (the comment at 1060-1063 explicitly rejected capacity-alone). The additive loop (1450-1495) then bypasses the (0,min) cohesion floor only for these provably-emptying donors. End state = 0, never (0, min). Behavior is compliant.\n\nThe only real residual is cosmetic: the in-code comment cites \"canonical §3, audit #3\" (a pre-v4 provenance) rather than §5.2, which is documentation-staleness, not behavioral drift — and the candidate itself frames the missing §5.2 label as its complaint, which does not alter runtime compliance.",
        "labelReasoning": "VERIFIED REAL DRIFT, not tracked.\n\nCODE (core/engine_b.hpp): the canDrain full-drain exemption sums drive-limited outflux across ALL six faces — the loop at :1067-1111 does `outFlux += std::min(capf, fluxf)` inside `for(int f=0;f<6;++f)`, then :1115 sets `(*cd)[i] = (outFlux >= mIi - eps) ? 1 : 0`. This exempt bit is consumed at :1463-1465 to skip the min_mass cohesion floor. So a 1000 kg donor with 600 down + 500 lateral (Σ=1100 ≥ 1000) is exempted and drains to 0.\n\nSPEC §5.2 (line 202-203): \"σ's donor budget is `m − minMass` (or `m` when a SINGLE full-drain face is the only outflow), so post-scale donors never land in (0, min).\" The ratified rule grants the `m` (no-floor) budget ONLY for a single full-drain face that is the only outflow; multi-face splits should retain budget `m−minMass` (the cohesion floor → 125 kg in the example). The code is materially broader. The finding's technical claim and worked example are accurate.\n\nNOT TRACKED. Searched:\n- In-code comments at the site (:1112-1115, plus :1023/:1439) cite only pre-v4 provenance (\"canonical §3, audit #3\"); no §5.2 owner-task / DEBT / TODO label. git blame = 406a793b (2026-06-07, pre-v4), never re-labeled against v4 §5.2.\n- Handoff queue docs/superpowers/handoffs/2026-06-11-engine-b-v4-T4-continuation.md has two NEARBY but DISTINCT canDrain debts: item 5 = \"§5.2 sub-min-donor FULL-MERGE branch HELD\" (the D≤min receiving-side `f←D if R+D≤maxMass` case, explicitly noted as \"only the D≤min case deviates\"); item 123 = \"canDrain is gasSpent-blind (ARG-PARITY :1078/:1380)\" (gas EOS-room over-promise). NEITHER addresses the single-vs-multi-face breadth of the full-drain exemption — they are orthogonal deviations.\n- git log --oneline -16: no commit mentions this; T3 hardening commit c148f49 added \"debt labels\" but not for this.\n\nSEVERITY medium: the additive flux is antisymmetric/permutation-conserving, so this does NOT fabricate or destroy mass (no conservation breach). It is a cohesion-floor SEMANTICS deviation — cells that should keep a min_mass cohered remainder can instead split-drain to 0, producing physically-thin/empty donors the ratified rule meant to forbid. Plausibly visible in-game as over-thinning fluid sheets, but bounded and self-correcting per tick. Matches the candidate's medium rating.",
        "disposition": "Record as an accepted note (not a drift); optionally file a trivial doc-only task to update the engine_b.hpp:1112-1115 / 1439 comment provenance from \"canonical §3, audit #3\" to cite v4 §5.2, but no code or spec change is warranted."
      },
      {
        "id": "ND-9",
        "clause": "Law #9 'A pushed cell with no escape is a no-op (mass stays)... Mass is never deleted... default is do-nothing, never destroy.'",
        "code_site": "core/sim_engine.hpp:834-837 (apply_injections seal-on-chain-failure) and :779-781 (mode-2 lighter-medium seal)",
        "description": "The injection displacement path DELETES a pushed cell that finds no escape instead of no-opping: a movable incumbent whose relocation chain dead-ends is 'sealed' (mass destroyed, booked to ledger.sealedLoss), and a displaced lighter medium (air) at a chain terminal with no 1-hop escape is likewise sealed. Law #9 sanctions a ledgered sink only for the caller's place/break; engine-side destroy-on-no-escape contradicts the no-op clause. The deletion IS reported via jLedgerOut, but the law's only sanctioned sink is the caller's own place/break.",
        "severity": "high",
        "evidence": "`if(!relocate_chain(...)){ ledger.sealedLoss[incSpecies]+=incMass; }` and mode-2 `else { ledger.sealedLoss[lSpecies]+=lMass; }`. Batch A law-8-9#2 + spec-6#13 [DRIFT-NEW]; adjacent to but not covered by the queued find_chain_hop fold-in (which tracks the symbol/routing, not destroy semantics).",
        "verdictReal": false,
        "verdictTracked": false,
        "refuteReasoning": "The cited code (sim_engine.hpp:779-781 mode-2 lighter-medium seal, and :834-837 chain-failure seal) lives in `apply_injections`, whose only non-test caller is jni/orge_jni.cpp:200 — the JNI boundary that executes the Java layer's placement/fill requests (player places a block, /orge fill). Both seals book to `ledger.sealedLoss[...]`, and the full ledger (injected + sealedLoss) is returned to Java via jLedgerOut.\n\nThe finding equates this seal with Law #9's clause \"A pushed cell with no escape is a no-op (mass stays)... the default is do-nothing, never destroy.\" That clause is a MISREAD in this context. Law #9's structure is: \"Inside the domain mass only *moves* — conservative antisymmetric flux ... or a permutation swap. The only source/sink is THE CALLER'S PLACE/BREAK AT THE BOUNDARY, SEPARATELY LEDGERED.\" The no-op/no_escape clause governs INTERIOR advection/RESOLVE (a cell pushed by internal dynamics with nowhere to go stays put). `apply_injections` is not interior advection — it IS the caller's place operation at the boundary, which the law explicitly names as the one sanctioned, separately-ledgered source/sink.\n\nWhen a place forces an injection onto a cell holding a movable incumbent, the engine relocates the incumbent if it can (relocate_chain, multi-hop heterogeneous shift); if it cannot reach a sink within INJ_CHAIN_MAX, the displaced remainder is consumed by the place and ledgered to sealedLoss — exactly \"the caller's place ... separately ledgered.\" Mass is not silently destroyed inside an advection sweep; it is accounted at the boundary the law sanctions. The finding even concedes \"The deletion IS reported via jLedgerOut\" yet fails to recognize apply_injections IS the caller's place. The compliant reading is concrete and the mismatch does not survive: this is the law-sanctioned ledgered boundary sink, not an interior-advection deletion.\n\nResidual nuance (accepted-note, not drift): whether a place SHOULD consume a trapped incumbent vs. refuse the placement is a caller-policy question Law #9 does not legislate; the law only requires boundary operations be ledgered, which they are.",
        "labelReasoning": "VERIFIED AS DRIFT, NOT OWNER-LABELED.\n\nLaw text (DESIGN-LAW.md:73-81, clause #9): \"Inside the domain mass only *moves* … The only source/sink is the caller's place/break at the boundary, separately ledgered. A pushed cell with **no escape** is a **no-op** (mass stays). … Mass is never deleted in either case. A `no_escape` detection seam fires on that case (empty body for now) … the default is do-nothing, never destroy.\"\n\nCode (sim_engine.hpp @ c148f49):\n- :834-837 apply_injections — `if (!relocate_chain(...)) { if (incSpecies < ledger.sealedLoss.size()) ledger.sealedLoss[incSpecies] += incMass; }` then the cell is overwritten by the injection. A movable incumbent whose relocation chain dead-ends is DELETED, not no-op'd.\n- :779-781 relocate_chain mode-2 — `else { if (lSpecies < ledger.sealedLoss.size()) ledger.sealedLoss[lSpecies] += lMass; }`: a displaced lighter medium (air) at a chain terminal with no 1-hop escape is likewise sealed/deleted.\n\nThis contradicts the \"no-op (mass stays) … never destroy\" clause. The seal is an engine-internal destruction of a THIRD-PARTY incumbent collided with by a place — the law's only sanctioned boundary sink is the caller's own placed/broken block, not an in-the-way cell. The sealedLoss IS reported via jLedgerOut (orge_jni.cpp:206), but reporting a destruction is not the same as the law sanctioning it. So it is genuine drift, not REFUTED.\n\nLABEL-CHECK (the assigned lens):\n- Code site: NO debt comment at :834-837 or :779-781 (grep for TODO/DEBT/T3-OPEN/T4/ARG-PARITY in sim_engine.hpp returns nothing). The nearby comments at :566-574, :678-680, :829-830 DESCRIBE the seal behavior as intended (\"may be sealed (and ledgered)\"), i.e. they bake the drift in rather than label it as debt.\n- Authoritative known queue (handoffs/2026-06-11-engine-b-v4-T4-continuation.md): the only adjacent T4 fold-in is `find_chain_hop` (line 128) — \"MASTER-RULES-banned symbol; route injection escape through the standard displacement path or escalate.\" That owns the SYMBOL/ROUTING, not the destroy-on-no-escape semantics (the candidate correctly flags this distinction; line 145/150-152 of the findings doc confirm it). F1 E-ledger (line 115) owns the vacuum-guard E, a different site. So no owner-task covers the mass-destroy semantics here.\n- Commit log (engine git log -16): no commit addresses seal/no-escape destroy semantics.\n\nThe finding does appear VERBATIM as item #2 in the law-8-9 dimension of docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md:144-146 — but that doc self-declares \"RAW + UNVERIFIED … NO dedup, NO adversarial refute/label lenses ran\" and is the INPUT being verified by this very pass, not a label/owner register. Appearing in the staging buffer is not ALREADY-LABELED.\n\nVerdict: REAL-DRIFT (recorded in the raw staging doc, but untracked with any owner in the authoritative queue; the adjacent find_chain_hop T4 item explicitly does not cover destroy semantics).",
        "disposition": "Record as an accepted note (no code/spec change required): the injection-seal is the caller's place at the JNI boundary, which Law #9 explicitly sanctions as a separately-ledgered sink (booked to sealedLoss, returned via jLedgerOut); the no-op/no_escape clause governs interior advection, not the boundary place — optionally add a one-line spec §6 clarification that place-on-trapped-incumbent ledgers the displaced remainder."
      },
      {
        "id": "ND-15",
        "clause": "Spec §6.2/§8.4 'Transient overshoot beyond maxMass is forbidden — the single exemption is the §8.4 freeze relabel (water→ice, 1000>917).'",
        "code_site": "core/engine_b.hpp:1841-1855 (DECODE keep-mass phase relabel) + tests/engine_b_real_lut.hpp stone 2700 maxTemp 1450→LAVA, lava maxMass 2650",
        "description": "The generic keep-mass relabel + the v4 LUT creates a SECOND unsanctioned overshoot: a 2700 kg stone cell melting (T>1450) relabels to LAVA at 2700 kg > lava maxMass 2650 — a 50 kg standing overshoot outside the single water→ice exemption. §8.4 discusses lava→stone (under-full) but is silent on stone→lava melt; the overshoot survives a faithful T7 implementation because the ratified text has no melt clause. Neither queue nor in-code label tracks it.",
        "severity": "medium",
        "evidence": "Relabel keeps `C.mass_kg[i]=mNew;` after `sNew=tgt`; LUT stone 2700/2700/2700, `stone.maxTemp=1450; stone.maxTarget=LAVA;`, lava maxMass=2650. Batch A spec-6#10 [DRIFT-NEW].",
        "verdictReal": false,
        "verdictTracked": false,
        "refuteReasoning": "The finding claims a SECOND unsanctioned maxMass overshoot: a 2700 kg stone cell crossing T>1450 K relabels to LAVA (maxMass 2650) at 2700 kg, a standing 50 kg overshoot outside the §8.4 water→ice exemption, allegedly surviving a faithful T7 implementation because the spec has no melt clause.\n\nI read the cited code and clauses. The premise is structurally unreachable:\n\n1. The phase-change relabel the finding points at (engine_b.hpp:1841-1855, the ONLY relabel in the engine — grep confirms lines 1844-1845 are the sole minTarget/maxTarget sites) lives inside the DECODE per-cell loop AFTER an early-out that holds frozen terrain. Lines 1717-1738: any cell with `!isfinite(viscosity) && mass>eps_mass` (frozen terrain) is written as-is and `continue`s, with the explicit comment \"No phase relabel for terrain (a freeze/solidify chain still terminates in terrain and cannot oscillate).\"\n\n2. In the cited LUT (tests/engine_b_real_lut.hpp:42) STONE has viscosity = INF, unconditionally. Stone is therefore always frozen terrain, held at line 1723, and can NEVER reach the relabel block at 1841. The scenario \"2700 kg stone melts → lava at 2700 kg\" cannot execute.\n\n3. Contrast with the genuinely-reachable water→ice exemption: water is μ=1e-3 (movable), so a 1000 kg water cell DOES reach the relabel and CAN stand at ice's 917 maxMass — that is why the spec calls it the single sanctioned overshoot. Stone→lava is the opposite case: the source label is immovable, so it is gated out before relabel.\n\n4. The spec §8.4 even addresses the lava lifecycle in the reachable direction: \"Lava→stone: 2650/2700 = 98.1% under-full (stated).\" It is silent on stone→lava melt not by omission of a needed clause, but because melting frozen terrain is not implemented (terrain is held; solids-melting/sinking is explicitly deferred to the granular stage per §10's \"solids in liquid: static (τ_y=∞)\" row).\n\nThe finding's load-bearing claim — \"the overshoot survives a faithful T7 implementation\" — is false: the implementation deliberately excludes terrain from the relabel. No code change, spec amendment, or test fix is warranted; the described overshoot does not occur.",
        "labelReasoning": "Physics verified against the cited sites. tests/engine_b_real_lut.hpp:42-43 sets STONE min/default/max = 2700/2700/2700 with stone.maxTemp=1450, stone.maxTarget=M.LAVA (line 55); LAVA maxMass=2650 (line 38). The DECODE relabel at core/engine_b.hpp:1841-1855 flips sNew=tgt while leaving mNew untouched (C.mass_kg[i]=mNew at :1858), so a fully-packed 2700 kg stone cell crossing T>1450 K relabels to LAVA at 2700 kg = a 50 kg (1.9%) standing overshoot above lava's 2650 maxMass. Spec §6.2 (line 246) and §6.3/§8.4 (lines 325-327) explicitly call the water→ice freeze \"the single exemption\" / \"the one sanctioned transient overshoot\"; §8.4 addresses only lava→stone (98.1% under-full) and is silent on stone→lava melt. The relabel is generic and the ratified text has no melt clause, so a faithful T7 implementation preserves the overshoot — genuine drift, not refuted.\\n\\nTracking check: No in-code debt label (no T7-OPEN/TODO/DEBT/owner) exists at engine_b.hpp:1841-1855 for this class (grep negative). The known queue in handoffs/2026-06-11-engine-b-v4-T4-continuation.md lists T7 as '§8.1 enthalpy curves ... + §8.4' but never names the stone→lava maxMass overshoot — and since §8.4's text itself lacks a melt clause, T7 would implement the drift, not fix it. The ONLY place this finding appears is docs/superpowers/notes/2026-06-11-drift-audit-partial-findings.md item #10 (Batch A spec-6), which is the raw output inventory of THIS very drift audit, not a pre-existing owned-debt tracker; that note's own text concludes 'neither the queue nor an in-code label tracks it.' Per the rubric, ALREADY-LABELED requires it be tracked WITH AN OWNER (code label / queue task / commit) — that condition is not met. Verdict: REAL-DRIFT. Note ND-15 is a verbatim duplicate of partial-finding #10, so the orchestrator should de-dup rather than re-file.\\n\\nSeverity medium is appropriate: bounded (a single ~1.9% transient on a melting solid cell, relievable by pushing 50 kg to a same-species neighbor or relaxation room), conserving, and only manifests on solid melt — but it is a true silent breach of a spec invariant the project tests assert ('no maxMass overshoot except the SINGLE §8.4 freeze-relabel', 00-MASTER-RULES.md:69, INV-RR).",
        "disposition": "Record as an accepted note: the stone→lava melt-overshoot is unreachable because stone is μ=∞ frozen terrain held at engine_b.hpp:1723 before the relabel block (the only phase relabel, lines 1844-1845), so no spec amendment or code/test fix is needed; if anyone later makes solids meltable (granular stage), re-evaluate the maxMass bound then."
      },
      {
        "id": "ND-18",
        "clause": "Spec §4 (only vel_damp's KE is given a heat-deposit) vs MASTER-RULES 'nothing is silently dropped' / grand_energy counts KE.",
        "code_site": "core/engine_b.hpp:1690-1697 (swap-path wall zero + CFL), :1761-1792 (normal-path wall/xspecies zero + CFL cap)",
        "description": "Wall no-penetration zeroing, cross-species no-penetration zeroing, and the CFL speed cap all remove momentum and destroy its kinetic energy with no E deposit and no ledger (an impact's ½m·u² vanishes; bounded by ½m·(dx/dt)²/event). The in-code comments classify these as 'reactions/clamps, no deposit' and spec §4 defines a deposit only for vel_damp, but MASTER-RULES grand-energy + the KE-counting auditor support the opposite reading. A genuine spec ambiguity worth a user call (e.g. at T4 with the F1 ledger).",
        "severity": "low",
        "evidence": "`if(uy<0 && wall_neighbor(...))uy=0;` and CFL `s2=vmax/spd` run BEFORE apply_vel_damp with no ΔE. Batch A spec-4-5#14 [NOTE/ambiguity]; not queued — carried as NEW so the user call surfaces.",
        "verdictReal": false,
        "verdictTracked": false,
        "refuteReasoning": "The candidate ND-18 claims wall no-penetration zeroing, cross-species no-flux zeroing, and the CFL speed cap silently destroy kinetic energy with no E deposit/ledger, in tension with MASTER-RULES grand-energy + \"nothing is silently dropped.\" Reading the cited clauses defeats this under a concrete compliant reading:\n\n(1) MASTER-RULES line 67 is explicit and anticipates exactly this question: \"Momentum: advected with mass; swaps permute it; the only sink is vel_damp (→ heat, same cell).\" The rules deliberately enumerate momentum's three fates and designate ONE heat-depositing sink: vel_damp. Wall/cross-species zeroing and the CFL cap are by design NOT in the \"sink → heat\" category. The in-code comments (engine_b.hpp:1701, 1797: \"reactions/clamps, no deposit\") trace directly to this sentence.\n\n(2) The finding's core premise — that wall/cross-species zeroing \"destroys KE from an impact\" — is the misreading. These are free-slip no-penetration BOUNDARY CONDITIONS, not friction/dissipation. Spec §4 (lines 182-183) explicitly: \"Solid faces: closed (no flux, no force exchange...). The v3 one-sided wall-reaction kick is DELETED; no transient is ever read as a physical impulse.\" A free-slip wall does no work and exchanges no force; the removed normal component is a non-physical velocity pointing into a cell the parcel cannot enter (there is no real ½m·u² impact to conserve). Depositing heat for it would REINTRODUCE the deleted wall reaction and actually violate §4. The cross-species zeroing is the same free-slip no-flux BC (in-code comment lines 1768-1781), velocity-only with mass untouched.\n\n(3) The CFL cap is named in spec §4 line 179 as a velocity INVARIANT ('‖u⃗'‖ ≤ dx/dt'), listed alongside the void floor. The authors ledgered the void floor's removed mass+E explicitly right beside it (\"mass AND E ledgered to the boundary ledger… ≤ ~1 J/event\") but attached NO ledger clause to the CFL cap — a positive design choice, not an oversight. The CFL cap is a numerical-stability invariant, spec-sanctioned, with vel_damp remaining the sole heat sink.\n\nThe \"opposite reading\" the finding leans on (grand_energy / KE-counting auditor) is refuted: the BCs remove non-physical into-wall velocity (no work, nothing physical dropped), and the CFL cap is an explicitly-named invariant governed by the \"only sink is vel_damp\" rule. This is spec-sanctioned behavior, not new unfiled drift. The strongest fragment (CFL cap scales a genuinely-physical over-CFL velocity) is at most already covered by the explicit \"only sink is vel_damp\" rule, i.e. an accepted note, not REAL-DRIFT.",
        "labelReasoning": "Verified the cited code: engine_b.hpp:1690-1697 (swap-path) and :1761-1792 (normal-path) zero velocity components into walls / cross-species no-flux interfaces and apply the CFL cap BEFORE apply_vel_damp, with comments at :1701 and :1796-1797 explicitly declaring these \"reactions/clamps, no deposit.\" So the ½m·u² removed by these clamps lands in neither E nor any ledger. That is real: MASTER-RULES (00-MASTER-RULES.md:65-67) says grand ENERGY is exact every step and \"nothing is silently dropped,\" and grand_energy in engine_b_real_lut.hpp counts the ½m|u|² kinetic term — yet spec §4 defines a heat deposit ONLY for vel_damp. Both readings (clamp = legitimate non-deposit reaction vs. clamp = silent KE drop) are defensible, so it is a genuine spec ambiguity, not a coding bug — bounded at ½m·(dx/dt)² per event.\n\nALREADY-LABELED ruled out: the finding is NOT tracked with an owner. (1) In-code there is no T?-OPEN/TODO(T_n)/DEBT(owner:) on this KE channel — only an affirmative anti-debt assertion that no deposit is owed. (2) The known queue's F1 fold-in (T4-continuation.md:115) is scoped to the m<ε_mass vacuum-guard relabel dropping booked CONDUCTION/advection E (~542 J/event) — a distinct channel from KE destroyed by velocity-zeroing on cells that are NOT emptied. (3) The user ratification queue items 1-9 cover the −mg/2 free-surface pump, gas-P, ¼Δρ, gas floor, sub-min, open-gas-column, Neumann wording, leveling speed — none is the wall/CFL KE sink. The ONLY place this finding appears is the partial-findings.md salvage batch item #14, which the handoff explicitly marks \"RAW + UNVERIFIED — no dedup, no adversarial lenses\" — i.e. the same candidate this very audit pass is adjudicating, not an owned tracking record. Hence REAL-DRIFT (a tracked-nowhere-with-owner spec ambiguity).",
        "disposition": "Record as an accepted note (no spec amendment, no code fix): MASTER-RULES line 67 already designates vel_damp as the sole heat-depositing momentum sink, and spec §4 lines 182-183 classify wall/cross-species zeroing as free-slip no-penetration BCs that do no work (the deleted wall-reaction kick) while line 179 names the CFL cap as a velocity invariant — so the cited code is compliant under the controlling reading; if the user still wants the ambiguity surfaced, raise it only as a confirm-the-note item at T4, not as queued drift."
      }
    ],
    "knownConfirmed": [
      "Queue#1 — §4 force/relaxation reads gas/vacuum EOS anchors (gas_anchor_v42/p_eos4 at a_i,a_j,ownf) instead of ½(P_i+P_j), contradicting §2.1 'p_eos never read by the force' (Batch A spec-4-5#2/#3, law-8-9#22; Batch B law-1-3#3, spec-2-3 §2.1:100, spec-1-manifest). Ratification-queue item 1.",
      "Queue#2 — gas cells' relaxed P is force-dead (every gas face reads ownf/anchor, never C.P[i]) + implicit gas relaxation source P_new=[(1−ω)P+ω(target+α·a)]/(1+ωα) (same fixed point, sanctioned). Ratification item 2 (Batch A spec-4-5#3; Batch B spec-2-3 §3.1:141).",
      "Queue#3 — liquid|liquid face adds ¼(ρ_j−ρ_i)·g·dx·DY beyond law #2's literal ½(P_self+P_neighbor) (Batch A spec-4-5#4; Batch B law-1-3#2). Ratification item 3.",
      "Queue#4 — gas_anchor_v42 1%-under-rest floor (−0.01 constant) + dual raw(neighbor)/floored(own) gauge readings per gas cell; floored gauge also feeds the §3.1 relaxation source (Batch A law-8-9#22; Batch B spec-1-manifest, spec-2-3 §3.1:125). Ratification item 4.",
      "Queue#5 — §5.2 sub-min-donor full-merge branch HELD (liquid want=0; gas trickles via EOS ladder); worked examples 130→[125|880]/250→[125|1000] bit-exact, only D≤min deviates (Batch A spec-4-5#7, law-8-9#11, spec-6#8). Ratification item 5.",
      "Queue#9 — solid/world-edge faces are not force-silent: p̄=P_i Neumann ghost (load-bearing for INV-P2), wording absent from §4; v3 one-sided wall KICK verified DELETED (Batch A spec-4-5#5). Ratification item 7 (spec-wording).",
      "Queue#10 — κ·divU sourced from PERSISTED (pre-ENCODE snapshot) velocity not post-ENCODE, to avoid a phantom −g·dt convergence; spec un-pinned (Batch A; Batch B law-1-3#9, spec-2-3 §3.1:126). Ratification item 7.",
      "Queue#12 (F1) — §D.4 DECODE vacuum guard zeroes mNew<ε_mass cells' mass AND booked conduction E (~542 J/event class) with no boundary ledger; T3 reduced trigger to ~0 (source gate :1519), class open, owner T4 (Batch A spec-4-5#1, law-6-7#8, law-8-9#12, spec-6#6; Batch B spec-9-11).",
      "Queue#13 — enthalpy_carrier waterfall conservation bound widened ×5 (1e-3→5e-3) during T3's face-value repair; T4 fold-in restores 1e-3-class discrimination (Batch A law-6-7#16).",
      "Queue#14 — gas flux DIRECTION uses post-force w_eff while CARRIED momentum is pre-force u_g (rising parcel carries downward cargo); liquids keep pre-force w; both labeled DEBT owner T4 R1 (Batch A spec-4-5#6, law-6-7#9; Batch B law-1-3#4).",
      "Queue#15 — gasSpent single-spend makes gas EOS-ladder allocation among competing donors iteration-order-dependent (total-correct), INV-7 blocker; T4 absorbs ladder/spent into R1.5 (Batch A law-8-9#13).",
      "Queue#16 — §6.3 eviction decrements recvRoom but never charges gasSpent → one-tick gas-ladder overshoot into same recipient, conserving (Batch A law-8-9#14, spec-6#5).",
      "Queue#17 — canDrain is gasSpent-blind (reads each face's FULL ladder room), can over-promise the full-drain cohesion exemption when gas donors converge; self-corrects next tick; ARG-PARITY markers (Batch A law-8-9#15).",
      "Queue#19 (INV-RR) — vacuum receiver seeded recvRoom=+INF so multiple donors can jointly overfill one vacuum cell past maxMass in a tick; T4 INV-RR named kill target (Batch A law-8-9#10, spec-6#3).",
      "Queue#20 — cohesion_settled gate tolerates a 150-tick sub-min streak, far looser than §6.4 next-RESOLVE drain; no DECODE flag (Batch A law-8-9#16, spec-6#9).",
      "Queue#21 — bug3_accept seeds lava cells at 3100 kg above v4 maxMass 2650 (stale pre-v4 premise); T7 sweeps stale seeds (Batch A law-8-9#17).",
      "Queue#22 — vacuum cells excluded as relaxation centers (held P≡0, continue) vs §3.1 'every non-solid cell'; behavior-neutral literal deviation (Batch B law-1-3#8, spec-2-3 §3.1:118).",
      "find_chain_hop — MASTER-RULES-banned symbol still live on the injection path (N-deep DFS push chain); T4 fold-in routes injection escape through standard displacement or escalates (Batch A law-8-9#4, spec-6#13).",
      "Gas|gas faces carry side-dependent values off-equilibrium (bounded momentum-fabrication channel) — named T4 fold-in / queue #18 family; NOTE this is DISTINCT from ND-3 (cross-gas absolute vs gauge), which a perfectly antisymmetric fix would still leave wrong.",
      "LADDER_BETA=0.5 + LADDER_REST_DEADBAND=0.99 — T3 gas-ladder tuning knobs absent from §1.3 manifest but carry in-code owner labels ('retired by T4's flux-intent rebuild'); part of the queued gas-ladder/spent machinery (queue 2/4/7 + T4 fold-in). Labeled debt with owner (Batch B spec-1-manifest)."
    ],
    "pendingNotes": [
      "T4 (RESOLVE→5 micro-passes): resolve_world is a single monolithic pass (DriveCtx + swap-select + vacClaim + canDrain + displacement pre-pass + one additive flux/conduction loop), not R0/R1/R1.5/R2; all cross-cell work stays inside RESOLVE (law-#4-compliant at phase boundaries). The transient SoA buffer set (budget/recvRoom/gasSpent/vacClaim/canDrain/disp/drive/swap/CellAccum) diverges from §9's named list (swapChoice/swapMutual/f[6]/σ/ρ′). Snapshot is taken PRE-ENCODE not post-ENCODE (§6.1). Expected pre-v4; T4 owns (Batch B law-4-5, spec-9-11; Batch A spec-6#1/#19).",
      "T5 (§8.2 conduction limiter): live RESOLVE conduction is already law-shape-compliant (on E, antisymmetric, harmonic-mean k_face, SYMMETRIC per-face conductance cap g≤CiCj/(Ci+Cj)/6 — not one-sided) but differs from §8.2's exact mechanism (scale offending face fluxes until T_next∈[stencil min,max]; dt≤m·cp·dx/Σk·A bound) — INV-COND absent. The LEGACY separate T-buffer conduction pass (one-sided clip + [0,6000] rail + re-encode E from clipped T) still exists OFF the live path, exercised only by direct unit tests; T5 retires it AND re-authors the tests that pin it (Batch A law-6-7#6/#7).",
      "T6 (§8.3 radiation): entirely absent — no σ_SB/ε consumer/T_sky/INV-RAD/INV-QUENCH; ε column stored, correctly labeled storage-only. T_sky=270 K manifest knob has no symbol (Batch A law-6-7#3; Batch B spec-1-manifest).",
      "T7 (§8.1 enthalpy curves + §8.4): enthalpy is pre-v4 linear E=m·cp·T; DECODE keep-E relabel jumps T by cp_old/cp_new (water→steam ~750 K superheat; steam→water ~185 K cascades to ICE — flip-flop class); latentHeatMin/Max stored unconsumed; no plateaus, no band relief; INV-LAT/STEAM absent (Batch A law-6-7#4). FORWARD-RISK flagged: grand_energy/test auditor reconstructs Σm·cp·T_curr instead of summing persisted Chunk::E — float-exact today, structurally breaks every conservation assertion the moment latent plateaus land (E≠m·cp·T on a plateau); NOT named in the T7 fold-in list (only bug3 lava seeds are) — T7 must re-base it on ΣC.E[i]+kinetic. Same reconstruct-from-T hazard at relocate_chain (re-encodes E per hop) (Batch A law-6-7#12/#15, t1-data-jni#5).",
      "T8 (§7 full-payload swap + §5.3 cadence): swap cadence is the retired √μ form period=swap_visc_rate·√(μ_i+μ_j)·(R/F) gated by global-clock fmod(simClock,period), NOT the ratified t_swap=max(t_swap_min,(μ_i+μ_j)/(Δρ_eff·g·dx)) with per-pair swapReady accumulator; no t_swap_min (0.5 s) symbol, no swapReady field (both §1.1/§1.3 manifest-absent); the dead dPE<−1e-3 co-gate is still live; swap does NOT deposit |ΔPE| as heat (§7.2). swap_visc_rate=0.3 is a manifest-absent knob. INV-DT (no dt=0.25 vs 0.5 comparison test anywhere — all tests hard-code dt=0.5) and INV-SWAP2 cadence are T8 (Batch A spec-4-5#11, law-6-7#2; Batch B law-1-3#6, spec-1-manifest, spec-9-11).",
      "T9 (§0 same-species ρ_eff(β) convection): swap buoyancy uses RAW mass diff (mUp−mLow)·g, not ρ_eff=(m/V)(1−β(T−T_ref)); β/thermalExpansion stored but unread in the swap path; same-species thermal convection (INV-CONV 126 N) unreachable (equal raw mass⇒forceDiff=0, and the cohesion term blocks same-species pairs which §0 says get NO cohesion). §10 honest-ledger 'liquid β-swap' row overstates current reality. T_ref,global=288 K is currently the single Globals::T_ref field doubling as the EOS fallback — when T9 lands the ρ_eff and EOS-fallback roles must not be conflated (Batch B law-1-3#5, spec-1-manifest, spec-9-11).",
      "T10 (JNI/Java integration): 12-array ABI unchanged; the five v4 LUT columns (ε/β/latentMin/Max/T_ref_gas) stubbed to 0 under accurate TODO(T10); JNI exchanges intensive T/v and reconstructs E from (T,mass,cp) on load — reconstruct-from-T breaks on latent plateaus (T7); a JNI-registered steam would rest +24 kPa (T_ref_gas=0→Globals::T_ref=288 fallback). Java Material record lacks the five columns; no external momentum-impulse plumbing exists (encrypt_cell takes no impulse param). Inert in-game (parent gitlink pinned pre-arc) (Batch A law-6-7#17, t1-data-jni#4/#7, law-8-9#18, spec-4-5#15; Batch B law-1-3#7, spec-1-manifest).",
      "§1.3 retired-symbol survival: T_curr/T_next still exist as persisted-shaped std::vector members on Chunk (re-derived from E every tick via derive_world_T, so E is the across-tick truth and drift-test (c) is NOT violated — a manifest/wording mismatch, not a physics change). NO task in T4-T10 explicitly owns deleting them (T5 kills the pass needing T_next, T10 kills the JNI exchange needing T_curr — an OWNERSHIP GAP to flag) (Batch A law-6-7#1; Batch B spec-1-manifest).",
      "§5.1 stacked-chain yield-propagation (F_out=F_in+m·(g·n̂)−τ_y·A) implemented NOWHERE and NO task in the T4-T10 map owns §5.1 — behaviorally compliant today (τ_y∈{0,∞}) but an OWNERSHIP GAP so the chain rule is not silently dropped from the arc (Batch A spec-4-5#10).",
      "swapReady persisted field (§1.1) and void_ix-as-per-cell (§1.1 lists per-cell, code is a per-chunk sentinel — sanctioned interpretation) — §1.1 persisted-set ledger items for the eventual INV-3 manifest diff; swapReady is T8/T10 (Batch B spec-1-manifest)."
    ],
    "dropped": [
      "Batch A spec-4-5#12, #13; law-6-7#3/#6/#13/#14; law-8-9#20/#21; spec-6#1/#7/#14/#15/#16/#17/#19; t1-data-jni#1; Batch B law-1-3#9/#10 (NOTE) / law-4-5#3-#7 / spec-1-manifest (Material LUT clean, κ/vel_damp ranges, Gauss-Seidel in-place, void_ix, P single-buffer) / spec-2-3 (EOS formula exact, sealed Neumann, implicit-source fixed point, Gauss-Seidel) / spec-9-11 (κ once-per-tick dt-note, §11 invariant-roster disposition) — all [NOTE]-tagged COMPLIANCE records or pre-v4-mechanism confirmations with no untracked drift; dropped from new_unique as they assert conformance, not deviation. The substantive ones are folded into pending_notes by task.",
      "Batch A law-8-9#1 vs the RESOLVE no-op: kept the injection-side destroy findings (ND-8/ND-9) but did NOT duplicate the RESOLVE push-with-no-room path, which the same finding confirms correctly no-ops — that half is compliant.",
      "Batch B spec-2-3 §3.1:125 floored-relaxation-source and §3.1:141 gas-force-dead: merged into queue#4 and queue#2 known_confirmed respectively rather than listed as new sites, since the root cause (floored anchor / force-dead gas P) is already the named queue mechanism.",
      "Batch B spec-1-manifest gas_anchor −0.01 floor constant: folded into queue#4 known_confirmed (the −0.01 literal is the manifest-absent constant rolled into the queued 1%-under-rest floor mechanism), not a separate ND.",
      "Batch A law-6-7#5 and the keep-E relabel: only the STALE COMMENT (ND-19) carried forward as new; the relabel mechanism itself is pending_notes T7 (not double-counted).",
      "Duplicate §6.2 +1e-3 eviction overshoot raised in BOTH Batch A law-8-9#19 and spec-6#2 and t1-data — merged into single ND-14.",
      "Duplicate injection-momentum gap raised in Batch A law-6-7#10, law-8-9#7, spec-6#12 — merged into single ND-7.",
      "Duplicate is_compressible dual-cutoff raised in Batch A t1-data-jni#3, law-8-9#6, Batch B spec-1-manifest — merged into single ND-4."
    ]
  }
}