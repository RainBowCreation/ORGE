# Drift-audit PARTIAL findings — salvaged 2026-06-11 (session-end)

**Status: RAW + UNVERIFIED.** 5 of 10 audit dimensions completed before the controller session was
stopped (weekly limit). NO dedup, NO adversarial refute/label lenses ran — expect overlaps with the
known queue (handoff ratification list) and some misreadings. The synthesis+verify stages of
`2026-06-11-drift-audit-workflow.js` must still run over THESE findings plus the 5 missing dimensions:
**law-1-3, law-4-5, spec-1-manifest, spec-2-3, spec-9-11** (edit the script's DIMS to just those 5,
then synthesize the union).

## Dimension: spec-4-5 (force/vel_damp/void-ledger/yield/cohesion/visc-rate) — 15 findings

1. **[DRIFT-KNOWN/high]** Spec §4: "void floor m < ε_mass ⇒ u⃗=0, mass AND E ledgered to the boundary ledger (law #9 has no de-minimis clause — nothing silently vanishes; magnitude ≤ ~1 J, ~1e-6 kg per event)" @ `ORGE-ENGINE/core/engine_b.hpp:1744-1751 (§D.4 vacuum guard); ORGE-ENGINE/core/sim_engine.hpp:485-488 (the only LedgerAccum)`
   - The vacuum guard zeroes mass, momentum, AND booked E with no ledger write of any kind. NO boundary-ledger structure reachable from step_world_b exists anywhere; the only ledger in the codebase (LedgerAccum{injected, sealedLoss}) is mass-only (no energy slot at all) and serves exclusively the injection path in sim_engine.hpp — so even structurally there is nowhere to ledger the E the spec requires. This is the F1 class in the known queue (T4 owner).
   - evidence: Code: `if (mNew < G.eps_mass) { ... C.mass_kg[i] = 0.f; ... C.px[i]=C.py[i]=C.pz[i]=0.f; ... C.E[i]=0.0; continue; }` — mass up to 1e-6 kg and any booked antisymmetric conduction/advection E (partner already paid its half) are silently destroyed. Spec demands both ledgered; LedgerAccum has fields `injected`/`sealedLoss` only, no E field.
2. **[DRIFT-KNOWN/high]** Spec §4: "p̄_f = ½(P_i + P_j); against gas/vacuum: ½(P_i + anchor Φ_f)" + §2.1: "p_eos is never read by the force" @ `ORGE-ENGINE/core/engine_b.hpp:750-757 (DriveCtx pbar: vacuum and gas-neighbor branches), :640-690 (face-value rule header)`
   - Implemented face values at gas/vacuum faces are NOT the ratified ½(P_i+Φ_f): any|VACUUM reads p̄=0 flat; liquid|GAS reads p̄ = a_j + ρ_j·g·(dx/2)·DY with a_j = the RAW p_eos4 (sub-min override to 0) — the §4 half-cell face-value repair (v4.2 ratification candidate), and the force reads EOS anchors directly, contradicting §2.1's sentence. Known queue items 1 and 4 (incl. the gas_anchor_v42 1%-under-rest floor + dual raw/floored gauge readings, :222-227, :725-727 vs :756).
   - evidence: Code: `if (mjm <= G.eps_mass) { pbar = 0.0f; } else if (is_gas(Mf)) { const float a_j = (mjm < Mf.minMass) ? 0.0f : ef->cells[fj].p_eos4; pbar = a_j + (mjm/G.V)*ghalf*DY[f]; }` vs spec `½(P_i + anchor Φ_f)`. Justified in-code by the −mg/2 static-heat-pump proof; sits in the user ratification queue.
3. **[DRIFT-KNOWN/medium]** Spec §4: "The force reads only `P` (and its boundary anchor)" / §3.1: gas cells participate fully in the relaxation @ `ORGE-ENGINE/core/engine_b.hpp:758-759, :735-738, :745, :753 (GAS i branches read ownf, never C.P[i])`
   - A gas cell's force never reads its own relaxed P — every face of a gas cell uses its own EOS anchor extrapolation (ownf = a_i − ρ_i·g·(dx/2)·DY), so the relaxed gas P is force-dead (kept only as a diagnostic/source-tracked value). Known queue item 2 (decide: delete, re-couple, or keep diagnostic).
   - evidence: Code: `pbar = i_gas ? ownf : P_i;` (solid/edge faces) and `} else if (i_gas) { pbar = ownf; }` (gas|liquid) — `P_i = C.P[i]` is consumed only on non-gas branches.
4. **[DRIFT-KNOWN/medium]** Law #2 / Spec §4: face pressure ½(P_self + P_neighbor) between fluid cells @ `ORGE-ENGINE/core/engine_b.hpp:760-766 (liquid|liquid branch)`
   - liquid|liquid faces add a ¼(ρ_j−ρ_i)·g·dx·DY density-difference correction to the law's literal ½(P_i+P_j) (the mean of the two sides' own half-cell hydrostatic extrapolations). Known queue item 3; probe-justified in-code (partial-over-full +1250 N limit cycle) but extends the ratified formula.
   - evidence: Code: `pbar = 0.5f*(P_i + Cf->P[fj]) + 0.5f*((mjm/G.V) - rho_i) * ghalf * (float)DY[f];` (ghalf = 0.5·g·dx ⇒ the second term = ¼Δρ·g·dx·DY) vs law #2's `½(P_self + P_neighbor)`.
5. **[DRIFT-KNOWN/low]** Spec §4: "Solid faces: closed (no flux, no force exchange; shielding via the §3.1 stencil)" @ `ORGE-ENGINE/core/engine_b.hpp:738, :745, :753, :673-677 (solid/world-edge face p̄ = P_i Neumann ghost)`
   - Solid and world-edge faces are not force-silent: a liquid's solid face contributes p̄=P_i (Neumann ghost — the wall pushes back with the cell's own pressure, load-bearing for INV-P2), and a gas's contributes ownf. Adjudicated law-#2-correct at T2b but the ghost wording is absent from §4. Known queue item 9. The v3 one-sided wall-reaction KICK is verified deleted (force_advect.hpp gone from core/, no kick site; DECODE wall handling is no-penetration clamps only) — that half of the sentence is compliant.
   - evidence: Code: `pbar = i_gas ? ownf : P_i; // SOLID: closed` — the face still contributes −p̄·A·dt·n̂ to the surface integral (a wall force), where a literal "no force exchange" reading would skip the face and un-balance the integral.
6. **[DRIFT-KNOWN/medium]** Spec §4: "+ Σ_faces ±ṁ_f·u⃗_donor — advected momentum" @ `ORGE-ENGINE/core/engine_b.hpp:783-785 (gas w_eff fold-in), :1523-1528 (labeled-debt carried-momentum site), :699-702 (liquids keep pre-force w, labeled T4)`
   - For gas donors the flux DIRECTION is decided by the post-force effective drive w_eff while the CARRIED momentum is still the pre-force u_g — a rising parcel carries downward cargo. Liquids' drive map is pre-force w entirely. Both labeled in-code with owner T4 (unify drive maps); known queue item 14 + T4 fold-in list.
   - evidence: Code: `d.wy[i] = (ei->cells[i].ugy + iy/m_i) * dragScale;` (direction) vs `float ugx=ed->cells[di].ugx ...; Ai.dpy += -mdot*ugy;` (cargo), with the comment "LABELED DEBT (owner: T4 R1, unify drive maps)".
7. **[DRIFT-KNOWN/medium]** Spec §5.2: "…else `f ← D` if `R + D ≤ maxMass`, else `f ← 0`" (the D≤min full-merge branch) @ `ORGE-ENGINE/core/engine_b.hpp:1468-1491 (sub-min donor branches inside the budget/cohesion gate)`
   - The D≤min case deviates from the ratified algorithm: a sub-min LIQUID donor is HELD (want=0, pre-T3 behavior; full-merge was implemented in T3 and reverted after probes) and a sub-min GAS donor trickles gradually under the EOS-ladder room. The two worked examples (130→[125|880], 250→[125|1000]) are bit-exact on the live path. Known queue item 5 — sits in the user ratification queue (amend §5.2 vs T5 receiver-side drain).
   - evidence: Code: `} else if (is_gas(Mdon)) { /* trickle: keep W/room-clamped proposal */ } else { want = 0.0f; /* SUB-MIN LIQUID donor: HELD */ }` vs spec `f ← D if R + D ≤ maxMass`.
8. **[DRIFT-NEW/medium]** Spec §5.2: "σ's donor budget is `m − minMass` (or `m` when a SINGLE full-drain face is the ONLY outflow), so post-scale donors never land in (0, min)" @ `ORGE-ENGINE/core/engine_b.hpp:1112-1115 (canDrain exemption rule), applied at :1463-1465`
   - The full-drain exception is materially BROADER than ratified: canDrain exempts any donor whose SUMMED drive-limited outflux over ALL outflow faces ≥ its snapshot mass — so a donor splitting its outflow across several faces (e.g. down + lateral) may fully drain to 0, where the spec text grants the m budget only when one single face fully drains and is the only outflow (otherwise budget m−minMass retains the cohesion floor). Not in the known queue; the in-code comments cite pre-v4 sources ("canonical §3, audit #3") and carry no owner-task debt label against §5.2.
   - evidence: Code: `(*cd)[i] = (outFlux >= mIi - G.eps_mass) ? 1 : 0;` where outFlux = Σ over up to 6 faces of `std::min(capf, rho_i*W*A*dt)` — a multi-face sum — vs spec's "a single full-drain face is the only outflow". Behavioral divergence example: 1000 kg water with 600 down + 500 sideways available ⇒ code drains to 0; spec budget = 875, donor keeps 125.
9. **[DRIFT-NEW/low]** Spec §5.1 "Net force vs τ_y·A_face [N vs N]" + §0 swap resistance "k_c·min(minMass)·g + τ_y·A_face" + §1.2 schema "τ_y [Pa]" @ `ORGE-ENGINE/core/engine_b.hpp:157-161 (swap_resistance); ORGE-ENGINE/core/sim_engine.hpp:56 (Material::yieldStress declared "N")`
   - The yield term in the implemented swap gate omits the ·A_face factor and the Material struct declares yieldStress in Newtons, contradicting the spec's τ_y [Pa] with the gate converting Pa→N via A_face. Numerically inert today (A=1 m² and τ_y ∈ {0, ∞} across the LUT) but the manifest unit and the formula text mismatch the ratified spec, and any future A≠1 or finite-τ_y material silently inherits the wrong scaling. Not in the known queue; no owner-task label.
   - evidence: Code: `float yld = std::max(a.yieldStress, b.yieldStress);` added straight into resistance [N] with no `* G.A`; struct comment `float yieldStress = 0.0f;  // N — Bingham yield` vs spec §1.2 `τ_y [Pa]` and §5.1 `τ_y·A_face [N vs N]`.
10. **[PENDING-IMPL/medium]** Spec §5.1: "Stacked chains, direction-projected: F_out = F_in + m_own·(g⃗·n̂_chain) − τ_y·A_face — weight ADDS down-chain, subtracts up-chain, contributes 0 sideways" @ `ORGE-ENGINE/core/engine_b.hpp:247-252 (labeled DEFERRED comment in encrypt_cell); grep: only τ_y consumer is swap_resistance (:159)`
   - The stacked-chain yield-propagation rule is implemented NOWHERE — no chain accumulation, and no net-force-vs-τ_y·A comparison exists on any flux/PUSH path (yield appears only in the swap gate). Behaviorally compliant today: τ_y=∞ solids never yield/swap (∞ resistance + the μ=∞ frozen-terrain exclusion upstream), τ_y=0 fluids need no gate, and the deferral is labeled in-code per the user's granular-stage decision. HOWEVER no task in the T4–T10 map owns §5.1, so the chain rule has no scheduled implementer — flagged so it is not silently dropped from the arc.
   - evidence: Code comment: "DEFERRED (v4 §5.1 ...): the Bingham yield_stress term belongs HERE on the net drive force ... Static yield against post-RESOLVE load (sand avalanche, dam burst) is a later granular stage ... Do NOT add a yield branch". Task map T4–T10 contains no §5.1 entry.
11. **[PENDING-IMPL/medium]** Spec §5.3: "t_swap = max(t_swap_min, (μ_i+μ_j)/(Δρ_eff·g·dx)); swapReady += dt/t_swap; fire when ≥1 (carry remainder)"; "t_swap_min = 0.5 s seconds floor"; "The dead dPE<−1e-3 co-gate is gone" @ `ORGE-ENGINE/core/engine_b.hpp:174-180 (swap_cadence), :974-975 (dPE co-gate), :1002-1005 (fmod global-clock window); ORGE-ENGINE/core/sim_engine.hpp:121-186 (Chunk: no swapReady field, grep-zero engine-wide)`
   - The whole §5.3 cadence is still pre-v4: period = swap_visc_rate·√(μ_i+μ_j)·min(1, R/F) gated by fmod(simClock, period) instead of the ratified t_swap formula with a per-pair swapReady accumulator; the dPE<−1e-3 co-gate the spec declares dead is live; no t_swap_min seconds floor; the persisted swapReady field does not exist. All exactly the expected pre-T8 mechanism per the task map (T8 = §7 payload + §5.3 cadence; swapReady persistence T10); nothing here contradicts the law beyond what the queue tracks (viscosity stays a rate, never a threshold — compliant with law #5).
   - evidence: Code: `float base = G.swap_visc_rate * std::sqrt(a.viscosity + b.viscosity); ... return base * excess;` + `if (dPE >= -1e-3f) continue;` + `std::fmod(world.simClock, (double)period)` vs spec's `max(t_swap_min, (μ_i+μ_j)/(Δρ_eff·g·dx))` with swapReady accumulation. Chunk struct persists matIx/T_curr/T_next/E/mass_kg/px/py/pz/P/void_ix — no swapReady.
12. **[PENDING-IMPL/low]** Spec §5.2/§6.1: "legalize per-face first, then σ" with `σ_i = min(1, budget_i/Σ_k f_k)` and R1.5 `ρ'_i = min(1, room_i/Σ inbound f·σ)` @ `ORGE-ENGINE/core/engine_b.hpp:595-600 (budget seeded with full snapshot mass), :1450-1495 (sequential donor single-spend), :620-631 + :1502-1512 (sequential recvRoom single-spend)`
   - The legalize-then-scale ORDER is honored (capacity clamp incl. donor mass D and receiver room runs before the budget/cohesion stage), but the proportional σ/ρ′ scales are replaced by sequential face-iteration-order single-spend against budget=m (with the min floor enforced as a separate residual snap — net-equivalent to budget m−minMass for liquids; §5.2's worked examples reproduce bit-exact per the T4-continuation handoff). Order-dependence is the known INV-7 debt family (gasSpent allocation, queue 15). This is the expected pre-T4 monolith awaiting R1/R1.5.
   - evidence: Code order: `float room = max(0, Mr.maxMass − m_recv); float headroom_donor = sd->mass[di]; cap = min(room, headroom); ...` then `float avail = (*bd)[di]; if (want > avail) want = avail; float residual = avail − want; if (... residual < min_d) want = avail − min_d;` — sequential spend, no Σf_k proportional scale; vacuum recvRoom=INF (queue 19, T4 INV-RR).
13. **[NOTE/low]** Spec §5.3: "Flux rate: λ = μ/(ρ·dx²) [1/s], damping 1/(1+dt·λ) (kept)" + §4: "vel_damp: DECODE applies u⃗ ← u⃗/(1+vel_damp·dt); removed KE deposited as heat in the same cell (ΔE = ½m(‖u‖²−‖u'‖²))" @ `ORGE-ENGINE/core/engine_b.hpp:262-264 (ENCODE dragScale), :778-782 (DriveCtx gas duplicate), :1654-1660 (apply_vel_damp), :1702 + :1799 (both DECODE paths)`
   - VERIFIED EXACT: dragScale algebra is bit-for-bit the spec's 1/(1+dt·μ/(ρ·dx²)); apply_vel_damp is exactly u/(1+c·dt) with ΔE = k0·(1−vd²) = ½m(‖u‖²−‖u'‖²) deposited into the same cell's E on BOTH decode paths; the ‖u'‖ ≤ dx/dt invariant is enforced on both paths before persisting (cap applied pre-damp, so post-damp u' ≤ dx/dt holds a fortiori). One hygiene item: the drag law is duplicated in the gas fold-in with a "MUST MATCH … change both together" label instead of a shared helper, explicitly deferred to T4's ENCODE restructure.
   - evidence: Code: `float lambda = m.viscosity / (e.rho * G.dx * G.dx); dragScale = 1.0f / (1.0f + dt * lambda);` and `const float vd = 1.0f/(1.0f + G.vel_damp*dt); ... return k0 * (1.0 - vd*vd);` — both formulas match the spec expressions term-for-term.
14. **[NOTE/medium]** Spec §4 (only vel_damp's KE is given a heat-deposit) vs MASTER-RULES conservation: "nothing is silently dropped" / "the only [momentum] sink is vel_damp (→ heat, same cell)" @ `ORGE-ENGINE/core/engine_b.hpp:1690-1697 (swap-path wall zeroing + CFL), :1761-1792 (normal-path wall/xspecies zeroing + CFL cap), comments at :1701 and :1796-1797`
   - Wall no-penetration zeroing, cross-species no-penetration zeroing, and the CFL speed cap all remove momentum and destroy its kinetic energy with no E deposit and no ledger entry (an impact's ½m·u² simply vanishes; bounded by ½m·(dx/dt)² per event). The in-code comments deliberately classify these as "reactions/clamps, no deposit", and spec §4 defines a deposit only for vel_damp — but the MASTER-RULES grand-energy rule ("nothing silently dropped") and the rlut::grand_energy helper (which counts KE) support the opposite reading. Both readings are defensible; recording as a spec ambiguity worth a user call (e.g., at T4 when the F1 ledger lands).
   - evidence: Code: `if (uy < 0 && wall_neighbor(...)) uy=0;` and `if (spd > vmax) { float s2 = vmax/spd; ux*=s2; ... }` execute BEFORE `apply_vel_damp` with no ΔE accounting, vs comment "the wall zeroing + CFL cap above are reactions/clamps, no deposit".
15. **[NOTE/low]** Spec §4: "(gravity m·g⃗·dt + external impulse: ONCE, in ENCODE)" / law #2 "the optional caller-supplied external momentum-impulse … applied once, in ENCODE" @ `ORGE-ENGINE/core/engine_b.hpp:241-272 (encrypt_cell signature and body)`
   - ENCODE applies gravity only; no external-impulse input port exists anywhere in the pipeline (encrypt_cell takes no impulse parameter; the JNI injection path is the place/break mass boundary, not a momentum impulse). Since the term is "optional caller-supplied" and no caller supplies one today, nothing is dropped — but no task in T4–T10 adds the plumbing either (T10's ABI growth is LUT columns + E + swapReady).
   - evidence: Code: `inline CellEncrypt encrypt_cell(const Material& m, float mass, float T, float ux, float uy, float uz, const Globals& G, float dt)` — gravity at `e.ugy = uy - dt * G.g;`, no impulse argument; grep for "external"/"impulse" hits comments only.

## Dimension: law-6-7 (thermal pipeline + extensive-state law) — 17 findings

1. **[PENDING-IMPL/medium]** Law #7 persisted set ('matIx, mass, momentum, E, P + swapReady/void_ix ONLY; a stored raw T is the temp-ghost drift — forbidden') + spec §1.3 retired symbols ('must not reappear: T_curr/T_next') @ `ORGE-ENGINE/core/sim_engine.hpp:126-131 (std::vector<float> T_curr, T_next in Chunk); mitigation at core/engine_b.hpp:1878-1882 and core/sim_engine.hpp:234-242 (derive_world_T)`
   - Chunk still persists the two raw-temperature buffers across ticks. They are demoted to within-tick scratch (derive_world_T overwrites T_curr from E at the start of every step, so E is the across-tick truth and no physics reads a stale T on a cell with substance), but the struct fails the law-#7/§1.3 manifest. No task explicitly owns deleting them: T5 kills the legacy pass that needs T_next, T10 kills the JNI T-exchange that needs T_curr — flag the ownership gap. (World.bprimeRot, a dead ABI relic at sim_engine.hpp:199-200, is similarly outside the §1.3 manifest.)
   - evidence: Code: `std::vector<float> T_curr; // K (front buffer, derived per tick)` + `std::vector<float> T_next; // K (back buffer, conduction scratch)` persisted in Chunk vs spec §1.3: 'Retired symbols (must not reappear): … T_curr/T_next, persisted vx/vy/vz'. Persisted vx/vy/vz are genuinely gone (momentum px/py/pz only).
2. **[PENDING-IMPL/low]** Law #7 bookkeeping field 'swapReady, the swap-cadence accumulator' + spec §5.3 per-pair seconds-floor cadence + spec §7.2 ΔPE→heat @ `ORGE-ENGINE/core/engine_b.hpp:1002-1005 (fmod(world.simClock, period) cadence), :1681-1715 (swap decode: no ΔPE heat deposit); `grep swapReady` over core/jni/tests = zero hits`
   - swapReady is absent and the cadence is a global-clock fmod phase using the retired swap_visc_rate·sqrt form; the swap permutes (m, matIx, E, p⃗) correctly but does not deposit |ΔPE| as heat. All of this is the pre-v4 mechanism the task map assigns to T8 (§7 payload/cadence, INV-SWAP2/DT); nothing here contradicts the law in an untracked way.
   - evidence: Code gate `if (period > dt && std::fmod(world.simClock, (double)period) < (double)(period - dt)) continue;` vs spec §5.3 `swapReady += dt / t_swap; eligible when ≥ 1 (carry remainder)`; swap decode writes `C.E[i] = sp->E[j] + dampHeat` with no (m_h−m_l)·g·dx deposit (spec §7.2).
3. **[PENDING-IMPL/low]** Law #6 radiation ('radiation = the 6-face flux ε_eff·σ·(T_i⁴−T_j⁴)·A·dt … vacuum faces exchange with a world T_sky') / spec §8.3 @ `ORGE-ENGINE/core/sim_engine.hpp:57-61 (emissivity stored, labeled STORAGE-ONLY); repo-wide grep for 5.67e-8/σ_SB/radiation consumers = zero hits in core/jni`
   - Radiation is entirely absent — no σ_SB constant, no ε consumer, no T_sky exchange, no boundary ledger. Expected: T6 owns §8.3 (+INV-RAD/QUENCH). The ε LUT column is present and correctly labeled as storage-only awaiting its consumer.
   - evidence: Material comment: 'v4 §1.2 columns … STORAGE-ONLY for now: their consumers land in later stages — radiation (v4 §8.3)…'. Real LUT carries spec-exact ε (water .96, lava .95, stone .90, ice .97, gases 0) at tests/engine_b_real_lut.hpp:35-47.
4. **[PENDING-IMPL/medium]** Law #6 enthalpy curve ('E = m·h(T) piecewise linear … plateaus of width latentHeat … chain-anchored, relabel is identity on E (ΔE ≡ 0) and T continuous') / spec §8.1, §8.4 @ `ORGE-ENGINE/core/sim_engine.hpp:99-107 (orge_enthalpy: E = m·cp·T linear, T = E/(m·cp)); core/engine_b.hpp:1829-1855 (DECODE keep-E relabel, T rescaled by cp ratio, one relabel/tick, no plateau, no band relief)`
   - The enthalpy curve is the pre-v4 linear E=m·cp·T everywhere; DECODE's phase relabel keeps E but jumps T by cp_old/cp_new (water→steam ≈750 K superheat; steam→water ≈185 K, which is <273 K so it cascades to ICE on the next tick — the audit's flip-flop class). latentHeatMin/Max are stored but unconsumed. Expected: T7 owns §8.1 chain-anchored plateaus + §8.4 band relief (water→ice 83 kg push). Tracked; reported because the cp-jump mechanism now contradicts the amended law text verbatim while awaiting T7.
   - evidence: Code: `Tnew = orge_enthalpy::derive_T(Enew, mNew, cpNew, Tnew); sNew = tgt;` (pure cp-rescale, no L) vs law #6 'T is continuous across every transition' and 'plateaus of width latentHeat'.
5. **[NOTE/low]** Law #6 (amended 2026-06-10): latent plateaus and T-continuity ARE the law; MASTER-RULES 'comments lag — verify against v4' @ `ORGE-ENGINE/core/engine_b.hpp:1836-1840 (relabel comment block)`
   - Stale law claim: the in-code comment asserts the keep-E cp-jump 'is the documented law-§6 semantic (audit B-19; latent heat / hysteresis is a law change, user-held)'. The amendments were ratified 2026-06-10, so latent plateaus are now the law and this mechanism is T7-owned debt, not law-sanctioned design. The label should point at T7, not at a 'user-held law change'.
   - evidence: Comment text 'the keep-E T jump by cp_old/cp_new is the documented law-§6 semantic … latent heat / hysteresis is a law change, user-held' vs DESIGN-LAW.md #6 current text: 'whose inverse has plateaus of width latentHeat … relabel is the identity on E (ΔE ≡ 0) and T is continuous across every transition'.
6. **[PENDING-IMPL/low]** Law #6 max principle ('enforced conservatively — the offending FACE fluxes are scaled symmetrically, never a one-sided clip') / spec §8.2 limiter form + dt bound @ `ORGE-ENGINE/core/engine_b.hpp:1554-1588 (live RESOLVE conduction; cap at 1582-1583); core/orge_kernel.hpp:53-55 (keff)`
   - Live conduction is already law-compliant in shape: inside RESOLVE on E, antisymmetric ±same-double both sides, mass-free flux k_face·(Ti−Tj)·(A/dx)·dt with harmonic-mean k_face = 2k_ik_j/(k_i+k_j), and the stability guard is a SYMMETRIC per-face conductance cap g ≤ CiCj/(Ci+Cj)/6 (conservative — NOT a one-sided clip; conservation survives clamping). It differs from spec §8.2's exact mechanism (scale offending face fluxes until T_next ∈ [stencil min,max]; stated dt ≤ m·cp·dx/(Σ k_face·A) bound) — T5 owns alignment + INV-COND. Note the handoff's 'current one-sided clamp' description fits only the legacy off-path pass, not this live code.
   - evidence: Code: `const double gmax = (Ci * Cj) / (Ci + Cj) / 6.0; if (g > gmax) g = gmax; const double Q = g * (Ti − Tj); Ai.dE += -Q; Aj.dE += +Q;` — symmetric in (i,j), exact ΣdE=0; vs spec §8.2 'scaling the offending FACE fluxes themselves … until T_next ∈ [stencil min, stencil max]'.
7. **[PENDING-IMPL/medium]** Law drift-test (c) 'a separate conduction pass — is drift' + law #6 'never a one-sided clip, which would create/destroy energy' @ `ORGE-ENGINE/core/sim_engine.hpp:306-411 (simulate_section_16x16x16 one-sided clip at 363-365; swap_all_backbuffers re-encodes E from clipped T at 404-411); core/orge_kernel.hpp:58-63 (finalize_temp [0,6000] rail); off-path proof: jni/orge_jni.cpp:221-229 + engine_b.hpp:1904-1911 route everything to step_world_b`
   - The legacy separate T-buffer conduction pass still exists and is exercised by tests (correctness_test, engine_b_conduction_stability_test, engine_b_enthalpy_carrier_test case 5, stress_test). It one-sidedly clips T_next into [Tlo,Thi] plus a [0,6000] rail and then encode_world_E bakes the clipped T into E — energy created/destroyed whenever the clamp fires, and E mutated from a raw-T source. It is OFF the live path (both JNI pass bits route to the unified step_world_b) and labeled as such; T5's 'one-sided clamp dies' owns its retirement, so this is the awaiting pre-v4 mechanism, not new drift — but T5 must also retire/re-author the tests that pin it.
   - evidence: sim_engine.hpp: `if (Tn < Tlo) Tn = Tlo; else if (Tn > Thi) Tn = Thi; C.T_next[i] = Tn;` then swap_all_backbuffers: `encode_world_E(world, world.materials);` (E ← m·cp·T_clipped) vs law #6 'never a one-sided clip'. Live-path label at engine_b.hpp:1904-1909: 'The legacy T-buffer pass … stays available for its direct unit tests but is OFF the live path.'
8. **[DRIFT-KNOWN/high]** Law #6/#9 + spec §4/§6.4: 'm < ε_mass ⇒ … mass AND E both ledgered to the boundary ledger — nothing silently vanishes' @ `ORGE-ENGINE/core/engine_b.hpp:1744-1751 (§D.4 vacuum guard: C.E[i]=0.0 with no ledger)`
   - F1: the DECODE vacuum guard zeroes a fully-drained cell's E including booked conduction dE the antisymmetric partner already paid (−Q applied, +Q dropped) — up to ~542 J/event class. Known queue item 12; T3 reduced trigger frequency to ~0 via the sub-eps mdot source gate (engine_b.hpp:1514-1519) but the class is open, T4 fold-in owns closing it.
   - evidence: Code: `if (mNew < G.eps_mass) { … C.E[i]=0.0; continue; }` drops E_snap + A.dE unledgered vs spec §4 'mass AND E ledgered to the boundary ledger (law #9 has no de-minimis clause)'.
9. **[DRIFT-KNOWN/medium]** Law #6 advection ('the moving mass carries its momentum ṁ·u_donor') — donor-cargo coherence @ `ORGE-ENGINE/core/engine_b.hpp:1523-1528 (labeled), flux direction from w_eff at :1349-1356 vs carried u_g at :1528-1534`
   - For GAS donors the flux DIRECTION uses the post-force effective drive w_eff (defect-C fold-in) while the CARRIED momentum cargo is still the pre-force u_g — a rising gas parcel carries downward momentum cargo. Known queue item 14, labeled in-code with owner T4 R1 (unify drive maps).
   - evidence: Code comment: 'LABELED DEBT (owner: T4 R1, unify drive maps): for a GAS donor the flux DIRECTION uses the post-force effective drive w_eff … while the CARRIED momentum is still the pre-force u_g'.
10. **[DRIFT-NEW/medium]** Law #6 advection ('moving mass carries its momentum ṁ·u_donor') + Law #7 extensive coherence ('store EXTENSIVE … a stored raw v is the velocity-ghost drift') at the law-#9 place/break boundary @ `ORGE-ENGINE/core/sim_engine.hpp:804-844 (apply_injections writes matIx/mass_kg/T_curr + persist_cell_E, never px/py/pz) and :681-802 (relocate_chain shifts (species, mass, T→E) cell-to-cell, momentum never moved)`
   - The injection/displacement seam moves mass WITHOUT its momentum: a placed cell inherits the displaced incumbent's stale momentum vector re-paired with the new injected mass (derived v = p_old/m_new next step), and every chain-shifted parcel abandons its momentum in its old cell while adopting the destination's. E is handled at every one of these sites (persist_cell_E) but momentum is not — the exact extensive-pairing incoherence law #7 forbids, on a live path that runs every placement. Global Σp⃗ is untouched (mis-pairing, not fabrication) and CFL/vel_damp bound the consequence, but it is in neither the known queue (the queued find_chain_hop item is the banned-symbol routing, not this) nor labeled in-code with an owner.
   - evidence: apply_injections: `C->matIx[ci] = inj.species; C->mass_kg[ci] = inj.mass; C->T_curr[ci] = inj.temperature; persist_cell_E(*C, ci, mats);` — no px/py/pz write anywhere in the injection path (grep confirms), while the engine's own movers all carry ṁ·u_donor (engine_b.hpp:1528-1534) and the swap permutes momentum (engine_b.hpp:1710-1712).
11. **[DRIFT-NEW/medium]** Law #9 'the only source/sink is the caller's place/break at the boundary, separately ledgered' + MASTER-RULES 'Grand ENERGY exact every step … place/break go to the boundary ledger — nothing is silently dropped' @ `ORGE-ENGINE/core/sim_engine.hpp:484-490 (LedgerAccum: injected/sealedLoss MASS only), :779-781 and :834-837 (sealed incumbents: mass ledgered, E destroyed unledgered); jni/orge_jni.cpp:199-210 (ledgerOut = 2×matCount mass floats, no energy slots)`
   - The place/break boundary ledger has NO energy side: a sealed incumbent's enthalpy E (and momentum) is destroyed with only its mass recorded, and injected enthalpy enters unrecorded — grand-E books cannot be closed across the sanctioned boundary. Distinct from the queued in-step F1 vacuum-guard item (queue 12 covers the §D.4/§6.4 site, not the injection seam); untracked and unlabeled.
   - evidence: struct LedgerAccum { std::vector<double> injected; std::vector<double> sealedLoss; } — mass-only; seal path: `ledger.sealedLoss[incSpecies] += incMass;` then the incumbent's cell (with its E) is overwritten by the injection. Compare MASTER-RULES: 'vacuum-relabel/radiation-to-sky/place/break go to the boundary ledger — nothing is silently dropped.'
12. **[NOTE/medium]** Law #7 'store EXTENSIVE, derive INTENSIVE' / Law #6 advection carries ṁ·h_donor — future T7 hazard at the injection seam @ `ORGE-ENGINE/core/sim_engine.hpp:729-802 (relocate_chain SnapCell{sp, mass, T} parcels; E re-encoded per hop via persist_cell_E; mass-weighted T merges at :752, :775)`
   - relocate_chain transports parcels as (species, mass, raw T) and re-encodes E = m·cp·T at every hop instead of permuting the persisted E. Exact today (linear curve; merges are same-species/same-cp), but structurally a raw-T carrier that will fabricate/destroy latent E the moment T7's plateaus land (E is not recoverable from T on a plateau) — the in-engine twin of the queued JNI reconstruct-from-T (T10) problem. Loosely tracked via the queued find_chain_hop banned-symbol re-route; flagged so T7 does not miss this site if the re-route lands later.
   - evidence: `struct SnapCell { uint16_t sp; float mass, T; };` + `CT->T_curr[term.i] = (m0*CT->T_curr[term.i] + pred.mass*pred.T)/mn; … persist_cell_E(*CT, term.i, mats);` vs the swap path, which permutes the persisted E bit-exactly (`C.E[i] = sp->E[j] + dampHeat`, engine_b.hpp:1707).
13. **[NOTE/low]** Law #6 conduction = the 6-face flux (no swap exception stated) vs spec §6.1 R2 'a face fluxes iff neither endpoint is swapMutual; Conduction q + radiation q_rad accumulate here' @ `ORGE-ENGINE/core/engine_b.hpp:1271-1278 (cell-level continue for swap/displaced cells), :1316-1322 (per-face neighbor skips) — both precede the conduction block at :1554-1588`
   - Every face of a swapping or §6.3-displaced cell exchanges NO conduction for that tick (the early continues skip the whole face loop, and the swap decode would discard any accumulated dE anyway, so the skip is conservation-necessary under the current accumulator-discarding swap). The law states conduction as an unconditional 6-face flux; the spec's R2 wording arguably sanctions the same skip for q. One-tick, bounded, both readings satisfiable — T4's micro-pass restructure (and T8's payload work) should decide explicitly whether conduction rides swapMutual faces.
   - evidence: `if (ai->swap[i].idx >= 0) continue;` and `if (dCadd && ((*dCadd)[i] & 1)) continue;` occur before the per-face loop containing the conduction block; the swap decode reads only `sp->E[j] + dampHeat`, so a hot-lava|water pair swapping this tick conducts zero heat this tick.
14. **[NOTE/low]** Law #7 'void_ix, the engine free-list index' (per-cell persisted bookkeeping) / spec §1.1 'void_ix = engine bookkeeping (free-list slot index)' @ `ORGE-ENGINE/core/sim_engine.hpp:162 (`uint16_t void_ix = 0;` — one per Chunk, the void-SPECIES LUT sentinel)`
   - Name/semantic mismatch: the law and spec describe void_ix as a per-cell free-list bookkeeping field; the code's void_ix is a per-chunk 'which LUT slot is the void species' sentinel. Both are non-physical bookkeeping and the per-chunk form is arguably the sane reading, but the §1.3/INV-3 manifest diff (when T4 lands it) will trip on the wording. Cheap to reconcile in the spec text.
   - evidence: Code comment: 'material index per cell (0=void recommended)' + `uint16_t void_ix = 0;` per Chunk vs law #7 'two persisted bookkeeping fields … void_ix, the engine free-list index' listed inside 'Each cell stores'.
15. **[PENDING-IMPL/low]** Law #6 E-as-truth + spec §8.1 (T7): energy audits must read the persisted E, not reconstruct from T @ `ORGE-ENGINE/tests/engine_b_real_lut.hpp:79-96 (grand_energy = Σ m·cp·T_curr + ½m|p/m|²)`
   - The shared test energy auditor reconstructs internal energy as m·cp·T_curr instead of summing the persisted Chunk::E. Identical today (T is derived from E and round-trips bit-exactly per the carrier design), but it silently mis-measures once T7's latent plateaus land (E ≠ m·cp·T on a plateau) — every conservation test using it would lose discrimination exactly when it matters. T7 should regenerate it to read E directly.
   - evidence: `e += (double)m * (c * C.T_curr[i] + kin);` vs the persisted truth `C.E[i]` one field away; momentum/velocity side is already done correctly from px/py/pz.
16. **[DRIFT-KNOWN/low]** MASTER-RULES conservation ('grand mass exact every step') — thermal-arc test discrimination; known queue item 13 @ `ORGE-ENGINE/tests/engine_b_enthalpy_carrier_test.cpp:114-126 (waterfall bounds 5e-3, was 1e-3)`
   - The enthalpy-carrier waterfall conservation bound was widened ×5 (1e-3 → 5e-3) during T3's face-value repair. Known queue item 13; T4 fold-in owns restoring 1e-3-class discrimination (double-precision accumulation or per-tick antisymmetry assert).
   - evidence: Comment: 'Bound RE-CALIBRATED 5e-3 (was 1e-3) with T3's v4.2 §4 face-value repair' + `CHECK(std::fabs(rlut::grand_mass(w) - m0) < 5e-3, …)`.
17. **[PENDING-IMPL/low]** Law #6/#7 at the JNI boundary: E persisted truth must cross (reconstruct-from-T breaks on latent plateaus); law #8 LUT thermal columns @ `ORGE-ENGINE/jni/orge_jni.cpp:64-73 (TODO(T10): ε/β/latentMin/Max/T_ref_gas defaulted 0), :174-180 (load: E = cell_E(mass, cp, T)), :236-242 (store: T = derive_T(E))`
   - The JNI ABI still exchanges intensive T and v (each re-paired with mass at the boundary, so no decoupled ghost), reconstructs E from (T,mass,cp) on load, and stubs the five v4 thermal LUT columns to 0 with a labeled TODO(T10). All expected T10 work (E + swapReady crossing, LUT ABI bump); the 0-defaults are harmless at HEAD because no consumer exists until T6/T7, and T_ref_gas==0 correctly falls back to Globals::T_ref for the live EOS.
   - evidence: `// TODO(T10): plumb v4 LUT columns through JNI — ε/β/latentHeatMin/latentHeatMax/T_ref_gas are v4 §1.2 schema columns not yet in this 12-array ABI` + `C->E[i] = orge_enthalpy::cell_E(C->mass_kg[i], …, C->T_curr[i]);` matching the handoff's T10 line 'E must CROSS the JNI boundary (reconstruct-from-T breaks on latent plateaus)'.

## Dimension: t1-data-jni (LUT data exactness + JNI defaults + test-side energy defn) — 8 findings

1. **[NOTE/low]** v4 §1.2 table (all rows incl. ICE; latentHeatMin/Max mapped to the minTemp/maxTemp transitions; per-gas T_ref_gas; χ margins pinned 0.99980/0.99946) + law #8 fixed schema @ `tests/engine_b_real_lut.hpp:35-63 (rows), core/sim_engine.hpp:48-75 (Material decl)`
   - Independent cell-by-cell re-verification: every value in the real test LUT matches the spec table exactly — water 125/1000/1000 cp4186 k0.6 mu1e-3 ty0 eps0.96 beta2.1e-4 M0.018 273->ICE(L 3.34e5)/373->STEAM(L 2.256e6); ice 917^3 cp2108 k2.2 mu/ty INF eps0.97 beta5e-5 melt 273->WATER L on the MAX side; lava 330/2650/2650 solidus 1275->STONE latentMin 4.0e5; stone 2700^3 melt 1450->LAVA latentMax 4.0e5; air 1.0/1.2/1000 T_ref_gas 288; steam 0.06/0.6/1000 condense 373->WATER latentMin 2.256e6 T_ref_gas 373. Positional aggregate order matches the struct declaration order in all ~81 Material{} initializers across every test file (no transposition found), and no hardcoded 0.999 magic exists outside the single named constant GAS_CHI_MIN.
   - evidence: Struct order {cp,k,M,min,max,mu,default,ty,eps,beta,latentMin,latentMax}+quadruple+T_ref_gas == aggregate-prefix comment at real_lut.hpp:32-34 == every row's positional values; chi(air)=(1000-1.2)/(1000-1.0)=0.999800, chi(steam)=999.4/999.94=0.999460, both pinned in engine_b_lut_schema_test.cpp:203-204; grep for 0.999 hits only GAS_CHI_MIN (engine_b.hpp:125) and comments.
2. **[NOTE/low]** lut_schema_test header claim: 'the engine Material struct must carry EXACTLY the fixed schema' and 'Pins the SCHEMA + ... the v4 §1.2 REALISTIC data' (vs spec §1.2 exact data + §1.3 'INV-3 = struct/knob diff against §1.3 must be empty') @ `tests/engine_b_lut_schema_test.cpp:2-12,96-210`
   - The test's assertions do not fully pin what its header claims: several §1.2 cells have no assertion, so they could drift silently (and the positional-transposition guard the real-LUT pins provide is absent for exactly those cells). Unpinned: lava k=1.5, tau_y=0, beta=5e-5, T_ref_gas=0; stone molarMass=0.065, beta=2e-5, latentHeatMin=0, T_ref_gas=0; ice molarMass=0.018, T_ref_gas=0; air yieldStress=0; steam k=0.025, beta=0, yieldStress=0, latentHeatMax=0. Also nothing enforces struct EXACTNESS (no field-count/sizeof pin), and no INV-3 manifest diff exists anywhere yet — the 'EXACTLY' claim is presence-only. Data itself verified correct (see clean-LUT finding), so this is a missing regression guard, not a data mismatch.
   - evidence: Lava block (lines 143-156) asserts 12 of 17 fields — comment lists 'k 1.5 ... beta 5e-5' but no CLOSE(L.thermalConductivity,...) / CLOSE(L.thermalExpansion,...) exists; steam block (187-200) has no CLOSE(V.thermalConductivity, 0.025f,...). Only water's row is asserted on all 17 fields.
3. **[DRIFT-NEW/medium]** v4 §1.2: 'Gas classification: χ > 0.999 ... the 0.999 cutoff ... pinned here'; §2.2: incompressible means max == default (the only other class boundary the spec defines) @ `core/engine_b.hpp:132-133 (CHI_COMPR_EPS=1e-3, is_compressible), consumers at :1154, :1202, :1645`
   - A SECOND gas-classification cutoff coexists with the spec-pinned one: is_compressible (chi >= 1e-3) is used as the gas predicate at three RESOLVE sites — engine_b.hpp:1154 gates the gas-eviction pass with the literal comment 'must be a gas' but tests chi>=1e-3, :1202 excludes displacement donors as 'incompressible' via the same loose cutoff, :1645 exempts 'both compressible' pairs from no-penetration. On the real v4 LUT the two predicates coincide (no material has chi in (1e-3, 0.999]), but any Java-datapack-registered material with an intermediate band (legacy synthetic tests have exactly such rows: resolve_test water chi=0.53, step_test water chi=0.10) is routed through gas-only paths the spec's classification forbids. Not in the known queue; the only in-code label is 'Stage-4 may calibrate CHI_COMPR_EPS' — a retired pre-v4 stage name, not an owner task in the T4-T10 map.
   - evidence: engine_b.hpp:1154 'if (!is_compressible(mats.byIx(gsp))) continue; // must be a gas' vs spec §1.2 'Gas classification: χ > 0.999' and the adjacent correct predicate is_gas (engine_b.hpp:126, chi(m) > GAS_CHI_MIN). CHI_COMPR_EPS=1e-3 vs GAS_CHI_MIN=0.999 — a 3-orders-of-magnitude looser boundary with no spec/manifest entry.
4. **[PENDING-IMPL/low]** v4 §1.2 schema columns + §2.1 per-gas T_ref; handoff T10 'JNI grows LUT columns (ε/β/latentMin/Max/T_ref_gas — defaults stubbed at orge_jni.cpp TODO(T10))'; §1.3 'T_ref,global = 288 K (ρ_eff only)' @ `jni/orge_jni.cpp:23-90 (orgeRegisterMaterials), :64-73 (TODO(T10) stub), core/engine_b.hpp:28,201,832 (fallback)`
   - Verified the T10-pending JNI surface: the 12-array ABI is unchanged (cond, heatCap, molar, minMass, maxMass, visc, defMass, yield, minTemp, maxTemp, minTarget, maxTarget); the five v4 columns are stubbed to 0 under an accurate TODO(T10) comment whose stated contract (T_ref_gas==0 => fall back to Globals::T_ref) is exactly what p_eos_v4 and the gas-ladder implement. Consequences until T10, both sanctioned by the label: a JNI-registered steam would rest at the wrong gauge (+~24 kPa at 373 K — the precise failure §2.1 cites for a global 288 K reference), and Globals::T_ref doubles as the gas-EOS fallback although §1.3 scopes T_ref,global to 'ρ_eff only'. Inert in-game because the parent gitlink/.so is pinned pre-arc. Minor staleness: the function header still says 'the six physics arrays' / 'the 8 physics floats' (jni/orge_jni.cpp:15-19) — pre-v4 counts.
   - evidence: jni/orge_jni.cpp:69-73 'm.emissivity = 0.0f; m.thermalExpansion = 0.0f; m.latentHeatMin = 0.0f; m.latentHeatMax = 0.0f; m.T_ref_gas = 0.0f;' under 'TODO(T10): plumb v4 LUT columns through JNI'; engine_b.hpp:201 'const float Tref = (m.T_ref_gas > 0.0f) ? m.T_ref_gas : G.T_ref;' with G.T_ref=288 (engine_b.hpp:28) vs spec §2.1 'a global 288 K left resting steam pushing +24 kPa forever'.
5. **[PENDING-IMPL/medium]** law #7 'store EXTENSIVE, derive INTENSIVE ... enthalpy E ... T = h⁻¹(E/m)'; v4 §8.1 enthalpy curves with latent plateaus (T7) @ `tests/engine_b_real_lut.hpp:80-96 (grand_energy); consumers tests/engine_b_stage1_rest_test.cpp + tests/engine_b_resolve_accept_test.cpp (banked)`
   - grand_energy computes Σ m·(cp·T_curr + ½|u|²) — it reconstructs energy through the derived temperature on the implicit LINEAR-curve assumption E = m·cp·T instead of summing the persisted truth Chunk::E. Today this is consistent with persisted-E-as-truth (DECODE re-derives T_curr from E every tick at engine_b.hpp:1859/1882 and the double-precision round-trip is float-exact), so it is the pre-v4 mechanism awaiting T7, not live drift. At T7 (chain-anchored curves with latent plateaus) m·cp·T ≠ E on any plateau/mushy cell and the helper structurally breaks every conservation assertion riding it. It is NOT named in the handoff's T7 fold-in list (only bug3_accept's lava seeds are), so flagging here: T7 must re-base it on Σ C.E[i] + kinetic. Blast radius is currently small — two consumer tests, one of them banked/red.
   - evidence: real_lut.hpp:91-92 'float kin = 0.5f*(vx*vx+vy*vy+vz*vz); e += (double)m * (c * C.T_curr[i] + kin);' vs law #7's persisted E (Chunk::E, sim_engine.hpp:138) and v4 §8.1 'inverse T(E/m) has a plateau of width L' — on a plateau, m·cp·T_curr undercounts the paid latent energy.
6. **[NOTE/low]** v4 §1.2 'guard χ ≡ 0 when maxMass == minMass' (law #8 'χ ≡ 0 whenever maxMass == minMass') @ `core/engine_b.hpp:114-120 (chi)`
   - Guard exactness verified: maxMass == minMass returns exactly 0.0f, including the INF==INF band (IEEE infinity compares equal, so a hypothetical INF/INF row reads exact 0, never NaN); the schema test pins the finite case (stone, ice). Two unsanctioned-but-benign extensions beyond the spec formula: a defensive denom<=1e-9 branch (also catches inverted bands max<min via negative denom) and a [0,1] clamp. One unguarded edge: maxMass=+INF with FINITE minMass yields (INF−d)/(INF−min) = INF/INF = NaN, and std::clamp propagates NaN → chi=NaN, silently classifying as not-gas/not-compressible. No such row exists in any LUT and the Java builder defaults absent maxMass to the finite defaultMass, so it is unreachable today; recorded as an edge the guard does not make total.
   - evidence: engine_b.hpp:115 'if (m.maxMass == m.minMass) return 0.0f;' (INF==INF → true → exact 0) vs the NaN path: maxMass=INF, minMass finite ⇒ denom=INF (not <=1e-9) ⇒ c=INF/INF=NaN ⇒ std::clamp(NaN,0,1) returns NaN. Test pin engine_b_lut_schema_test.cpp:83-84 covers only the finite min==max case.
7. **[NOTE/low]** law #8 'Material LUT = a fixed schema (adding or removing a field is a law change)' — Java-side fields outside it; v4 §8.3 'world heat input (sun/biome) remains the parent project's pinned-source system, not this spec' @ `core/src/main/java/net/rainbowcreation/orge/material/Material.java:36-53 (parent repo)`
   - Note-only per brief: the Java Material record carries defaultTemperature and pinned, which are not in the engine's law-#8 schema and never cross orgeRegisterMaterials — they drive the Java-side Dirichlet pinned-source/biome-seeding system, which §8.3 explicitly leaves to the parent project (sanctioned split, no drift). The Java record also still lacks the five v4 columns (emissivity/thermalExpansion/latentHeatMin/Max/T_ref_gas) — that is T10's 'Java Material record/marshaller/save format' work, consistent with the stubbed JNI defaults.
   - evidence: Java record fields (thermalConductivity, heatCapacity, molarMass, defaultMass, defaultTemperature, viscosity, minMass, maxMass, yieldStress, minTemp, maxTemp, ..., pinned) vs engine schema sim_engine.hpp:48-75 — defaultTemperature/pinned absent engine-side by design; ε/β/latent/T_ref_gas absent Java-side pending T10.
8. **[PENDING-IMPL/low]** v4 §1.2 retired the pre-v4 data ('1000 K was ~300 K too cold', steam 'kills its χ=0/0') + MASTER-RULES 'Real material LUT only'; handoff T7 'sweep stale lava-3100 seeds in bug3_accept' @ `tests/engine_b_phase_relabel_test.cpp:41-58 (make_phase_lut)`
   - phase_relabel_test carries its own pre-v4 synthetic LUT under a comment claiming 'Real LUT + REAL phase quadruple (values verbatim from the materials/*.json datapack)': lava cp 1450 / band 400/3100/3100 / solidus 1000 K, steam degenerate 0.6/0.6/0.6 band (the exact χ=0/0 the spec killed), stone 840/2500. Self-consistent for the keep-E relabel it pins (the pre-v4 mechanism T7 replaces — expected), but the handoff's stale-seed sweep names ONLY bug3_accept; this second stale-lava-3100 + 'Real LUT' mislabel would survive the T7 sweep as written and could mislead a future agent reading the comment as authoritative data.
   - evidence: phase_relabel_test.cpp:54 'M.LAVA = ...{1450.f, 1.5f, 0.060f, 400.f, 3100.f, 100.f, 3100.f}' + ':57 M.STEAM = ...{2080.f, 0.025f, 0.001f, 0.6f, 0.6f, 1e-4f, 0.6f}' + lava.minTemp = 1000.f under the header 'Real LUT ... values verbatim' vs v4 §1.2 lava 330/2650/2650 solidus 1275 K and steam 0.06/0.6/1000. Handoff T7 line names only 'stale lava-3100 seeds in bug3_accept'. Same family: real_lut.hpp:26 comment still says 'Stage 4 calibrates K/gamma/...' — constants T2b deleted.

## Dimension: law-8-9 (LUT schema law + mass-never-vanishes/ledger/no_escape/EOS) — 22 findings

1. **[DRIFT-NEW/medium]** Law #9: "A `no_escape` detection seam fires on that case (empty body for now) for future handling; the default is do-nothing, never destroy." @ `ORGE-ENGINE @ c148f49 — repo-wide grep zero; the case is detected-and-absorbed silently at core/engine_b.hpp:1497-1513 (receiver-room clamp) and sim_engine.hpp:720 (relocate_chain dead-end)`
   - The law-mandated no_escape detection seam does not exist anywhere in the engine (grep for no_escape/noEscape hits only third-party hedley.hpp). The RESOLVE push-with-no-room path correctly no-ops, but no seam fires; the injection path's no-escape case destroys instead (separate finding). Law says the seam must exist even with an empty body — absent = drift.
   - evidence: grep -rn 'no_escape|noEscape|NO_ESCAPE' core/ jni/ tests/ → no engine hits. The two no-escape decision points (engine_b.hpp:1509 `if (want > avail) { mdot = ...*avail; }`; sim_engine.hpp:720 `if (!reached) return false;`) carry no hook/callback/marker of any kind.
2. **[DRIFT-NEW/high]** Law #9: "A pushed cell with no escape is a no-op (mass stays)... Mass is never deleted in either case... the default is do-nothing, never destroy." @ `core/sim_engine.hpp:834-837 (apply_injections seal-on-chain-failure) and :779-781 (relocate_chain mode-2 lighter-medium seal)`
   - The injection displacement path DELETES a pushed cell that finds no escape instead of no-opping: a movable incumbent whose relocation chain dead-ends is "sealed" (mass destroyed, booked to ledger.sealedLoss), and a displaced lighter medium (air) at a chain terminal with no 1-hop escape is likewise sealed. Law #9 sanctions ledgered source/sink only for the caller's place/break; engine-side destroy-on-no-escape directly contradicts the no-op clause. Adjacent to (but not covered by) the tracked find_chain_hop fold-in, which tracks the banned symbol/routing, not the destroy semantics.
   - evidence: Code: `if (!relocate_chain(...)) { if (incSpecies < ledger.sealedLoss.size()) ledger.sealedLoss[incSpecies] += incMass; } C->matIx[ci] = inj.species;` and `} else { if (lSpecies < (int)ledger.sealedLoss.size()) ledger.sealedLoss[lSpecies] += lMass; }` — vs law #9 "no escape is a no-op (mass stays)". The deletion IS reported to the caller via jLedgerOut (orge_jni.cpp:206), but the law's only sanctioned sink is the caller's own place/break.
3. **[DRIFT-NEW/high]** Law #9: "The only source/sink is the caller's place/break at the boundary, separately ledgered." @ `core/sim_engine.hpp:813-825 (apply_injections nothingToDisplace branch)`
   - Placement over an incumbent classified nothingToDisplace (!movable — e.g. a 2700 kg stone cell — OR incMass ≤ ADV_EPS_MASS=1e-4 kg OR void-labeled) overwrites the cell and destroys the incumbent's mass with NO ledger entry at all — not even sealedLoss. Contradicts the law's "separately ledgered" and the file's own header comment claiming "(mass lost — bounded, logged)" (sim_engine.hpp:472).
   - evidence: `const bool nothingToDisplace = !movable(incMat) || incMass <= orge::ADV_EPS_MASS || incSpecies == C->void_ix; if (nothingToDisplace) { C->matIx[ci]=inj.species; C->mass_kg[ci]=inj.mass; ...; ledger.injected[inj.species] += inj.mass; continue; }` — ledger.injected records the new mass; the incumbent's incMass (up to 2700 kg for solids) is dropped without any sealedLoss accumulation.
4. **[DRIFT-KNOWN/medium]** Law #9 / MASTER-RULES banned-symbol list ("Code-side bans unchanged: ... find_chain_hop") @ `core/sim_engine.hpp:607 (definition), :706 (call from relocate_chain)`
   - The banned symbol find_chain_hop is still present and live on the injection path (N-deep DFS push chain). Tracked: T4-continuation handoff fold-in list orders it routed through the standard displacement path or escalated.
   - evidence: handoff 2026-06-11-engine-b-v4-T4-continuation.md:98-99: "find_chain_hop (sim_engine.hpp:598, injection path) — MASTER-RULES-banned symbol; route injection escape through the standard displacement path or escalate." Definition now at sim_engine.hpp:607 at c148f49.
5. **[DRIFT-NEW/medium]** Law #9 "mass moves, never vanishes" + spec §1.3 frozen knob manifest (ε_mass = 1e-6 kg) + §6.4 (only m < ε_mass relabels, mass and E both ledgered) @ `core/orge_kernel.hpp:21 (ADV_EPS_MASS = 1e-4f); consumers sim_engine.hpp:83-85 (is_vacuum), :549, :557, :641, :660, :668, :817`
   - The injection path runs on a second, off-manifest void floor ADV_EPS_MASS = 1e-4 kg — 100× the spec's ε_mass = 1e-6. A cell holding up to 1e-4 kg is treated as vacuum and overwritten (vacuum-adopt at chain terminals and escape mode 0), deleting up to 1e-4 kg per event with no ledger — 100× the §6.4 "≤1e-6 kg" deletion bound, and the §1.3 manifest pins exactly one ε_mass.
   - evidence: `constexpr float ADV_EPS_MASS = 1e-4f; // kg; below this a cell is empty/air` vs spec §1.3 "ε_mass = 1e-6 kg" and §6.4 "Only m < ε_mass relabels to VACUUM with mass and E both ledgered (≤1e-6 kg)". Engine-B proper uses Globals::eps_mass = 1e-6f (engine_b.hpp:34); the injection path never ledgers the overwritten residue.
6. **[DRIFT-NEW/medium]** Law #8: "gas means χ > 0.999" + spec §1.3 frozen knob manifest (no such knob listed) @ `core/engine_b.hpp:132-133 (CHI_COMPR_EPS = 1e-3f / is_compressible), consumed at :1154 (§6.3 displacement "must be a gas" gate), :1202, :1645 (xspecies_noflux)`
   - A second, parallel gas-classification threshold exists: is_compressible (χ ≥ 1e-3) gates the §6.3 displacement pre-pass with the comment "must be a gas", while the law fixes the gas class at χ > 0.999 (is_gas, used by the EOS/relaxation/force). Coincident on every current LUT row, but a future row with χ ∈ [1e-3, 0.999] would be displacement-"gas" yet EOS-non-gas — a split-brain class the law forbids; CHI_COMPR_EPS is also a knob absent from the §1.3 frozen manifest (INV-3 diff).
   - evidence: `inline constexpr float CHI_COMPR_EPS = 1e-3f; inline bool is_compressible(const Material& m) { return chi(m) >= CHI_COMPR_EPS; }` and `:1154 if (!is_compressible(mats.byIx(gsp))) continue; // must be a gas` vs law #8 "gas means χ > 0.999" and `GAS_CHI_MIN = 0.999` (engine_b.hpp:125).
7. **[DRIFT-NEW/medium]** Law #9: in-domain movement is "conservative antisymmetric flux ... or a permutation swap" (and §7.1: payload = mass, matIx, E, momentum) @ `core/sim_engine.hpp:743-801 (relocate_chain shift) and :838-842 (apply_injections place)`
   - The injection chain shift bills itself "a conserving permutation" but permutes only (species, mass, T): persisted momentum (px/py/pz) is never moved — every relocated parcel lands with the previous occupant's momentum, and the injected cell inherits the displaced incumbent's momentum. E is also re-encoded from T via persist_cell_E rather than carried (equivalent under today's linear E=m·cp·T; breaks once T7's latent plateaus land — the same reconstruct-from-T class the handoff flags for the JNI). Grand mass exact; extensive momentum state misattributed.
   - evidence: relocate_chain writes only `Cj->matIx / mass_kg / T_curr` + persist_cell_E per link; no px/py/pz touched anywhere in the injection path, while the engine's own RESOLVE swap permutes full payload incl. momentum (engine_b.hpp:1703-1712: `C.px[i]=ux*C.mass_kg[i]; ...` from the partner's snapshot).
8. **[DRIFT-NEW/low]** Law #8: "molarMass's consumer is the gas EOS (#9)" @ `core/sim_engine.hpp:51 (struct comment), :557 and :661-662 (lighter-medium tests)`
   - molarMass is still consumed as a gravitational/buoyancy sort key on the injection path (escape mode-2 "strictly lighter movable" tests compare molarMass), and the Material struct comment still documents it as "gravitational sort key (higher sinks)". Inside the Engine-B pipeline proper its only consumers are the EOS sites (compliant).
   - evidence: `float molarMass; // kg/mol — gravitational sort key (higher sinks)` and `mE.molarMass < incMat.molarMass` / `mE.molarMass < predMat.molarMass` vs law #8's single-consumer sentence. Compliant sites: engine_b.hpp:202-203, 224-225, 828-830 (EOS only).
9. **[DRIFT-NEW/low]** Law #8 yieldStress threshold axis + spec §1.2 schema column "τ_y [Pa]" and §0/§7.3 gate "τ_y·A_face [N]" @ `core/sim_engine.hpp:56 (field comment "N") and core/engine_b.hpp:159 (swap_resistance)`
   - yieldStress is declared in Newtons ("// N — Bingham yield") and consumed directly as a force (`max(yieldStress_i, yieldStress_j)` with no ·A_face factor), while the ratified manifest declares τ_y in Pa converted by ·A_face. Numerically identical at the reference A = 1 m², but the declared unit and the gate formula diverge from the spec text (and the spec does not specify the max() pair-combination).
   - evidence: `float yieldStress = 0.0f; // N — Bingham yield (D7)` and `float yld = std::max(a.yieldStress, b.yieldStress);` vs spec §1.2 "τ_y [Pa]" and §7.3 "k_c·min(minMass)·g + τ_y·A".
10. **[DRIFT-KNOWN/high]** Law #9 receiver-room clamp / spec §6.2 "room = maxMass − m" + §6.1 INV-RR @ `core/engine_b.hpp:626-629 (recvRoom seeding)`
   - A vacuum receiver is seeded with recvRoom = +INF; its real ceiling is only the per-face donor-maxMass cap, so multiple donors converging on one vacuum cell in the same tick can jointly overfill it past the adopted species' maxMass. Known queue item (19); T4's INV-RR is the named kill target.
   - evidence: `r[i] = vacuum ? std::numeric_limits<float>::infinity() : std::max(0.0f, M.maxMass - s->mass[i]);` vs §6.1 R1.5 "ρ'_i = min(1, room_i / Σ inbound f·σ)" and INV-RR "m ≤ maxMass every tick". Handoff: "kills the vacuum recvRoom=INF overfill at engine_b.hpp:574-581" (now :626-629).
11. **[DRIFT-KNOWN/medium]** Spec §5.2 cohesion legalization (D ≤ min full-merge branch) + §6.4 sub-min residue @ `core/engine_b.hpp:1465-1491 (cohesion flux-gate sub-min branches)`
   - The §5.2 "else f ← D if R + D ≤ maxMass" full-merge branch is HELD: a sub-min LIQUID donor is frozen (want = 0) and a sub-min GAS donor trickles via the EOS ladder, deviating from the ratified D≤min rule (the 130→[125|880] and 250→[125|1000] examples are bit-exact on the live path; only D≤min deviates). Known queue item (5), explicitly awaiting user ratification or T5 receiver-side drain.
   - evidence: engine_b.hpp:1477-1491 in-code label: "v4 §5.2's full-merge branch ... was implemented here during T3 and REVERTED after probes ... A held droplet is the v4 §6.4 flagged-residue state; its proper consolidation is T5's RECEIVER-side drain pass". Handoff ratification-queue item 5.
12. **[DRIFT-KNOWN/high]** Law #9 "mass moves, never vanishes" + spec §4/§6.4: m < ε_mass relabel must ledger mass AND E to the boundary ledger @ `core/engine_b.hpp:1744-1751 (§D.4 vacuum guard in decrypt_world); mitigation at :1519`
   - F1: the DECODE vacuum guard relabels mNew < ε_mass cells to VACUUM zeroing both mass and E with no boundary ledger (no in-step boundary ledger exists in engine_b.hpp at all) — up to ~542 J/event of booked conduction E in the worst class. T3 gated sub-eps trickles at the source (:1519, donor keeps mass), reducing trigger frequency to ~0, but the class stays open. Known queue item (12)/handoff F1, owner T4.
   - evidence: `if (mNew < G.eps_mass) { C.matIx[i]=C.void_ix; C.mass_kg[i]=0.f; ... C.E[i]=0.0; continue; }` — vs spec §4 "void floor m < ε_mass ⇒ u⃗=0, mass AND E ledgered to the boundary ledger (law #9 has no de-minimis clause)".
13. **[DRIFT-KNOWN/medium]** Spec §6.1/§9 order-independence (INV-7) on the receiver-room composition (law #9 donor-budget+receiver-room) @ `core/engine_b.hpp:872-879 (gasSpent map), :1423-1427 (charge), :1522 (spend)`
   - gasSpent single-spend makes the gas EOS-ladder room total-correct but its allocation among competing donors is iteration-order-dependent — an INV-7 blocker. Known queue item (15); T4 absorbs the ladder/spent machinery into R1.5.
   - evidence: Per-face sequential `room = min(room, max(0, faceRoom − *gasLeftSlot)); ... *gasLeftSlot += fabs(mdot);` — first-processed donor wins the shared room. Handoff: "gasSpent order-dependence among competing donors (allocation, not total) — INV-7 blocker".
14. **[DRIFT-KNOWN/low]** Spec §6.2 receiver room single-budget (law #9 receiver-room clamp) @ `core/engine_b.hpp:1236 (eviction recvRoom decrement; no gasSpent charge)`
   - The §6.3 eviction decrements the recipient's recvRoom but never charges gasSpent, so same-tick gas flux into the same recipient can overshoot the EOS ladder by one tick (conserving). Known queue item (16).
   - evidence: `if (rri) (*rri)[k] = std::max(0.0f, kroom - evict);` with no corresponding `gasSpent[k] += evict` — vs the additive loop where every gas inflow is charged (:1522).
15. **[DRIFT-KNOWN/low]** Spec §6.1 donor-scale/receiver-scale parity (law #9 donor-budget) @ `core/engine_b.hpp:1097-1102 and :1403-1404 (ARG-PARITY markers)`
   - canDrain reads each face's FULL ladder room (gasSpent-blind), so it can over-promise the full-drain cohesion exemption when multiple gas donors converge; self-corrects next tick. Known queue item (17); T4's R1/R1.5 split is the named unifier.
   - evidence: In-code label at :1099: "NOTE: this site is gasSpent-BLIND by design — it reads each face's FULL room, so it can over-promise the drain exemption...". Handoff: "canDrain is gasSpent-blind (ARG-PARITY markers at :1078/:1380)".
16. **[DRIFT-KNOWN/low]** Spec §6.4: "0 < m < min: never deleted — DECODE flags it; next RESOLVE drains it into the strongest same-species neighbor" @ `tests/engine_b_cohesion_settled_test.cpp:132, :234 (maxStreak < 150 gates)`
   - The settled-state sub-min gate tolerates a 150-tick consecutive sub-min streak, far looser than §6.4's next-RESOLVE drain; no DECODE flag exists either. Known queue item (20), T4 sub-min tightening fold-in.
   - evidence: `CHECK(maxStreak < 150, "A: no water cell PERSISTS sub-min (v4 §6.4: transients drain/fill, never strand)");` vs §6.4 "next RESOLVE drains it".
17. **[DRIFT-KNOWN/low]** Spec §1.2 LUT bands (lava min/default/max = 330/2650/2650) + §6.2 no-overshoot @ `tests/engine_b_bug3_accept_test.cpp:127, :225 (lava seeded at 3100 kg)`
   - bug3_accept still seeds lava cells at 3100 kg — above the v4 maxMass 2650 (stale pre-v4 scene premise). Known queue item (21); T7 sweeps the stale seeds.
   - evidence: `setc(w,*C, 4,Y0+1,4, M.LAVA, 3100.f, T_LAVA);` vs §1.2 lava row "330 / 2650 / 2650".
18. **[PENDING-IMPL/medium]** Law #8 per-gas T_ref "kept in the engine LUT (not Java)" + law #9 EOS P₀ at its T_ref / spec §2.1 per-gas T_ref @ `jni/orge_jni.cpp:64-73 (TODO(T10) zero-stubs for ε/β/latentMin/Max/T_ref_gas); fallback at core/engine_b.hpp:201 + Globals::T_ref engine_b.hpp:28`
   - The JNI 12-array ABI cannot carry the five v4 LUT columns; JNI-registered gases get T_ref_gas=0 and p_eos_v4 falls back to Globals::T_ref=288 K — so an in-game steam row would rest at ≈ +24 kPa standing gauge, exactly the defect §2.1's per-gas T_ref was ratified to remove (correct-by-coincidence for air). Engine-native LUTs (real test LUT) are unaffected. Explicitly stubbed-and-owned by T10; not live in-game (parent gitlink pinned pre-arc).
   - evidence: `// TODO(T10): plumb v4 LUT columns through JNI — ε/β/latentHeatMin/latentHeatMax/T_ref_gas ... m.T_ref_gas = 0.0f;` + `const float Tref = (m.T_ref_gas > 0.0f) ? m.T_ref_gas : G.T_ref;` vs spec §2.1 "a global 288 K left resting steam pushing +24 kPa forever". Task map: T10 = JNI LUT columns (defaults stubbed — expected).
19. **[DRIFT-NEW/low]** Spec §6.2: "Transient overshoot beyond maxMass is forbidden — the single exemption is the §8.4 freeze relabel" @ `core/engine_b.hpp:1184 (`if (evict > kroom + 1e-3f) continue;`)`
   - The §6.3 eviction acceptance test tolerates the evicted gas exceeding the recipient's room by up to 1e-3 kg, permitting a ≤1e-3 kg transient past maxMass — outside the spec's single sanctioned exemption. Epsilon-scale; conserving.
   - evidence: `float kroom = rri ? (*rri)[k] : 0.0f; if (evict > kroom + 1e-3f) continue; // k can't hold it all -> treat as sealed` — the +1e-3f slack admits evict = kroom + 1e-3, i.e. recipient mass = maxMass + 1e-3 kg.
20. **[NOTE/low]** Law #8 fixed schema (full field list incl. emissivity, thermalExpansion, latentHeatMin/Max, phase quadruple, per-gas T_ref) + chi formula/guard + GAS_CHI_MIN + viscosity/yieldStress axes @ `core/sim_engine.hpp:48-75 (Material struct); tests/engine_b_real_lut.hpp:30-63; core/engine_b.hpp:114-126 (chi/is_gas)`
   - Compliance record: the Material struct carries exactly the law-#8 schema field-for-field (12 floats + minTemp→minTarget / maxTemp→maxTarget quadruple + T_ref_gas, engine-resident — no extra, none missing); the native real LUT matches the v4 §1.2 table numerically row-for-row (lava 330/2650/2650 solidus 1275, stone 2700 liquidus 1450, steam band 0.06/0.6/1000, ice row, ε/β/L columns, T_ref air 288 / steam 373); chi = (max−default)/(max−min) with the max==min ⇒ 0 guard; GAS_CHI_MIN = 0.999 with the pinned margins (air 0.99980, steam 0.99946); viscosity +INF = frozen (movable = isfinite); yieldStress present-but-deferred in the gate. p_eos_v4 (engine_b.hpp:197-205) implements law #9's EOS exactly — p_eos = (m/M)·R·T/V − P₀, P₀ = (m₀/M)·R·T_ref/V, R = 8.314 — and §2.2 incompressibles have no branch (p_eos ≡ 0; relief is P rising through the relaxation, as the law requires).
   - evidence: Struct fields enumerate exactly: heatCapacity, thermalConductivity, molarMass, minMass, maxMass, viscosity, defaultMass, yieldStress, emissivity, thermalExpansion, latentHeatMin, latentHeatMax, minTemp, maxTemp, minTarget, maxTarget, T_ref_gas. `if (m.maxMass == m.minMass) return 0.0f;` ; `inline constexpr float GAS_CHI_MIN = 0.999f;` ; real-LUT rows verified value-for-value against §1.2.
21. **[NOTE/low]** Law #8 chi guard: "the guard χ ≡ 0 whenever maxMass == minMass" @ `core/engine_b.hpp:117-119`
   - chi() carries two defensive extensions beyond the law's single stated guard: a near-degenerate/inverted-band guard (denom ≤ 1e-9 ⇒ 0) and a [0,1] clamp. Both are unreachable on legal §1.2 rows and benign (a sanctioned interpretation; both readings satisfied), recorded so a future band edit knows they exist.
   - evidence: `float denom = m.maxMass - m.minMass; if (denom <= 1e-9f) return 0.0f; float c = (m.maxMass - m.defaultMass) / denom; return std::clamp(c, 0.0f, 1.0f);` vs the law's lone `χ ≡ 0 when maxMass == minMass` guard.
22. **[DRIFT-KNOWN/medium]** Law #9 EOS gauge consumption (gas pushes back via its live EOS) — anchor floor + dual readings @ `core/engine_b.hpp:222-227 (gas_anchor_v42 1%-under-rest floor), :756 (neighbor-gas RAW read with sub-min→0 override), :382-383, :725-727, :1190`
   - The gas EOS gauge is consumed through gas_anchor_v42 (raw gauge floored at the 1%-under-rest line) on own-side anchors while neighbor-gas force faces read the RAW p_eos with a sub-min→0 override — two different gauge readings per gas cell, neither in the ratified §2.1 text. Known queue item (4), held for user ratification (v4.2 candidate).
   - evidence: `return std::max(a_raw, floorA);` with floorA = −0.01·(m₀/M)·R·T/V, vs `const float a_j = (mjm < Mf.minMass) ? 0.0f : ef->cells[fj].p_eos4;` — vs §2.1's single gauge definition. Handoff ratification-queue item 4: "gas_anchor_v42 floor (1%-under-rest) + dual raw/floored gauge readings per gas cell".

## Dimension: spec-6 (room/cross-species-permutation/sub-min constraints on live monolith) — 19 findings

1. **[PENDING-IMPL/low]** Spec §6.1 — RESOLVE as five GPU micro-passes R0→R1→R1.5→R2, 1-hop reads of snapshot/prior-pass buffers, normative in-step order @ `ORGE-ENGINE/core/engine_b.hpp:581-1593 (resolve_world monolith), :1877-1894 (step_world_b order)`
   - RESOLVE is still one monolithic function (swap-select → cohesion canDrain pre-pass → §6.3 displacement pre-pass → additive pair loop) instead of the R0/R1/R1.5/R2 kernels; the displacement pre-pass is sequentially order-dependent (ascending-y eviction, mutating shared accumulators/budgets in walk order). The macro order the monolith MUST honor IS honored: ENCODE → 2·N_relax red–black half-sweeps (relax_pressure_world) → movement → DECODE, with the §4 force reading the freshly relaxed P and all movement reading the post-ENCODE snapshot.
   - evidence: step_world_b: derive_world_T → snapshot_world → encrypt_world → relax_pressure_world → resolve_world → decrypt_world (matches §6.1 macro order); no swapChoice/swapMutual/f[6]/σ/ρ' buffers exist anywhere (T4 task #5 pending).
2. **[DRIFT-NEW/low]** Spec §6.2 — "Transient overshoot beyond maxMass is forbidden — the single exemption is the §8.4 freeze relabel" @ `ORGE-ENGINE/core/engine_b.hpp:1184 with :1236 (displacement-eviction room check)`
   - The §6.3 eviction accepts when the whole evicted gas exceeds the recipient's remaining room by up to 1e-3 kg, then floors the room at 0 — so the recipient cell k can persist at maxMass + ≤1e-3 kg. A maxMass-overshoot path that is neither the §8.4 freeze exemption nor the queued vacuum recvRoom=INF item (#19); not labeled in-code as debt.
   - evidence: Code: `if (evict > kroom + 1e-3f) continue;` then `AK.dm += evict;` and `(*rri)[k] = std::max(0.0f, kroom - evict);` — spec expression is room = maxMass − m with overshoot forbidden; the +1e-3f slop admits evict = kroom + 1e-3.
3. **[DRIFT-KNOWN/medium]** Spec §6.2 / §6.1 INV-RR — Σ inflows ≤ room structurally; room = maxMass − m @ `ORGE-ENGINE/core/engine_b.hpp:626-628 (recvRoom init: vacuum → infinity), :1509-1511 ("INF room stays INF")`
   - A VACUUM receiver gets recvRoom = +INF, so the single-spend receiver budget never binds: up to 6 donors can each push up to the per-face cap (donor maxMass) into one vacuum cell in a single tick — the multi-face overfill INV-RR is specified to kill.
   - evidence: Queue item #19 (T4 INV-RR target, handoff: "kills the vacuum recvRoom=INF overfill at engine_b.hpp:574-581"). Code: `r[i] = vacuum ? std::numeric_limits<float>::infinity() : std::max(0.0f, M.maxMass - s->mass[i]);`.
4. **[PENDING-IMPL/medium]** Spec §6.3 — "displacement-swap: liquid ... enters a gas/vacuum neighbor by swapping volumes — the gas payload relocates into the donor's vacated volume (a pure permutation, handled as an R0 face-swap candidate ... priority after vertical swaps)" @ `ORGE-ENGINE/core/engine_b.hpp:1119-1253 (§6.3 displacement pre-pass)`
   - The live displacement is a 3-cell relocation, not the spec's 2-cell volume permutation: the gas's whole mass is evicted UP into the cell above k (which must be vacuum/same-gas with room), the liquid donor sends only a min_mass quantum (its volume is never vacated), and the cell relabels via forceSpecies. It is also HORIZONTAL-only (`if (DY[ff]!=0) continue;`) where the spec's R0 priority includes vertical §6.3 displacement-swaps. Conserving per species and energy-closed, with the ratified driver P_donor − Φ_anchor (the anchor's floored flavor is queue #4); the geometric/payload shape awaits T4's R0 face-swap rebuild.
   - evidence: Code: `AG.dm -= evict; AK.dm += evict;` (k = cell above) + `AL.dm -= fq; AG.dm += fq;` with `fq = min(min_L, availL − min_L)` vs spec text "the gas payload relocates into the donor's vacated volume (a pure permutation)". Driver matches: `float drive = CL->P[li] - phi_g;`.
5. **[DRIFT-KNOWN/low]** Spec §6.2/§6.1 — receiver-scale composition (gas-axis R1.5 pre-implementation) @ `ORGE-ENGINE/core/engine_b.hpp:1236 (eviction decrements recvRoom) vs :1522 (gasSpent charged only in the additive loop)`
   - The §6.3 eviction decrements recvRoom[k] but never charges gasSpent[k], so same-tick gas flux into k still sees the full EOS-ladder room — a one-tick ladder overshoot (conserving, gauge-spike only).
   - evidence: Queue item #16 verbatim ("§6.3 eviction decrements recvRoom but never charges gasSpent"). Code: `(*rri)[k] = std::max(0.0f, kroom - evict);` with no `gasSpent` write in the pre-pass.
6. **[DRIFT-KNOWN/medium]** Spec §6.4 — "Only m < ε_mass relabels to VACUUM with mass and E both ledgered (≤1e-6 kg, ~1 J)" @ `ORGE-ENGINE/core/engine_b.hpp:1744-1751 (§D.4 vacuum guard)`
   - The DECODE vacuum guard zeroes the residual mass (≤ε) AND the cell's E (including same-tick booked conduction dE) with no boundary ledger — there is no boundary ledger structure in the engine at all besides the injection LedgerAccum. F1 class; T3 reduced trigger frequency to ~0 (source gate at :1519) but the class stays open, owner T4.
   - evidence: Queue item #12 (F1, ~542 J/event class). Code: `C.mass_kg[i] = 0.f; ... C.E[i]=0.0;` with no ledger write; spec requires "mass AND E ledgered to the boundary ledger".
7. **[NOTE/low]** Spec §6.4 — "Decomp §3's m < max(ε, min_mass) → VACUUM rule is dead" @ `ORGE-ENGINE/core/engine_b.hpp:1744 (only VACUUM-relabel site), grep over core/ + jni/`
   - Verified COMPLIANT: the dead mass-deleting sub-min→VACUUM rule is truly dead. The only relabel-to-VACUUM in the live pipeline is the ε-only §D.4 guard; sub-min masses are otherwise held by the cohesion flux-gate (forbidden flux stays in the donor) or trickled (gas), never destroyed.
   - evidence: grep for `max(.*eps.*min`/`minMass.*VACUUM` over core/ and jni/ returns zero hits; the decode guard tests only `mNew < G.eps_mass`.
8. **[DRIFT-KNOWN/medium]** Spec §6.4 — "0 < m < min: never deleted — DECODE flags it; next RESOLVE drains it into the strongest same-species neighbor" (+ §5.2 D≤min branch) @ `ORGE-ENGINE/core/engine_b.hpp:1465-1495 (cohesion flux-gate sub-min branches)`
   - Sub-min LIQUID donors are HELD (want=0; v4 §5.2's full-merge branch was implemented in T3 and reverted after probes destabilized marginal sheets); sub-min GAS donors trickle through the EOS-ladder room. There is no DECODE flag and no receiver-side "strongest same-species neighbor" drain — that is the banked T5 stage3_cleanup work. In-code comment labels the held droplet as the §6.4 flagged-residue state with owner T5.
   - evidence: Queue item #5 ("§5.2 D≤min full-merge branch HELD (liquid want=0, gas trickles)"); code comment: "its proper consolidation is T5's RECEIVER-side drain pass (the BANKED stage3_cleanup gap), not a donor-side dump".
9. **[DRIFT-KNOWN/low]** Spec §6.4 — "next RESOLVE drains" (one-tick consolidation) @ `ORGE-ENGINE/tests/engine_b_cohesion_settled_test.cpp:132 and :234`
   - The settled-state gate tolerates a sub-min water cell persisting up to 149 ticks (`maxStreak < 150`) — far looser than §6.4's next-RESOLVE drain semantics.
   - evidence: Queue item #20. Test: `CHECK(maxStreak < 150, "A: no water cell PERSISTS sub-min (v4 §6.4: transients drain/fill, never strand)");`.
10. **[DRIFT-NEW/medium]** Spec §6.2 / §8.4 — "Transient overshoot beyond maxMass is forbidden — the single exemption is the §8.4 freeze relabel (water→ice, 1000 > 917)" @ `ORGE-ENGINE/core/engine_b.hpp:1841-1855 (DECODE keep-mass phase relabel) + tests/engine_b_real_lut.hpp:42-43,55 (stone 2700, maxTemp 1450 → target LAVA, lava maxMass 2650)`
   - The generic keep-mass relabel combined with the v4 LUT creates a SECOND unsanctioned overshoot transient: a 2700 kg stone cell melting (T > 1450 K) relabels to LAVA at 2700 kg > lava maxMass 2650 — outside the spec's single water→ice exemption. §8.4 discusses lava→stone (98.1% under-full) but is silent on stone→lava melt; the overshoot class survives a faithful T7 implementation because the ratified text has no melt clause, and neither the queue nor an in-code label tracks it.
   - evidence: Relabel keeps mNew unchanged (`C.mass_kg[i] = mNew;` after `sNew = tgt`); LUT: STONE min/default/max 2700/2700/2700 with `stone.maxTemp = 1450; stone.maxTarget = M.LAVA;` while LAVA maxMass = 2650 — 50 kg standing overshoot, vs spec "the single exemption is the §8.4 freeze relabel".
11. **[DRIFT-NEW/medium]** Law #9 / MASTER-RULES conservation — "place/break go to the boundary ledger — nothing is silently dropped" (injection as the sanctioned boundary sink, ledgered) @ `ORGE-ENGINE/core/sim_engine.hpp:816-825 (apply_injections nothingToDisplace branch)`
   - Placing onto an IMMOVABLE occupied incumbent (e.g. stone, 2700 kg: `!movable(incMat)` with real mass) overwrites the cell and ledgers only the injected mass — the destroyed incumbent's mass is NOT added to sealedLoss, unlike the movable-incumbent no-escape path at :836 which ledgers. An unledgered mass sink at the boundary; not in the queue and not labeled (the queued find_chain_hop item covers escape routing, not this branch).
   - evidence: `nothingToDisplace = !movable(incMat) || incMass <= ADV_EPS_MASS || incSpecies == void` → `C->mass_kg[ci] = inj.mass; ... ledger.injected[inj.species] += inj.mass;` with no `ledger.sealedLoss[incSpecies] += incMass;` — contrast the displacement-failure path: `if (incSpecies < ledger.sealedLoss.size()) ledger.sealedLoss[incSpecies] += incMass;`.
12. **[DRIFT-NEW/medium]** Law #7 store-extensive (momentum) / Spec §7.1-§6.3 full-payload relocation — applied to the injection displacement path @ `ORGE-ENGINE/core/sim_engine.hpp:743-747, 768-785, 791-797 (relocate_chain), :820-823/:838-841 (apply_injections direct writes)`
   - The injection displacement chain moves only (matIx, mass_kg, T_curr) + re-encoded E; px/py/pz are never moved, zeroed, or permuted — relocated parcels leave their momentum behind and every shifted/overwritten cell pairs its OLD momentum with NEW mass (the old-p/new-m velocity-ghost class law #7 forbids; e.g. a vacated 1000 kg water cell's momentum now divides by 1.2 kg of pushed-in air). Bounded downstream by DECODE's CFL/no-penetration/vel_damp clamps, but it injects spurious velocity and vel_damp heat at every displacing placement. The queue's find_chain_hop item targets the banned symbol/routing; this payload gap is stated nowhere.
   - evidence: relocate_chain shift loop writes exactly three fields + E: `Cj->matIx[...] = snap[j-1].sp; Cj->mass_kg[...] = snap[j-1].mass; Cj->T_curr[...] = snap[j-1].T; persist_cell_E(...)` — no px/py/pz anywhere in the injection path; next snapshot derives `vx = C.px[i] * inv` with the new mass.
13. **[DRIFT-KNOWN/medium]** Spec §6.3 — "Cross-species movement = swap/displacement ONLY" vs the injection escape chain; MASTER-RULES banned symbol @ `ORGE-ENGINE/core/sim_engine.hpp:607-674 (find_chain_hop), :520-564 (find_injection_escape mode 2), :756-781 (lighter-medium seal)`
   - The injection escape path is a pre-Engine-B cross-species mover outside the sanctioned swap/§6.3-displacement mechanisms: an N-deep heterogeneous push-through chain plus a "lighter terminal medium" mode that consumes (seals, sealedLoss-ledgered) air when it has nowhere to go — vs law #9's no-escape = no-op + empty no_escape seam. Tracked: the handoff's T4 fold-in list names find_chain_hop a MASTER-RULES-banned symbol with the remedy "route injection escape through the standard displacement path or escalate".
   - evidence: Handoff fold-in: "find_chain_hop (sim_engine.hpp:598, injection path) — MASTER-RULES-banned symbol". Code mode 2: lighter medium relocated one hop `else { ledger.sealedLoss[lSpecies] += lMass; }` — a ledgered deletion, where law #9 says "the default is do-nothing, never destroy" for engine-internal pushes.
14. **[NOTE/low]** Spec §6.3 — "No partial cross-species flux exists" (live RESOLVE) @ `ORGE-ENGINE/core/engine_b.hpp:1341 (crossOccluded), :1368 (advective gate), :1392-1395 (vacuum claim block), :899-948 (vacClaim pre-pass)`
   - Verified COMPLIANT in the live monolith: the additive channel is hard-gated to same-species or into-vacuum (`crossOccluded = (mi != mj) && !si_vac && !sj_vac`), a vacuum receiver accepts exactly ONE claimed species per tick (foreign donors zeroed at the donor), and the only cross-species movers are the §7-style whole-cell swap and the §6.3 displacement pre-pass. No partial cross-species flux path found in RESOLVE.
   - evidence: `if (!wall && !crossOccluded && std::fabs(W) > 1e-12f)` gates all advective flux; `if (vc && (*vc)[ri] != NO_CLAIM && (*vc)[ri] != donorSp) mdot = 0.0f;` blocks mixed-species vacuum refill at the donor.
15. **[NOTE/low]** Spec §6.1 R1/R2 — whole-cell flux-XOR-swap ("a cell with swapMutual zeroes ALL six flux intents"; "a face fluxes iff neither endpoint is swapMutual") @ `ORGE-ENGINE/core/engine_b.hpp:1275, 1317-1318 (swap exclusion), :1278, 1321-1322 (displaced exclusion), :1554-1588 (conduction inside the excluded face loop)`
   - Verified: the flux-XOR-swap is honored WHOLE-CELL by the current selection — a swap-selected or §6.3-displaced cell takes no additive flux on any of its 6 faces, checked from both endpoints, and decode's swap path is a pure snapshot permutation. Side effect worth recording: because the `continue`s precede the conduction block, CONDUCTION is also skipped on every face touching a swapping/displaced cell that tick — symmetric (no one-sided booking, grand-E exact), but spec §6.1 R2 has conduction accumulate at commit and only ties the XOR to mass flux; both readings are defensible, one-tick heat pause per swap.
   - evidence: `if (ai->swap[i].idx >= 0) continue;` / `if (aj_sw && aj_sw->swap[j].idx >= 0) continue;` skip the whole face body including the conduction `{ ... Ai.dE += -Q; Aj.dE += +Q; }` block; decode swap path reads only `sp->...[j]` snapshot fields + own dampHeat (booked dE discarded, but nothing can book it).
16. **[PENDING-IMPL/low]** Spec §6.1 R0 — deterministic priority "(1) vertical down, else up ..., (2) strongest Δρ_eff·g·V − R_pair, (3) lower coordinate-parity" + §9 INV-7 order-independence @ `ORGE-ENGINE/core/engine_b.hpp:906-1020 (swap candidate gather + greedy selection)`
   - Swap selection is a global greedy sort by force-difference only: candidates are gathered in unordered_map iteration order and `std::sort` has no tie-break, so equal-priority pairs resolve by hash-map order (not coordinate-parity) — not coordinate-canonical and an INV-7 hazard. Also vertical-only (lateral §6.3 candidates live in the separate pre-pass) and gated by the dPE<−1e-3 co-gate (T8-expected). The R0 deterministic priority scheme is T4's restructure.
   - evidence: `std::sort(cands.begin(), cands.end(), [](a,b){ return a.prio > b.prio; });` with `cands` filled from `for (const auto& kv : world.chunks)` over an unordered_map — no parity/coordinate tie-break; INV-7 is a named T4 invariant.
17. **[DRIFT-KNOWN/low]** Spec §6.2 — "room = maxMass − m (gas: large band)" vs the gas EOS-ladder receiver room @ `ORGE-ENGINE/core/engine_b.hpp:795-879 (gas_ladder_room + gasSpent), constants at :821-823 (LADDER_BETA=0.5, LADDER_REST_DEADBAND=0.99)`
   - For gas donors the receiver room is additionally throttled to the hydrostatic gauge ladder (β-under-relaxed, with a 99%-of-rest free-compaction floor and per-receiver gasSpent single-spend) — a §6.2 extension labeled in-code as "v4.2 §6.2" candidate, conservative (only tightens room; forbidden flux stays in the donor) but carrying two constants absent from the frozen §1.3 knob manifest. Tracked: the handoff queues the ladder/spent machinery as a labeled pre-implementation of R1.5 on the gas axis to be absorbed by T4 (queue #15/#16/#17 family).
   - evidence: Handoff: "absorb the gas-axis ladder/spent machinery into R1.5 properly (it pre-implements R1.5 on the gas axis, labeled)"; code: `room = std::min(room, std::max(0.0f, faceRoom - *gasLeftSlot));` with `constexpr float LADDER_BETA = 0.5f; constexpr float LADDER_REST_DEADBAND = 0.99f;` — neither symbol in spec §1.3.
18. **[NOTE/low]** Spec §6.2 mass bands / §1.2 gas rest state — seeding helper semantics @ `ORGE-ENGINE/core/sim_engine.hpp:850-870 (fill_section_with: "a full cell holds the material's max_mass")`
   - fill_section_with seeds every cell at maxMass — a premise from the pre-v4 model ("defaultMass was dropped"). With T1's v4 gas bands (air 1.0/1.2/1000, steam 0.06/0.6/1000) filling a section with a gas now seeds 1000 kg/cell ≈ 69 MPa gauge instead of rest density. Callers today are tests/viz only (correctness/stress tests with synthetic LUTs), so no live-path impact, but any future real-LUT use of the helper for gas seeds a pathological scene.
   - evidence: `const float mdef = mats.byIx(mat_ix).maxMass; ... C.mass_kg[i] = (mat_ix == C.void_ix) ? 0.0f : mdef;` vs v4 §1.2 air defaultMass 1.2 / maxMass 1000; callers grep: tests/correctness_test.cpp, tests/stress_test.cpp, viz/main.cpp only.
19. **[PENDING-IMPL/low]** Spec §6.1 normative order + snapshot definition ("'Snapshot' = the post-ENCODE state") @ `ORGE-ENGINE/core/engine_b.hpp:1877-1894 (step_world_b) with sim_engine.hpp:437-459 (snapshot_world taken PRE-ENCODE)`
   - The monolith's "snapshot" is taken BEFORE ENCODE (pre-gravity velocities; ENCODE's u_g/w live in the separate WorldEncrypt), whereas §6.1 defines the snapshot as the post-ENCODE state with gravity+external already in momentum. The current split (snapshot + enc consulted together) reproduces the same information, and the known queue already tracks the two places where the distinction leaks physics (gas flux direction w_eff vs carried pre-force u_g — queue #14; κ·divU reading persisted velocity — queue #10). Recording the structural mismatch itself: it dissolves when T4 rebuilds the passes on one post-ENCODE buffer set.
   - evidence: `WorldSnapshot snap = snapshot_world(world);` precedes `encrypt_world(...)`; resolve passes read `snap` (pre-step v, T, m) plus `enc` (u_g, w, p_eos4) — vs §6.1: "'Snapshot' = the post-ENCODE state (gravity + external already in momentum; T, p_eos cached)".
