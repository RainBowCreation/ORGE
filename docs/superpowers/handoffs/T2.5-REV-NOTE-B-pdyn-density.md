# 📌 T2.5-REV — ADDENDUM B (read mid-task): SCENE 1 is a `p_dyn` bug, NOT a spec gap to escalate

You are mid-REV, on **phase 2 (§8.5 lateral)**. **Phase 2 is correct — keep it.** This note only changes how
phase 3 (SCENE 1) is handled. It SUPERSEDES the original T2.5-REV §3 "escalate SCENE 1 as a spec gap."

## The finding (user-confirmed against the spec + code)
SCENE 1 (two same-height same-species cells, 1000|500 → 750|750) is **already covered by the spec** and is an
**implementation bug in `p_dyn`**, not a missing driver. Do **not** escalate it as a spec decision, and do
**not** re-add `p_surf`.

Spec §C.5 / DEC-6 / §J.5: a resting floor cell's **weight** pushes down → floor blocks → reflects into
pressure → transmits sideways next tick. Weight is **m·g — mass/density-dependent**. So a 1000 cell MUST build
higher `p` than a 500 cell at the same height ⇒ horizontal gradient ⇒ the **existing same-species flux** levels
them. One field, no new term.

## Why it currently fails (two coupled defects in T2's artificial-compressibility)
- `engine_b.hpp:~193` — gravity is applied as **acceleration**: `e.ugy = uy - dt*G.g`. Mass cancels (a=g), so
  1000 and 500 get the **same** downward velocity.
- `Globals.p_ac_scale = 2000` (a **constant**) sits where the physics needs the cell's **density ρ** — the
  `dp/dt = -c²ρ·divU` coupling flattened `ρ` to a constant.
⇒ the blocked-velocity divergence is mass-independent ⇒ both cells build **identical `p`** ⇒ no gradient.
(`p_surf = c_head·ρ·g·φ·dx` was literally the cell-averaged hydrostatic head ρg·h — the *correct value*, added
as a parallel 4th field to paper over this. Fix the source instead.)

## What to do for phase 3
- Make the **emergent `p_dyn` density/fill-dependent** so a resting partial/under-filled liquid cell carries
  hydrostatic head ∝ `ρ·g·h` (`ρ = mass/V`), faithful to §C.5/§J.5 — i.e. the pressure response must scale
  with the cell's actual density, not the constant `p_ac_scale`, and/or the gravity reflection must carry
  **weight (m·g)** not bare acceleration. Pick the minimal change that restores the mass-weighting; document it
  citing §C.5/§J.5.
- Acceptance unchanged: **SCENE 1 → 750|750 via the ONE pressure field + existing same-species flux** (no
  `p_surf`, no new Globals field beyond making the existing coupling ρ-aware). Conserve exact, no sub-min.
- This same corrected `p_dyn` is also the **driver** for your phase-2 §8.5 water→air lateral move (column base
  high `p`, air low `p`); §8.5 only transports the cross-species mass, it does not need its own pressure term.
- **Guard rail:** if making `p_dyn` density-dependent re-introduces the old runaway / per-species leak (the
  thing the T2 free-surface fix originally cured), **STOP and escalate** with the trace — do NOT re-patch it
  with a separate field. The fix must keep the walled-column `maxP = ρg·depth` exact and settle.

## Still binding
00-MASTER-RULES (spec>code, conserve exact, escalate-don't-invent), original T2.5-REV (revert `p_surf`,
§8.5 phase 2, re-validate YG_B), three reviews incl. adversarial conservation before push.
