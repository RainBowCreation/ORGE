> # 🛑 LOAD FIRST — do not skip (the controller may forget to say it)
> **Before reading anything else, read these two files IN FULL and obey them as the law:**
> 1. `/tmp/orge-engine-b-resolve-handoff/00-MASTER-RULES.md` — spec>code, the **DO-NOT-TRUST** list,
>    🧨 **STALE-TESTS triage**, conservation non-negotiables, TDD/subagent process, report format.
> 2. **This file** (your task brief).
>
> Then read **ONLY** the authoritative specs the master rules name (canonical pipeline + decomposition §8
> + unified-formula §B.1/§C/§J.5). **Do NOT learn the model from the C++ or code comments — the code is
> drifted; the spec is truth.** Skipping the master rules ⇒ you WILL rebuild the stale design. If `/tmp` is
> empty, ask the controller for the handoff set.

> # ⛔ SERIALIZE — do NOT start until T4 is committed.
> T4 (swap = pair resistance) is editing `core/engine_b.hpp` RIGHT NOW. You edit the SAME file. Starting in
> parallel = stomp/conflict. The controller will release you only after T4 lands + passes its three reviews.

---

# T2.5 — Horizontal flow is DEAD in-game (the T2 leveling false-positive) · read 00-MASTER-RULES.md first

## Why this task exists (the in-game audit finding)
T2 shipped "leveling GREEN" headless, but the **in-game re-audit FAILED A1/A4: liquid does not flow
horizontally at all.** The headless GREEN test (`engine_b_stage2_leveling_test::test_connected_leveling`)
only exercises a **vertical U-shape** (tall arm drops, short arm rises through a base channel). It NEVER
tests same-height horizontal equalization or a column slumping sideways. So it went green while the
in-game-relevant horizontal flow was never implemented. **Vertical fall/stack + swap work; horizontal is the
gap.** This is the exact drift the master rules warn about: a test authored to match the one mechanism that
works.

## Confirmed headless repro (run this FIRST — it reproduces the in-game failure cold)
Recreate as `tests/engine_b_horizontal_flow_test.cpp` (these are your RED gates; real LUT, no synthetic
Material):

```cpp
// SCENE 1: two adjacent water cells, SAME height, masses 1000 & 500. Expect equalize -> ~750/750.
//   floor stone y=20 x=2..11; water(1000) at (5,21); water(500) at (6,21); air above. dt=0.5, ~400 steps.
//   OBSERVED NOW: stays 1000 | 500 forever.
// SCENE 2: dam-break. 4-tall water column (all 1000) at x=2, flat air floor x=3..10. Expect lateral slump.
//   floor stone y=20 x=2..10; water(1000) at (2, y=21..24); air elsewhere. dt=0.5, ~400 steps.
//   OBSERVED NOW: water spreads to 1/9 columns (column just stands).
```
(The controller already ran this against the current working tree and confirmed both fail; mass is conserved
— no fabrication — so the bug is "no horizontal drive," not a leak.)

## 🔬 STEP 0 — DIAGNOSE BEFORE YOU FIX (mandatory; report before writing any production code)
There are **two candidate failures with different owners**. Classify each scene against the spec:

- **SCENE 2 (column base p>0 beside low-p air)** is **spec-covered**: §8.4 says the vertical-accumulated `p`
  at the column base must transmit **horizontally** (Pascal, no gravity added) toward the lower-`p` neighbour,
  and §8.5 says the **lowest-`p` reachable cell** (the air) is the one consumed/displaced. If this scene
  fails, it is an **IMPLEMENTATION bug** — the horizontal pressure-difference flux (and/or the water→air
  lowest-`p` displacement) is not firing. **You fix this.**

- **SCENE 1 (two same-height, under-filled cells)** may be a **SPEC GAP**: per §B.1 (table row ②) an
  under-filled liquid cell sits at **`p = 0` (free surface)**, and a rest-density cell is **also `p = 0`**.
  Two `p=0` cells ⇒ §8.4 "uniform pressure ⇒ faces cancel ⇒ no flow." So the **current spec produces no
  driver** for same-height free-surface equalization. The physical answer the user wants (1000|500 → 750|750,
  because the half-full cell's free surface is lower) requires a **free-surface / fill-fraction head term**
  that the decomposition spec does **not** currently define. Check §J.5 (hydrostatic head / overburden
  `O/O_ss`) — if it defines a within-cell free-surface head that drives same-layer equalization, then SCENE 1
  is implementable; **if it does not, STOP and escalate** (see STRICT DON'T). **Do not invent it.**

Report STEP 0 as: "SCENE2 = impl bug, here's where the horizontal flux dies"; "SCENE1 = covered by §J.5
head, will implement" **OR** "SCENE1 = spec gap, escalating — §8.4+§B.1 give both cells p=0, no driver."

## STRICT DO
- Recreate the two scenes as a RED test file on the **real LUT** (9 refs, no synthetic Material). Run, watch
  it fail, commit the RED test first (TDD).
- Fix **SCENE 2** against the spec: make the column-base `p>0` transmit horizontally (§8.4 Pascal, no sideways
  gravity) and displace the **lowest-`p` reachable** neighbour (§8.5). Keep the **yield gate** working
  sideways (a stone wall between two pools must still block — do not regress the `engine_b_yield_gate_test`
  lateral-wall case). Same antisymmetric snapshot pass — no new sweep/sub-cycle.
- Conserve EXACTLY (grand + per-species) every step; no cell left at `0 < mass < min` (§8.7 gate still holds).
- For **SCENE 1**: only implement if STEP 0 found a spec-defined driver (§J.5). Otherwise escalate.
- Keep the existing vertical `test_connected_leveling` GREEN — your change must not break vertical head flow.
- **RE-VALIDATE the T3 yield gate `YG_B` (wall-blocks-sideways).** It currently passes **vacuously**: with
  horizontal flow dead, the two pools can't level through the stone wall *regardless* of the wall, so the
  assertion can't tell "wall bears lateral load" from "nothing ever flows sideways." Once your SCENE 2 fix
  makes lateral flow real, confirm YG_B still holds for the RIGHT reason — i.e. **remove the wall and prove
  the same two pools NOW level** (lateral flow alive), then **restore the wall and prove they DON'T** (the
  wall genuinely bears the load). If the wall fails to block once lateral flow is real, that's a yield-gate
  bug to fix here. (T3's `YG_C` "transmits" is fine but only proves the *vertical* channel — it does not
  cover lateral.)

## STRICT DON'T
- ❌ Do NOT invent a free-surface / fill-fraction / "Minecraft-style spread 7 blocks" term to force SCENE 1
  green. If the spec doesn't define the same-height driver, that's a **design decision for the user** —
  escalate with the precise gap (both cells `p=0` ⇒ §8.4 cancels). Inventing it = exactly the drift the
  master rules forbid.
- ❌ Do NOT touch the swap code region (`engine_b.hpp` ~§ cross-species swap, ~lines 400-420) — that's T4's.
  You own the **same-species horizontal flux / pressure-difference** path only.
- ❌ Do NOT start before T4 is committed (same-file conflict).
- ❌ Do NOT "fix" by adding a global constant, an EOS compression band, or scaling gravity (off-spec).
- ❌ Do NOT loosen conservation tolerances to make a scene pass — a drift in these scenes is a real bug.

## Acceptance
- RED test committed first; SCENE 2 GREEN (column slumps sideways, conserved, yield wall still blocks).
- SCENE 1 either GREEN (via a spec-defined §J.5 head) **or** a written escalation to the controller naming the
  spec gap — controller's call, not a silent hack.
- Vertical `test_connected_leveling` still GREEN; cheap tier green; conservation exact.
- Report: STEP-0 classification, the diff, which scenes are green, and any escalation. This is a foundation
  fix — in-game re-audit (with T4's swap) is the gate before T5+.
```
