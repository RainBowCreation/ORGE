> # 🛑 LOAD FIRST — do not skip
> Read IN FULL and obey as law, in this order:
> 1. `docs/superpowers/handoffs/00-MASTER-RULES.md` — spec>code, DO-NOT-TRUST list, 🧨 STALE-TESTS triage,
>    conservation non-negotiables, TDD/subagent process, report format.
> 2. `docs/superpowers/handoffs/T2.5-horizontal-flow.md` — the ORIGINAL brief (its STEP-0 + STRICT-DON'T
>    still bind).
> 3. **This file** — the CORRECTION. It overrides the original where they differ.
> Learn the model from the **spec**, never the C++/comments. Skipping ⇒ you rebuild the drift.

> # ⚠ WHY THIS CORRECTION EXISTS — the first T2.5 attempt DRIFTED. Read this before touching code.
> The first attempt (engine `a536d69` + `814869b`, already pushed) **did the opposite of the brief**:
> - It **INVENTED a `p_surf` free-surface head** (`p_surf = c_head·ρ·g·φ·dx`, `c_head`, `c_head_mob`, a
>   self-declared "DEC-6 amendment") to force **SCENE 1** green. The brief's **STRICT-DON'T #1** said in
>   plain words: *do NOT invent a free-surface / fill-fraction head — if the spec gives no driver, ESCALATE.*
>   It did not escalate. **That term is unratified design and must come OUT.**
> - It **did NOT fix SCENE 2** (the dam-break / column-slump-into-air it WAS told to fix); it re-labeled it
>   "T5-deferred." So **in-game leveling is still broken** — a poured pool of full cells still won't spread.
> - It **did NOT re-validate `YG_B`** (still vacuous).
> The user reviewed this and chose **Option B: revert `p_surf`, unify on the spec-defined lateral mover.**

---

# T2.5-REV — Revert the invented head; build the spec-defined lateral flow · read 00-MASTER-RULES.md first

**One sentence:** SCENE 1 and SCENE 2 are the same physics (water seeking its level **sideways**); replace the
bolted-on 4th driver with the **one spec-defined mechanism** — §8.5 lateral lowest-`p` displacement — and where
the spec genuinely has no driver, **STOP and escalate; do not re-invent.**

## STRICT DO (in order)

### 1. REVERT the invented free-surface head — completely.
- Remove everything `a536d69` + `814869b` added to **production**: the `p_surf` field, `c_head`, `c_head_mob`
  Globals, the ENCRYPT `p_surf` computation, and the RESOLVE `W_head` face-drive. `core/engine_b.hpp` must
  return to its `8701e36` (post-T4) behaviour for the pressure/head path. Verify: `git diff 8701e36 -- core/engine_b.hpp`
  shows **no `p_surf`/`c_head` residue**.
- Demote the SCENE-1 test back out of CHEAP (it goes RED again — that is correct, see §3). Keep
  `tests/engine_b_dambreak_test.cpp` (SCENE 2) — it becomes your primary GREEN target.
- Re-confirm `eos`, `swap_resistance`, `yield_gate`, `stage2_leveling`(connected/flat-pool), `conduction`,
  `conservation_levels` still pass after the revert (the head was supposed to be isolated — prove it).

### 2. BUILD the spec-defined piece: §8.5 lateral lowest-`p` displacement (this is SCENE 2 + the real in-game case).
- A FULL liquid cell whose pressure exceeds a **horizontally** reachable lower-`p` neighbour must drive mass
  sideways, **consuming the lowest-`p` reachable cell** (§8.5 — usually the adjacent air; air is *not* gated by
  `max_mass`). This is the §8.4 horizontal difference (`p_left − p_right`, no sideways gravity) made to move
  **cross-species mass** sideways, the same way vertical fall already consumes the cell below.
- Same antisymmetric snapshot pass; GPU-safe gather; flux-XOR-swap (a face fluxes OR swaps, never both — do
  not double-move with T4's vertical swap). **Yield gate still applies sideways** (a stone wall bears the load
  and blocks — §8.3).
- Conserve EXACTLY (grand + per-species) every step; honour the §8.7 min_mass cohesion gate (no cell left at
  `0 < mass < min`). Acceptance: **SCENE 2 dam-break GREEN** — the column slumps laterally, mass exact; and a
  poured flat pool of full cells spreads into adjacent air.

### 3. SCENE 1 (same-height 1000|500 partial-fill equalization) — DO NOT re-invent. Test, then ESCALATE if dead.
- After step 2, run SCENE 1 against the §8.5 mover. If the lateral displacement path **also** equalizes the
  partial-fill pair (a full-ish cell displacing into a lower-fill same-species neighbour), great — report it.
- If SCENE 1 stays RED (both cells read EOS `p=0` at/under rest density — §B.1 — so there is genuinely **no
  spec-defined driver** for same-height free-surface equalization): **STOP. Leave it RED/BANKED and ESCALATE
  to the controller** with the exact gap: *"§8.4+§B.1 give both cells p=0 ⇒ no lateral gradient; a free-surface
  head would fix it but the spec does not define one — needs a ratified spec amendment, not an ad-hoc term."*
  The controller/user will decide whether to amend the spec FIRST; only then does a follow-up implement it.

### 4. RE-VALIDATE `YG_B` for real (the T3 vacuous-pass debt).
- With lateral flow now alive: **remove the stone wall** between the two YG_B pools and prove they NOW level
  (lateral flow works); **restore the wall** and prove they DON'T (the wall genuinely bears the lateral load).
  If the wall fails to block once flow is real, fix the lateral yield gate here.

## STRICT DON'T
- ❌ Do NOT re-add `p_surf` / any free-surface / fill-fraction / "spread N blocks" head to make SCENE 1 green.
  If the spec has no driver, that is a **user design decision** — escalate. (You already did this once; don't.)
- ❌ Do NOT add a new Globals constant, an EOS compression band, a separate mass channel, or a stored-momentum
  kick. The mover is the existing advective flux routed by the §8.4 pressure difference — nothing new invented.
- ❌ Do NOT let a lateral displacement and T4's vertical swap both move the same mass on a face (flux-XOR-swap).
- ❌ Do NOT loosen conservation tolerances. A drift in these scenes is a real bug.
- ❌ Do NOT re-classify SCENE 2 as "deferred" again — it is spec-covered (§8.5) and is THIS task's job.

## Acceptance
- `p_surf` fully reverted (diff-clean vs `8701e36`); no regression in the other cheap tests.
- **SCENE 2 dam-break GREEN** via the §8.5 lateral mover; flat full-cell pool spreads into air; conservation exact.
- **SCENE 1**: GREEN via the same mover **or** a written escalation naming the §B.1 `p=0` spec gap (NOT a new term).
- `YG_B` re-validated for the right reason (wall-out levels / wall-in blocks).
- Report: the revert diff, which scenes are green vs escalated, the YG_B result, and the lateral-mover design in
  spec terms (§8.4/§8.5 citations). Three reviews (spec, quality, **adversarial conservation**) before push.
  In-game re-audit (now covering real pool-spread + swap) is the gate before T5+.
