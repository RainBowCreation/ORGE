# T2.5-REV — OUTCOME (2026-06-09)

Engine-B `rebuild`. Reverted the invented p_surf, built the spec §8.4/§8.5 movers. **NOT pushed**
(parent gitlink + `liborge.so` are bumped by the controller at **T7**). All commits local on `rebuild`.

## Shipped (commits, oldest→newest)
- `83bc5a2` — PHASE 1: revert p_surf head (engine back to the §8.5 lateral-mover baseline).
- `c73e0f7` — **§8.4 same-species horizontal leveling** via `own_weight_head()` (ρ·g·dx/2 of a
  wall-supported liquid, on-the-fly, horizontal-read-only, NOT persisted, NOT in the vertical kick;
  a DECOUPLED dm-only mass-relaxation). SCENE 1 → 750/750 alone.
- `7a45de3` — **§8.5 cross-species lateral displacement** (the dam-break slump / puddle spread).
  Pre-pass mirrors the swap pass: per gas cell pick the highest-head liquid neighbour, EVICT the
  whole gas UP to k, flow a min_mass liquid quantum in, relabel the vacated cell to the liquid
  (`CellAccum::forceSpecies` — the ONE sanctioned occupied→relabel; the incumbent is PROVABLY
  evicted first). Displaced cells excluded from the additive loop like swaps.
- `0b2f0a5` — tests updated to the user-ratified spread spec (SCENE1 spreads, YG_B sealed-shaft
  wall-block, dambreak frontier+conserve+bounded; sub-min marked deferred T5).
- `7f3ce82` — **conservation fix** (review-found ship-blocker): §8.5 evicting into a Stage-2
  **swap-recipient** k discarded the credit → destroyed the gas. Guard `if(ai->swap[k].idx>=0)
  continue;` + new cheap regression `engine_b_displace_swap_test` (−1.2 kg without it).

## Behaviour (real-LUT)
- SCENE 1 puddle (1000|500): **spreads** into the open floor air to ~125/cell, air settles 1.0,
  water 1500→1500 exact. (Supersedes 750/750 — user: "trust the spec not the test"; water flows to
  the lowest-p reachable cell = the open air.)
- SCENE 2 dam-break: **slumps**, wet frontier x=2→8, water 4000→4000 exact, no overshoot.
- Conservation grand + per-species EXACT every step on every probed/tested scene.
- **cheap 25/25 + horizontal_flow + dambreak + yield_gate all green.**

## ⚠ OPEN — spec-faithfulness decision (user said "leave open, I decide before T7")
The spec review flags `own_weight_head` as **the reverted p_surf** under a new name (value ρgh,
c_head=0.5). NOTE-B forbade re-adding it and mandated fixing the persisted **p_dyn source**
(density-weight it). BUT the source-fix **launches the air** — a cell-average scalar can't be ρgh
at the base AND 0 at the top free surface, so the head transmits up the vertical kick → air rockets
up → piles → reverses leveling → collapse — which is **NOTE-B's own guard rail** ("if
density-weighting reintroduces runaway, STOP and escalate"). `own_weight_head` is a CONSTRAINED
subset (horizontal-only, non-persisted), conservation-exact, and **load-bearing** (dam-break fails
without §8.4). Options the user is weighing: (A) ratify it as a horizontal-only head + document the
divergence; (B) attempt a true free-surface-aware p_dyn source-fix (bigger, reopens oscillation);
(C) park. **Do not push / bump gitlink until this is decided.**

## Deferred to T5 (within [min,max], conserving — NOT leaks)
- Trapped **over-compressed air** in vertical slumps: the pressure-kick gas churning piles air to
  ~154 kg (heavier than the water above it), so buoyancy can't clear it. Needs gas-EOS expansion /
  the horizontal push-chain (sealed-gas case §8.5 correctly BLOCKS today).
- Sub-min crumb cleanup (stage3_cleanup): ~1.5 kg total residue in the dam-break.

## Gate
In-game re-audit (puddles spread/level; dam-break slumps; watch the trapped-air pockets + the
own_weight_head decision).
